---
description: "Task list for the Civilization-Playing Harness (feature 002)"
---

# Tasks: Civilization-Playing Harness

**Input**: Design documents from `/specs/002-civ-playing-harness/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md),
[data-model.md](./data-model.md), [contracts/](./contracts/)

**Tests**: Test tasks ARE included. The plan specifies a four-tier `pytest` strategy
(`tests/unit`, `tests/contract`, `tests/integration`, `tests/live`), the contracts name specific
conformance suites, and the spec makes several audits release-blocking (SC-006, SC-008, SC-009,
SC-018, SC-019). Tests are a requirement of this feature, not an option.

**Organization**: Tasks are grouped by user story so each story can be implemented, tested, and
demoed independently.

**Revision 3** — regenerated against plan.md revision 3, which corrected a factual error about
platform support: the tuner interface the harness speaks to ships in the native **Windows, macOS,
and Linux** builds, and only the Windows-only FireTuner *GUI* is Windows-bound — which the harness
never uses. Added a `HostPlatform` port and three adapters (T047–T055), per-platform capture spikes
and screening profiles, a support-tier gate, and platform as part of the seed set's pinned identity.
Task IDs were renumbered again; 205 tasks.

**Revision 2** — regenerated against plan.md revision 2, which re-planned the feature after the
spec's clarification session of 2026-09-19. Four decisions changed shape, and they touch most
phases: the turn became an **interactive decision-step loop** (FR-008), the **per-turn time budget
was removed** in favour of a no-progress step count (FR-014), save retention became
**explicit-archival-only** (FR-036), and the **Civilization VI build is pinned** per seed set
(FR-002, FR-031). Task IDs were renumbered; do not map them against the previous revision.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story the task belongs to (US1–US5)
- Every task names an exact file path

## Path Conventions

Single Python project, per plan.md Project Structure:

- `catalogs/` — the parity boundary as versioned YAML (not Python)
- `lua/gamecore/`, `lua/ingame/` — declared Lua as reviewable source
- `src/civsim_harness/` — the harness package, portable on every target platform
- `src/civsim_harness/host/` — **the only place an OS-specific import may appear** (research R19)
- `tests/{unit,contract,integration,live}/` — the four test tiers

Paths in task descriptions use POSIX separators. Where a task names a platform-specific location it
is resolved through the host port at runtime, never hard-coded.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [X] T001 Create the repository skeleton per plan.md Project Structure: `catalogs/observations/`, `catalogs/actions/`, `lua/gamecore/`, `lua/ingame/`, `src/civsim_harness/{config,nexus,capability,parity,observe,act,agent,provider,run,saves,resilience,store,operator,telemetry,models}/`, and `tests/{unit,contract,integration,live,fakes}/`, with an `__init__.py` in every Python package
- [X] T002 Initialize the `uv`-managed Python 3.12+ project in `pyproject.toml` with **portable** runtime dependencies only (`pydantic>=2`, `httpx`, `psutil`, `typer`, `fastapi`, `uvicorn`, `SQLAlchemy`, `Pillow`, `PyYAML`) and dev dependencies (`pytest`, `pytest-asyncio`, `syrupy`, `ruff`, `mypy`). Platform libraries go in T003's optional groups, never the base set — a macOS install must not pull `pywin32` (research R19)
- [X] T003 Declare per-platform optional dependency groups in `pyproject.toml` with environment markers: `windows` (`pywin32`, `winsdk`, `pydirectinput`), `macos` (`pyobjc` Quartz/ScreenCaptureKit bindings), `linux` (X11/XTest and portal-PipeWire bindings), so `uv sync` installs only the current host's extras (research R19)
- [X] T004 [P] Configure `ruff` and `mypy` (strict on `src/civsim_harness`) in `pyproject.toml`
- [X] T005 Configure `pytest` in `pyproject.toml`: `asyncio_mode = "auto"`, register the `live` marker, and set `addopts` so `tests/live` is excluded by default per plan.md Testing
- [X] T006 [P] Create `.gitignore` excluding the secrets file, `.env`, the local store database, capture blobs, and copied `.Civ6Save` files
- [X] T007 [P] Create `.github/workflows/ci.yml` running `uv run pytest tests/unit tests/contract tests/integration` as a **matrix across `windows-latest`, `macos-latest`, and `ubuntu-latest`** (the live tier stays excluded, per quickstart.md "CI-runnable subset"). The matrix is what catches an accidental OS-specific import in the portable core long before the live tier would (research R19)
- [X] T008 Add a lint rule in `pyproject.toml` (ruff `flake8-tidy-imports` banned-api or equivalent) forbidding imports of `pywin32`, `win32*`, `winsdk`, `pydirectinput`, `Quartz`, `AppKit`, `Xlib`, and `ctypes.windll` anywhere outside `src/civsim_harness/host/`, so "is the core platform-neutral?" is answerable from the import graph rather than by reading thirteen subpackages (plan Structure Decision, research R19)
- [X] T009 [P] Create `catalogs/VERSION` containing the single-line initial catalog version `2026.09.1` per contracts/capability-catalog.md
- [X] T010 [P] Create `configs/turn50-validation.yaml` as the example run configuration in the exact format of contracts/run-configuration.md (`schema_version: 1`, `stop_condition.type: turn_reached`, `turn: 50`, `no_progress_step_limit: 8`, `recovery_attempt_limit: 3`, `min_free_disk_gb: 25`). **No `turn_time_budget_s` field** — a turn has no time bound (FR-014)
- [X] T011 [P] Create `configs/seedsets/shuffle-classic-2026q3.yaml` as the example seed set in the format of contracts/run-configuration.md "Seed set definition", including `game_build` and an empty `accepted_build_changes` list (FR-031)
- [X] T012 Create the `civsim` console-script entry point in `pyproject.toml` wired to a Typer app stub in `src/civsim_harness/operator/cli.py`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The parity boundary machinery, the record schemas, the game transport, and the store
port. Nothing can reach the game or the agent except through these, so no user story can begin
until they exist.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

**Telemetry and errors**

- [X] T013 Define the exception hierarchy in `src/civsim_harness/errors.py`: `HarnessError`, `PreflightError`, `CatalogError`, `NexusError`, `StoreWriteError`, `ParityViolation`, `ProviderChainExhausted`, `RecoveryLimitReached`, `ObservationAssemblyError`, `BuildMismatchError`, `DiskHeadroomError`
- [X] T014 Implement the credential redaction filter in `src/civsim_harness/telemetry/redaction.py`, applied to every record, log line, and exception payload — enforced as a filter, not a convention (FR-043, SC-018, invariant I8)
- [X] T015 Implement out-of-game structured logging in `src/civsim_harness/telemetry/logging.py` with the T014 redaction filter mandatory on every handler; harness telemetry is recorded here and never as game information (FR-020)
- [X] T016 [P] Unit test in `tests/unit/test_redaction.py`: key-shaped values planted in config, responses, and raised exceptions appear in no serialized record, log line, or traceback (SC-018)

**Record models**

- [X] T017 [P] Define shared value types in `src/civsim_harness/models/common.py`: id types, `ModRef` (id + version), `ModelRef` (provider + model), `Cost`, `CatalogVersionRef`, `BuildAcceptance` (`acceptance_id`, `from_build`, `to_build`, `accepted_by`, `accepted_at`, `reason`), timestamp aliases
- [X] T018 [P] Define `SeedSet`, `RunConfiguration`, `GuidanceSet`, `StopCondition`, and `ModelConfig` pydantic models in `src/civsim_harness/models/config.py` per data-model.md §1–3: `SeedSet.seeds` non-empty; `civilization`, `leader`, `ruleset`, `mod_set` immutable after creation (seeds may be appended); `SeedSet.game_build` required and `accepted_build_changes` defaulting to empty; `StopCondition` is exactly one of `turn_reached(n)` / `game_outcome` / `operator_stop`; `ModelConfig` carries `primary`, ordered `fallbacks` (possibly empty), `request_params`, and **no credentials**; `mod_set` empty list is meaningful, not absent. Together with T019 these models are the complete recorded definition FR-001 requires before a run may start
- [X] T019 Define `RunConfiguration`'s bound fields in `src/civsim_harness/models/config.py` per data-model.md §2: `no_progress_step_limit` (int, ≥ 1, **no upper bound**), `recovery_attempt_limit` (int), `min_free_disk_gb` (number). **A `turn_time_budget_s` field must not exist** — data-model.md §2 states its reappearance is a regression, not an addition (FR-014)
- [X] T020 [P] Define `Run` and its enums in `src/civsim_harness/models/run.py` per data-model.md §4: `lifecycle_state` ∈ {preparing, playing, waiting_on_model, waiting_on_game, paused, interrupted, resuming, finished, failed}; `record_completeness_status` ∈ {complete, has_gaps, unknown}; `comparability_status` ∈ {comparable, visually_degraded, not_comparable}; plus `observation_catalog_version`, `action_catalog_version`, `parent_run_id`, `parent_turn`, `client_identity`, `game_build`, `game_build_acceptance_ref` (nullable), `archived_at` (nullable), `capture_path`
- [X] T021 [P] Define `TurnCycle` in `src/civsim_harness/models/turn.py` per data-model.md §5: `turn_number ≥ 1`; `attempt_index` starts at 0; `save_point_id` **non-nullable**; `step_count` ≥ 1 and **unbounded above**; `outcome` ∈ {ended_by_agent, ended_on_no_progress, abandoned}; `final_no_progress_streak`; `visually_degraded` derived from its steps; `yields`; `persisted_at`. **No `observation_id`** — observations are step-scoped
- [X] T022 Define `DecisionStep` in `src/civsim_harness/models/turn.py` per data-model.md §6: `decision_step_id`, `turn_cycle_id`, `step_index` (1-based, contiguous), `observation_id` required, `decision_id` required, `model_call_id` required, `progress` ∈ {changed_state, no_change, rejected}, `no_progress_streak_after`, `visually_degraded`, `started_at`/`ended_at`. Exactly one decision and one model call per step (invariant I13)
- [X] T023 Define `Observation`, `ObservationEntry`, and `ScreenCapture` in `src/civsim_harness/models/turn.py` per data-model.md §7–8: `Observation.decision_step_id` (step-scoped, **not** turn-scoped); `ObservationEntry.declaration_id` required with `context` ∈ {GameCore_Tuner, InGame}; `ScreenCapture` binds `run_id`, `turn_number`, **and `decision_step_id`**, with `screening_status` ∈ {screened_clean, withheld}, `withheld_reason` ∈ {non_player_ui, geometry_mismatch, provenance_failure, capture_failed}, and `blob_ref` null when withheld
- [X] T024 [P] Define `Decision` and `ActionExecution` in `src/civsim_harness/models/decision.py` per data-model.md §9: `decision_step_id` (replacing any turn-level `order_index`); `action_declaration_id` and `model_call_id` both required; `trigger` ∈ {proactive, prompt_response} with `prompt_type` set when `prompt_response`; `is_end_turn` bool; `outcome` ∈ {applied, rejected, partially_applied}; `rejection_reason` ∈ {not_in_catalog, unavailable_to_human_now, illegal_in_context, out_of_parity_camera, verification_failed}
- [X] T025 [P] Define `ParityDeclaration`, `CatalogVersion`, and `IntegrationCapability` in `src/civsim_harness/models/catalog.py` per data-model.md §10–11: `parity_basis` required and non-empty; `kind` ∈ {observation, view, action}; `camera_requirements` (`mode` ∈ {world, strategic, city_screen, diplomacy, congress}, `zoom_range`, `target_must_be_revealed`) and `screening_profile` required when `kind == view` and absent otherwise; `path` ∈ {firetuner, bespoke}; `firetuner_gap` required and non-empty when `path == bespoke`
- [X] T026 [P] Define `ModelCall`, `SavePoint`, and `RunEvent` in `src/civsim_harness/models/records.py` per data-model.md §12–14: `ModelCall` carries `decision_step_id` and `outcome` ∈ {decision_returned, empty_response, failed, rate_limited, context_rejected} — **singular `decision_returned`**, since one call serves one step; `SavePoint.retention_status` ∈ {retained, eligible, removed} with `verified` and `missing` flags; `RunEvent` carries an optional `step_index` and an `event_type` covering the full 28-value enum in data-model.md §14 (including `turn_ended_on_no_progress`, `observation_assembly_failed`, `run_archived`, `game_build_change_accepted`, and `disk_headroom_low`, and **excluding** the removed `stall`)
- [X] T027 Implement JSON Schema export in `src/civsim_harness/models/export_schemas.py`, writing every record schema into `specs/002-civ-playing-harness/contracts/schemas/` with its schema version
- [X] T028 Contract test in `tests/contract/test_record_schemas.py`: every record model round-trips its published JSON Schema, and a schema change that removes or retypes a field fails the build (additive-only, per contracts/match-store-port.md "Schema evolution")

**Game transport**

- [X] T029 Implement the Nexus wire codec in `src/civsim_harness/nexus/codec.py`: `length` uint32 LE (payload size **including** the null terminator), `tag` int32 LE, NUL-terminated UTF-8 payload, `TAG_HANDSHAKE = 4`, `TAG_COMMAND = 3` (contracts/nexus-protocol.md)
- [X] T030 [P] Unit test in `tests/unit/test_nexus_codec.py`: frame/parse round-trips, payloads fragmented across packets, short and oversize frames
- [X] T031 Implement connection and handshake in `src/civsim_harness/nexus/client.py`: connect `127.0.0.1:4318`, send `APP:`, send `LSQ:`, resolve `GameCore_Tuner` and `InGame` state indices, and surface connection-refused as a *preparation* failure rather than a run failure
- [X] T032 Implement request/response discipline in `src/civsim_harness/nexus/client.py`: per-request nonce `---BEGIN:<nonce>---` / `---END:<nonce>---` wrapping, JSON document extraction between matched pairs, unmatched output routed to telemetry and never returned as a result, a **per-command timeout** (one of the per-operation bounds that satisfy SC-010 without a turn timer, research R12), and a lock serializing access to the single socket
- [X] T033 [P] Unit test in `tests/unit/test_nexus_sentinels.py`: stray game prints, a previous request's tail, and output with no matching nonce are all discarded to telemetry rather than parsed as results
- [X] T034 Implement the heartbeat probe in `src/civsim_harness/nexus/heartbeat.py`: periodic nonce round-trip through `GameCore_Tuner` with a bounded timeout

**The parity boundary machinery**

- [X] T035 Implement the catalog YAML loader in `src/civsim_harness/capability/loader.py` enforcing all seven load-time validations from contracts/capability-catalog.md — non-empty `parity_basis`; every `capability_id` resolves and every `path: bespoke` has a non-empty `firetuner_gap`; `declaration_id` unique across all files; actions have both `availability_predicate` and `verification_predicate`; observations and views have a valid `output_schema`; predicates reference only exposed symbols; `catalogs/VERSION` present. **All failures abort startup, not turn 1.**
- [X] T036 Implement catalog versioning in `src/civsim_harness/capability/version.py` (content hash over all catalog files, `declaration_ids` set) and the registry with wrong-context execution refusal in `src/civsim_harness/capability/registry.py` (FR-022, research R3)
- [X] T037 [P] Unit test in `tests/unit/test_catalog_load.py`: a missing `parity_basis` fails load; `path: bespoke` with an empty `firetuner_gap` fails load; duplicate `declaration_id` fails load; an action missing either predicate fails load; an observation missing `output_schema` fails load; executing an entry in the wrong Lua context is refused

**Store and provider ports**

- [X] T038 Define the `MatchStore` Protocol in `src/civsim_harness/store/port.py` exactly as specified in contracts/match-store-port.md — nine writes (including `archive_run`), eight reads (including `step_gaps` and `list_eligible_save_points`), and `ping()`
- [X] T039 Implement the SQLite + content-addressed blob reference adapter in `src/civsim_harness/store/sqlite_adapter.py` satisfying D1–D6: durable-before-return, raise on any failure, atomic whole-turn `write_turn_cycle` carrying **every decision step in order**, idempotency on `(run_id, turn_number, attempt_index)`, captures and save-point references through the same port, and `ping()`. Nothing a record depends on may exist only in local or ephemeral form — the adapter is a storage implementation of the port, not a second bypassing path (FR-051, D5). A turn of several hundred steps must round-trip without truncation
- [X] T040 Implement archival and eligibility in `src/civsim_harness/store/sqlite_adapter.py` per contracts/match-store-port.md A1–A4: `archive_run` sets `Run.archived_at`, writes a `run_archived` event, and transitions that run's save points to `eligible`; it is rejected on a non-terminal run; **nothing else may set `archived_at` or produce an `eligible` save point** — no TTL, no age rule, no quota, no retention window, no thinning (FR-036)
- [X] T041 Port conformance suite in `tests/contract/test_match_store_port.py` asserting D1–D6, A1–A4, idempotency under repeated writes, parent-immutability rejection, `turn_gaps` **and `step_gaps`**, step-order preservation on read-back, authoritative-attempt selection, and that a failed write surfaces as a raise rather than a falsy return
- [X] T042 Adversarial store tests in `tests/contract/test_match_store_port.py`: a turn of several hundred steps round-trips with its order intact and no truncation, and a finished-but-unarchived run's save points never appear in `list_eligible_save_points()` no matter how old the run is (invariant I17)
- [X] T043 Define the `ModelProvider` Protocol plus `ModelCapabilities`, `DecisionRequest` (carrying `step_index`), and `DecisionResponse` (carrying a **singular** `decision`, not a list) in `src/civsim_harness/provider/port.py` exactly as specified in contracts/model-provider-port.md

**Lifecycle and guards**

- [X] T044 Implement the run lifecycle state machine in `src/civsim_harness/run/lifecycle.py` per data-model.md §4, emitting a `lifecycle_transition` `RunEvent` on every transition (FR-003)
- [X] T045 [P] Unit test in `tests/unit/test_lifecycle.py`: legal and illegal transitions, an event recorded for every transition, `finished` requiring exactly one `stop_resolution`, and `archived_at` being orthogonal to `lifecycle_state` — an archived run keeps its records and stays readable (data-model.md §4)
- [X] T046 Implement the write-before-advance guard in `src/civsim_harness/store/guard.py`: the end-turn action is reachable only downstream of an acknowledged durable write, and a failed write raises and halts the run (FR-013, invariant I3)

**The host platform port — the only place OS-specific code may live**

- [X] T047 Define the `HostPlatform` Protocol in `src/civsim_harness/host/port.py` covering exactly the six capabilities the harness needs from an OS (research R19): locate the game process, identify its window, capture that window, resolve game directories (saves + `AppOptions.txt`), optional synthetic input, and free disk space. Nothing else belongs here, and nothing outside `host/` may import a platform library
- [X] T048 Implement platform and session detection in `src/civsim_harness/host/detect.py`: resolve OS, and on Linux distinguish **X11 from Wayland**, since they have different capture paths and Wayland blocks synthetic input entirely (research R19)
- [X] T049 Implement support-tier resolution in `src/civsim_harness/host/detect.py`: probe the adapter's capabilities and assign `VALIDATED` (full capability, capture-hygiene spike passed), `SUPPORTED` (verified quicksave path but no passing capture spike — runs visually degraded), or `UNSUPPORTED` (no verified quicksave path). **A platform is `UNSUPPORTED` until probed** — the safe default (research R19)
- [ ] T050 [P] Implement the Windows host adapter in `src/civsim_harness/host/windows/`: `pywin32` window identity, Windows.Graphics.Capture, SendInput, and `%USERPROFILE%\Documents\My Games\Sid Meier's Civilization VI\` directory resolution. *(Audit 2026-09-21, Linux node: PARTIAL — `pywin32` identity, the rank-3 `PrintWindow` capture with real `BGRA8` extraction (T224), `pydirectinput` input, and evidence-based directory resolution are landed in `host/windows/adapter.py`; the rank-1 Windows.Graphics.Capture path returns `None` by design (the `winsdk` interop factory could not be confirmed off-platform). Other-platform: cannot be closed from the Linux node; on-platform verification is T198.)*
- [ ] T051 [P] Implement the macOS host adapter in `src/civsim_harness/host/macos/`: Quartz window list, ScreenCaptureKit with an `SCContentFilter` scoped to the single game window, `CGEvent` input, and `~/Library/Application Support/Sid Meier's Civilization VI/` directory resolution. Detect and report missing **Screen Recording** and **Accessibility** grants as an actionable preflight failure rather than an opaque capture error. *(Audit 2026-09-21, Linux node: PARTIAL — Quartz window list, the `CGWindowListCreateImage` fallback, `CGEvent` input, the Screen Recording preflight (`CGPreflightScreenCaptureAccess`) and directory resolution are landed in `host/macos/adapter.py`; ScreenCaptureKit raises `NotImplementedError` pending the missing `pyobjc-framework-ScreenCaptureKit` dependency and an on-platform R6 spike. Other-platform: no macOS machine exists anywhere in this project; cannot be closed from the Linux node.)*
- [X] T052 [P] Implement the Linux host adapter in `src/civsim_harness/host/linux/`: XComposite redirected-pixmap capture and `XTest` input on X11, `xdg-desktop-portal` ScreenCast on Wayland, and `~/.local/share/aspyr-media/Sid Meier's Civilization VI/` directory resolution. On Wayland, report synthetic input as unavailable rather than attempting it — the compositor blocks it by design (research R5, R19). *(Closed 2026-09-21 on the X11 half, all live-verified on the Linux node: XComposite capture (`spikes/r6-xcomposite-readback-linux.md`, 12 frames in the landed-code demo `run-9505f323`), `XTest` input (`spikes/r5-xtest-input-linux.md`; T248's single Escape), `focus_window`, the compositor precondition (`_NET_WM_CM_Sn`, T249), directory resolution (R5). Wayland: synthetic input is reported `unavailable` without being attempted, as the task requires; the `xdg-desktop-portal` ScreenCast **capture** path is NOT implemented — it raises and is reported `unavailable` with its reason — because the portal needs an interactive grant per research R6 and this node is X11. That remainder is tracked as its own task in Phase 13 (T257) rather than hidden behind this checkbox.)*
- [X] T053 `HostPlatform` port conformance suite in `tests/contract/test_host_platform_port.py`, running the **same** assertions against every adapter so "this platform is supported" is a test result rather than a claim: window identity resolves, capture returns a frame matching the client rect, directories resolve and exist, disk space reads, and an unavailable capability reports unavailable rather than raising an opaque error
- [X] T054 [P] Unit test in `tests/unit/test_platform_neutrality.py`: import every module under `src/civsim_harness/` except `host/` and assert none pulls a platform library, enforcing T008's rule at test time as well as lint time (research R19)

**Test doubles**

- [X] T055 [P] Implement the fake host adapter in `tests/fakes/fake_host.py` with scriptable capability availability and tier, so the turn cycle, preflight tier gate, and degradation paths run deterministically on any CI platform
- [X] T056 [P] Implement the recorded-transcript fake Nexus server in `tests/fakes/fake_nexus.py`, replaying captured request/response transcripts so the full turn cycle runs deterministically without Civ VI, with scriptable mid-turn drops and per-operation stalls (research R15)
- [X] T057 [P] Implement the fake `ModelProvider` in `tests/fakes/fake_provider.py` with scriptable outcomes: a single decision, empty response, rate limit, context rejection, failure, and the contract-violating multi-decision response T180 asserts against

**Checkpoint**: Boundary machinery, records, transport, and store exist — user story work can begin

---

## Phase 3: User Story 1 - Play an unattended run from a fixed seed to a stop condition (Priority: P1) 🎯 MVP

**Goal**: A configured run brings Civ VI to exactly the specified starting position, then plays turn
after turn on its own. Each turn is a loop of decision steps — observe, decide, execute, verify,
observe again — ending when the agent issues its declared end-turn decision or the no-progress
backstop trips, with a complete step-by-step record and no human interaction with the game client.

**Independent Test**: Configure a run on a known seed with a turn-50 stop condition, start it, and do
not touch the keyboard or the game client until it stops. Verify it reached turn 50, that every turn
from 1 to 50 has a persisted record containing every decision step in order — the state observed at
that step, the decision made, the reasoning given — plus the resulting yields, and that no turn and
no step is missing. Then repeat with the stop condition set to a game outcome and verify it plays
across era transitions to victory or defeat.

### Tests for User Story 1 ⚠️

> Write these first and confirm they fail before implementing

- [X] T058 [P] [US1] Integration test in `tests/integration/test_turn_cycle.py` against the fake Nexus and fake provider: the full cycle — quicksave → **multi-step loop** → persist → end turn — runs to a `turn_reached` stop with 50/50 authoritative turns, zero turn gaps, and zero step gaps (SC-001, SC-003)
- [X] T059 [P] [US1] Integration test in `tests/integration/test_decision_loop.py`: for each step *n*, the observation is assembled **after** step *n−1*'s effect was verified; each step has a distinct capture and exactly one model call; and no observation or capture is reused across steps (FR-008, FR-015, invariants I13, I14). Include the **self-cancellation** edge case: an agent that undoes its own earlier work later in the same turn, having seen the result, produces two steps both recorded as issued and both `changed_state`, and the turn's recorded yields reflect the **resulting** state rather than the intent (spec Edge Cases, data-model §6)
- [X] T060 [P] [US1] Integration test in `tests/integration/test_turn_endings.py`: a turn ends `ended_by_agent` when the agent issues `turn.end_turn`; a turn ends `ended_on_no_progress` when `no_progress_step_limit` consecutive rejected/no-change steps occur; a productive step mid-sequence **resets** the counter; the two endings are distinguishable in the record; and a one-step turn where the agent ends immediately is valid, not a failure (FR-008, FR-014, SC-022)
- [X] T061 [P] [US1] Unit test in `tests/unit/test_no_truncation.py`: a scripted 500-step productive turn completes untouched — **no wall-clock bound, no step cap, and no cost ceiling ends it** (FR-014, invariant I16). This is a negative test; every plausible-sounding guard rail it would catch is a violation
- [X] T062 [P] [US1] Unit test in `tests/unit/test_turn_state_machine.py`: a failed quicksave halts the run and the turn never comes into existence as an attempt; a failed persist halts the run before end-turn; a mid-turn observation-assembly failure at step *n* > 1 **abandons and replays** the attempt from its start quicksave rather than finishing from the last good view (invariants I2, I3, data-model.md §5 failure table)
- [X] T063 [P] [US1] Contract test in `tests/contract/test_run_configuration.py` asserting V1, V3, V7, V8, V9, V12, and V13 from contracts/run-configuration.md — a partially specified run does not start, a `seed_set` mismatch is an error rather than an override, an unreachable store is rejected, a second run on the same client or identity is rejected, a key-shaped value in configuration is a validation error, `no_progress_step_limit` ≥ 1 with no silent default cap, and an `UNSUPPORTED` host platform is rejected before turn 1 with the missing capability recorded, while a merely degraded one starts and is marked instead (FR-054, V13, exercised through the fake host of T055)
- [X] T064 [P] [US1] Contract test in `tests/contract/test_build_pin.py` asserting V10: a client build differing from the seed set's `game_build` fails the run before turn 1 with the mismatch recorded, and a matching `BuildAcceptance` permits it while recording `game_build_acceptance_ref` on the run (FR-002, FR-031)
- [X] T065 [US1] Contract test in `tests/contract/test_build_pin.py` for the platform half of the composite pin: a **platform** difference fails preflight exactly as a version difference does, and an acceptance of `win/1.0.12.9 → win/1.0.12.11` does **not** also permit `win → mac`. A set carrying any accepted platform change must report as non-uniform with its runs partitioned by platform (FR-002, FR-031, research R20)
- [X] T066 [P] [US1] Integration test in `tests/integration/test_preparation.py`: when the fake game reports a setting differing from the configuration, the run fails before turn 1 with a `preparation_mismatch` event recorded and no turn 1 attempted (FR-002, V2, US1 §2)
- [X] T067 [P] [US1] Integration test in `tests/integration/test_prompts.py`: each declared prompt type is answered as a `prompt_response` decision at its own decision step, and an unrecognised screen stalls the run visibly rather than being dismissed, defaulted, or absorbed (FR-010, FR-049, SC-005)
- [X] T068 [P] [US1] Integration test in `tests/integration/test_stop_conditions.py`: exactly one stop condition is recorded, and a stop condition coinciding with a victory, defeat, or crash on the same turn records one and leaves the others as events (FR-005, invariant I10)

### Implementation for User Story 1

**Configuration and preparation**

- [X] T069 [US1] Implement the run configuration loader in `src/civsim_harness/config/run_config.py`: parse the contracts/run-configuration.md YAML, check `schema_version`, and apply V1 (every required field present and non-null), V9 (no credential-shaped value present), and V12 (`no_progress_step_limit` ≥ 1)
- [X] T070 [US1] Implement seed set loading in `src/civsim_harness/config/seed_set.py` with the V3 agreement check — when `seed_set` is set, `civilization`, `leader`, `ruleset`, and `mod_set` must match the set — and loading of `game_build` and `accepted_build_changes` (FR-031)
- [X] T071 [US1] Implement the game build read in `src/civsim_harness/observe/game_build.py` following the research R18 spike: a declared `GameCore_Tuner` version observation where reachable, otherwise the client binary's version via the `HostPlatform` port. The value is a **composite of platform and version** (e.g. `mac/1.0.12.9`), since the Aspyr ports are separately built binaries whose numbering need not track the Windows build's. Either way it is **out-of-game provenance** and never enters the agent's context (FR-020, R18, R20)
- [X] T072 [US1] Implement the build-pin preflight in `src/civsim_harness/run/preparation.py` (V10): compare the client's build against the seed set's `game_build`; a mismatch fails the run before turn 1 with the mismatch recorded, unless a `BuildAcceptance` covers that exact `from_build → to_build` transition, in which case `Run.game_build_acceptance_ref` is recorded. A run whose build differs from its seed set's without an acceptance reference must not be constructible (FR-002, invariant I18, R18)
- [X] T073 [US1] Implement guidance loading in `src/civsim_harness/config/guidance.py`: content-addressed hash recorded with the run, plus the runtime assertion that content is identical across runs referencing the same hash — guidance that varies per run is a contract violation (FR-021, V6)
- [X] T074 [US1] Implement secrets resolution in `src/civsim_harness/config/secrets.py`: keys resolve from the environment or a secrets file at call time only, never from run configuration (FR-043)
- [X] T075 [US1] Implement the run-identity lock in `src/civsim_harness/run/identity_lock.py` keyed to the client PID and run ID, refusing a second attach to an active client or run identity (FR-006, V8, research R4)
- [X] T076 [US1] Implement preparation in `src/civsim_harness/run/preparation.py`: apply the configured seed, civilization, ruleset, mod set, map and game settings, difficulty, and opponents; read the actual setup back through declared observations and compare **field by field**; any difference creates the run in `failed` state with the mismatch recorded and no turn 1 (FR-002, V2)

**Saves — required before any turn may proceed**

- [X] T077 [US1] Run the research R5 spike: enumerate callable save paths (`Network.*`, `UI.*`, `Game.*`) in the `InGame` and `GameCore_Tuner` contexts through the tuner, and record the enumeration output as gap evidence in `specs/002-civ-playing-harness/spikes/r5-save-path.md`. **This must run before save work begins**, to preserve Principle II's Firetuner-first ordering
- [X] T078 [US1] Implement the save capability in `src/civsim_harness/saves/save_game.py` following the R5 outcome — the FireTuner Lua path if one exists, otherwise a bespoke Save Game dialog driver with the R5 enumeration as its `firetuner_gap` statement (FR-007, FR-028, plan C1) *(Audit 2026-09-22: **R5 returned Outcome A**, so the conditional resolved to the FireTuner branch and the bespoke arm was never built. `saves.save_game` ships as `path: firetuner` / `lua/ingame/save_game.lua` / `firetuner_gap: null`, and `catalogs/actions/saves.yaml` records that under Principle II the positive result makes the dialog driver "forbidden, not merely unnecessary". This line previously named `src/civsim_harness/saves/dialog_driver.py` as the fallback's path; that module does not exist and must not be created, so the name is removed — an automated deliverable checker reading it would look for a file the catalog forbids. Complexity item C1 is discharged, not deferred.)*
- [X] T079 [US1] Implement filesystem save verification in `src/civsim_harness/saves/verify.py`: the `.Civ6Save` must appear in the save directory **resolved through the `HostPlatform` port** — never a hard-coded path — with its size stable across two reads before the turn proceeds; an unverifiable quicksave fails the turn rather than being recorded as taken (FR-007, SC-004, research R5, R19)
- [X] T080 [US1] Implement save point naming and record writing in `src/civsim_harness/saves/save_point.py` using `civsim__<run_id>__t<turn:04d>`, writing a `SavePoint` record through the store with `verified` set from T079 (FR-032)
- [X] T081 [US1] Implement the disk headroom check in `src/civsim_harness/saves/headroom.py`: estimate the run's save and capture footprint at preflight (V11) and check free space **before every quicksave**; below `min_free_disk_gb` the run halts in a recorded state with a `disk_headroom_low` event. **Nothing is deleted to make room** — no save is eligible until its run is archived (spec edge case, research R17)

**Declared Lua and catalog content — the game surface**

- [X] T082 [P] [US1] Write `GameCore_Tuner` read Lua for map, plots, units, and cities in `lua/gamecore/map.lua`, `lua/gamecore/units.lua`, and `lua/gamecore/cities.lua`, each emitting a single JSON document between the sentinels (contracts/nexus-protocol.md "Structured payloads")
- [X] T083 [P] [US1] Write `GameCore_Tuner` read Lua for research, civics, government, religion, diplomacy, congress, espionage, and great people in `lua/gamecore/research.lua`, `lua/gamecore/government.lua`, `lua/gamecore/religion.lua`, `lua/gamecore/diplomacy.lua`, `lua/gamecore/congress.lua`, `lua/gamecore/espionage.lua`, and `lua/gamecore/great_people.lua`
- [X] T084 [P] [US1] Write `InGame` Lua for unit movement, orders, and promotions in `lua/ingame/unit_orders.lua` (FR-009)
- [X] T085 [P] [US1] Write `InGame` Lua for city production, purchasing, and management in `lua/ingame/city_orders.lua` (FR-009)
- [X] T086 [P] [US1] Write `InGame` Lua for research, civics, policies, government, and governors in `lua/ingame/empire_orders.lua` (FR-009)
- [X] T087 [P] [US1] Write `InGame` Lua for religion (pantheon through founding and religious units), diplomacy/war/peace/city-states, espionage, great people, and World Congress in `lua/ingame/religion.lua`, `lua/ingame/diplomacy.lua`, `lua/ingame/espionage.lua`, `lua/ingame/great_people.lua`, and `lua/ingame/congress.lua` (FR-009)
- [X] T088 [P] [US1] Write `InGame` Lua for the screen-identity probe and prompt detection/response in `lua/ingame/screens.lua` (research R13)
- [X] T089 [P] [US1] Write `InGame` Lua for ending the turn and reading back the turn number in `lua/ingame/turn_control.lua`, supporting the `turn.end_turn` verification predicate (FR-008)
- [X] T090 [P] [US1] Author observation declarations in `catalogs/observations/{map,cities,units,research,diplomacy,religion,government,congress,espionage}.yaml`, each with a non-empty `parity_basis` naming the in-client action, a `context`, a resolving `capability_id`, a valid `output_schema`, and `introduced_in_version: "2026.09.1"` (FR-016)
- [X] T091 [P] [US1] Author action declarations in `catalogs/actions/{units,cities,research,policies,religion,diplomacy,congress,great_people}.yaml`, each with a non-empty `parity_basis`, an `availability_predicate` (when a human could do this right now) and a `verification_predicate` (how the effect is confirmed) (FR-017, FR-009)
- [X] T092 [P] [US1] Author the `turn.end_turn` declaration in `catalogs/actions/turn.yaml` exactly as specified in contracts/capability-catalog.md: `parity_basis` "Click the end-turn button in the lower-right action panel, or press its hotkey"; `availability_predicate` refusing an end-turn while a blocking prompt is up (so FR-010 requires the prompt be answered first); and a `verification_predicate` distinguishing a turn that actually ended from a click the game swallowed (FR-008)
- [X] T093 [P] [US1] Author prompt-response action declarations in `catalogs/actions/prompts.yaml` covering unit promotion, pantheon and religion selection, great person selection, AI diplomatic approach, declaration of war, city-state quest, World Congress vote, and era transition (FR-010, research R13)
- [X] T094 [P] [US1] Author the `IntegrationCapability` declarations in `catalogs/capabilities.yaml`, binding every declaration to its `lua/` file or module path with `path: firetuner | bespoke`, `reads`, `writes`, and a non-empty `firetuner_gap` for every bespoke entry (FR-027, FR-028, SC-020)

**Observe — per decision step**

- [X] T095 [US1] Implement structured state assembly in `src/civsim_harness/observe/assemble.py`: consume capability results only, validate each produced value against its declaration's `output_schema`, and attach the producing `declaration_id` to every `ObservationEntry`. **Assembly runs once per decision step**, with no incremental-update or caching path that would skip the filter or return a stale board (FR-012, FR-018, FR-024, invariant I14)
- [X] T096 [US1] Implement assembly-failure handling in `src/civsim_harness/observe/assemble.py`: a failure at step *n* > 1, after earlier steps have executed and verified, raises `ObservationAssemblyError`, records an `observation_assembly_failed` event, and abandons the attempt for replay — **the turn may not be finished from the last good view** (spec edge case, FR-046)
- [X] T097 [US1] Implement the screen-identity probe in `src/civsim_harness/observe/screen_identity.py`, probed between decision steps and after every interruption, with unrecognised screens surfaced as a recordable state rather than an exception (research R13)
- [X] T098 [US1] Implement capture-path selection in `src/civsim_harness/observe/capture_paths.py`: ask the `HostPlatform` adapter for its ranked paths and select the highest available, recording the chosen path on `Run.capture_path`. Each platform carries its own ranking per research R6 — Windows (WGC → DXGI+crop → `PrintWindow` → none), macOS (ScreenCaptureKit → `CGWindowListCreateImage` → none), Linux (XComposite on X11, portal/PipeWire on Wayland → none). The selector itself contains no OS-specific code
- [ ] T099 [US1] Run the research R6 capture-hygiene spike **once per target platform** and record each result in `specs/002-civ-playing-harness/spikes/r6-capture-hygiene-<platform>.md`: confirm that a console and unrelated windows placed over the client are absent from the captured frame, and that platform-specific chrome is excluded — the WGC capture border on Windows (`IsBorderRequired = false`), the menu bar and Dock on macOS, panels and notification toasts on Linux. **A pass on one platform carries no implication for another.** Until a platform's spike passes, no run on it may show images to the agent — those runs proceed visually degraded under FR-050 (research R6, R19). *(Audit 2026-09-21: Linux/X11 **PASSED** all four checks (`spikes/r6-capture-hygiene-linux.md`, after the Steam FPS overlay was disabled); Windows passed 3 of 4 with the same Steam FPS overlay still composited in-frame (`spikes/r6-capture-hygiene-windows.md`) — a re-run with that overlay off is a Windows-node item; macOS has never run. Other-platform: cannot be closed from the Linux node.)*
- [X] T100 [P] [US1] Author per-platform non-player-chrome profiles as **catalog data** in `catalogs/screening_profiles.yaml` — one per platform, enumerating what the content gate must reject: a Windows taskbar, a macOS menu bar and Dock, a Linux panel or notification toast, plus FireTuner chrome, developer consoles, and debug overlays on every platform. These are declarations, not code: T129 implements the gate that reads them, so the data precedes the code rather than the reverse. A platform with no profile resolves to the strictest available, never a permissive default (FR-025, FR-030, SC-009, SC-019, contracts/capability-catalog.md `screening_profile`)
- [X] T101 [US1] Implement the host-capability preflight gate in `src/civsim_harness/run/preparation.py` satisfying **FR-054** and **V13**: resolve the host tier via T049 and refuse an `UNSUPPORTED` run before turn 1, **recording which capability was missing**. FR-054 grounds this in FR-007 — a host with no verified quicksave path cannot complete a single turn — rather than in FR-002, which governs configured elements rather than host capability. A merely *degraded* capability must **not** refuse the run: a `SUPPORTED` host starts and marks its runs visually degraded under FR-050, which is the distinction FR-054's last sentence draws. The tier is recorded on the run so a degraded platform's data is never mistaken for a validated platform's (FR-054, V13, FR-007, FR-050, research R19)
- [X] T102 [US1] Implement the capture pipeline in `src/civsim_harness/observe/capture.py`: **one capture per decision step**, bound to `(run_id, turn_number, decision_step_id)` with its `camera_state` and `captured_at`. An earlier step's capture may **never** stand in for a later one — the board the agent is shown must reflect what it has already done this turn (FR-015). **No frame may be persisted before the screening gates of T129 exist.** FR-030 and SC-019 govern *stored* captures, not merely shown ones, so until US2 lands this pipeline records each step's capture as explicitly unavailable — which US1 §9 permits in as many words — and marks the step visually degraded under FR-050. US1 is a complete and honest increment without stored images; it would not be a complete one with unscreened stored images

**Agent and provider**

- [X] T103 [US1] Implement agent context assembly in `src/civsim_harness/agent/context.py`: system role plus out-of-game guidance plus this step's parity-filtered structured state. Images are attached only once screening exists (US2); harness telemetry is never included (FR-020, FR-021, FR-024)
- [X] T104 [US1] Implement the decision response schema and parsing in `src/civsim_harness/agent/decisions.py`: **exactly one** decision carrying its stated reasoning, with malformed, multi-decision, or decision-free output treated as a failed call rather than a decision to do nothing (FR-008, FR-012, P4, P10)
- [X] T105 [US1] Implement the OpenRouter adapter's `complete()` in `src/civsim_harness/provider/openrouter.py` over `httpx` using the OpenAI-compatible chat completions shape with images as `image_url` content parts — **no vendor SDK on the decision path** — mapping a successful-but-empty response to `CallOutcome.empty_response`, and coupling no timeout to the turn (only the request timeout and its retry ladder) (FR-037, P4, P11)

**Act**

- [X] T106 [US1] Implement the restricted predicate evaluator in `src/civsim_harness/act/predicates.py` exposing a fixed symbol table with no arbitrary evaluation (contracts/capability-catalog.md load-time rule 6)
- [X] T107 [US1] Implement action dispatch in `src/civsim_harness/act/dispatch.py`: resolve the step's single requested action to a catalog declaration, evaluate its `availability_predicate`, and reject an unregistered action as `not_in_catalog` and an unavailable one as `unavailable_to_human_now`, with the reason recorded and the action never performed. A rejection also increments the turn's no-progress counter (FR-017, FR-014)
- [X] T108 [US1] Implement execution verification in `src/civsim_harness/act/verify.py`: read state back through `GameCore_Tuner`, evaluate the declaration's `verification_predicate`, and **derive** `applied | rejected | partially_applied` from the result — never assert it from the executor. The same verification result determines the step's `progress` ∈ {changed_state, no_change, rejected}, so a weak predicate both mis-records the action and disables the backstop (FR-011, FR-014, invariant I4, research R14)
- [X] T109 [US1] Implement prompt and interrupt handling in `src/civsim_harness/act/prompts.py`: game-initiated prompts and between-turn interrupts route through `catalogs/actions/prompts.yaml` as `prompt_response` decisions at their own decision steps; an unrecognised screen records an `unknown_screen` event and stalls the run visibly rather than clicking or dismissing (FR-010, FR-049, SC-005)

**Run loop**

- [X] T110 [US1] Implement the decision-step loop in `src/civsim_harness/run/decision_loop.py` per research R14: for step *n* = 1, 2, 3 … unbounded — assemble a fresh observation and capture, obtain one decision and its reasoning, execute, verify, then assemble a new observation reflecting that effect before asking for the next. The agent is **never** asked to commit to a later decision before seeing the earlier one's result (FR-008, invariants I13, I14)
- [X] T111 [US1] Implement no-progress accounting in `src/civsim_harness/run/no_progress.py` per data-model.md §5: increment the counter on a step whose decision was rejected or whose verification showed no game-state change, **reset it to zero** on any step verified as having changed state, and end the turn once it reaches `no_progress_step_limit`. Record `final_no_progress_streak` and a `turn_ended_on_no_progress` event (FR-014, SC-022)
- [X] T112 [US1] Implement loop termination in `src/civsim_harness/run/decision_loop.py`: the loop exits **only** on the agent's `turn.end_turn` decision (`outcome = ended_by_agent`) or the T111 backstop (`outcome = ended_on_no_progress`). There is no timed exit, no step cap, and no cost ceiling; a turn must not be ended for any other harness-initiated reason nor truncated because it has grown long or expensive (FR-008, FR-014, invariant I16)
- [X] T113 [US1] Ensure the backstop synthesises no decision in `src/civsim_harness/run/no_progress.py`: a turn ended on no progress records the outcome on the `TurnCycle` and **does not** write an `end_turn` decision to represent it — fabricating one would be a defaulted move recorded as an agent decision and would destroy the distinction SC-022 requires (FR-042, SC-012, SC-022)
- [X] T114 [US1] Implement the turn cycle orchestrator in `src/civsim_harness/run/turn_cycle.py` — quicksave → T110 loop → persist → end turn — as the **only** component permitted to issue the end-turn action, sequenced strictly after an acknowledged store commit, halting the run on quicksave or persistence failure (FR-007, FR-008, FR-013, invariants I2, I3)
- [X] T115 [US1] Implement stop resolution evaluation in `src/civsim_harness/run/stop.py`: resolve the run's configured `StopCondition.type` (`turn_reached` | `game_outcome` | `operator_stop`) to exactly one recorded `Run.stop_resolution` — `turn_reached`, `victory`, `defeat`, `operator_stop`, or `unrecoverable_failure` — with coincident conditions written as events (FR-005, invariant I10)
- [X] T116 [US1] Implement the run orchestrator in `src/civsim_harness/run/runner.py` driving preparing → playing across every era to the stop condition, recording each lifecycle transition, and handling the game auto-advancing a turn by recording what actually happened rather than what was planned (FR-003, FR-009, SC-002)
- [X] T117 [US1] Wire per-turn record persistence in `src/civsim_harness/run/turn_cycle.py`: the whole `TurnCycleRecord` — **every decision step in order**, each with its observation, entries, capture references, single decision, reasoning, execution outcome, and model call, plus the turn's resulting yields — written as one atomic unit before the turn ends. The record is never summarised, sampled, or streamed incrementally, however many steps the turn contained (FR-012, FR-013, D3)

**Operator surface**

- [X] T118 [US1] Implement the Typer CLI in `src/civsim_harness/operator/cli.py`: `run start`, `run pause`, `run resume`, `run stop`, `run status`, and `doctor` (FR-004, contracts/operator-surface.md)
- [X] T119 [US1] Implement the loopback HTTP endpoint in `src/civsim_harness/operator/api.py` bound to `127.0.0.1` only, with a **closed** status response schema carrying `run_id`, `lifecycle_state`, `requested_state`, `current_turn`, `current_step`, `last_known_good_save`, `last_error`, `connection_health`, `record_completeness_status`, `comparability_status`, `archived`, and `disk_headroom_gb` — and no turn records, observations, decisions, reasoning, yields, metric series, or captures (FR-053, Principle VI)
- [X] T120 [US1] Implement command handling in `src/civsim_harness/operator/commands.py`: every command recorded as a `lifecycle_command_received` event **before** it takes effect, with pause and stop landing on a turn boundary rather than mid-turn. A pause requested during a long turn waits for that turn to end and is surfaced through `requested_state`; the harness must **not** cut the turn short to honour it faster (FR-004, FR-008, SC-022)
- [X] T121 [US1] Implement `doctor` in `src/civsim_harness/operator/doctor.py` reporting tuner connection with resolved state indices, client liveness **with its build**, store reachability, catalog version with declaration and undeclared counts, capture path with hygiene-spike status, disk headroom, and provider key *presence* only (never its value) (quickstart.md Setup, FR-043)

**Checkpoint**: A run plays itself from turn 1 to its stop condition with a complete step-by-step record — the MVP

---

## Phase 4: User Story 2 - Prove the agent played within human parity (Priority: P2)

**Goal**: Every observation and action resolves to a versioned parity declaration, images enter the
agent's context only after screening, and an auditor can verify the whole boundary from the run's
record alone.

**Independent Test**: Take one completed run and, working only from its record, enumerate every
distinct observation the agent received — structured and visual — and every distinct action it
issued; verify each resolves to a declared parity basis, that every image it saw was screened, and
that no observation or action in the run lacks a declaration.

### Tests for User Story 2 ⚠️

- [X] T122 [P] [US2] Parity red-team suite in `tests/contract/test_parity_redteam.py`: a fixture list of forbidden values — unrevealed map contents, opponent internal state, hidden AI intent, undisclosed opponent research or civics, unit/city data beyond what the standard UI reveals, RNG state, debug and provenance data — asserted absent from contexts assembled from realistic transcripts, **at every decision step, not once per turn**. Any finding fails the build (FR-019, SC-006, SC-008, research R15)
- [X] T123 [P] [US2] Contract test in `tests/contract/test_catalog_coverage.py`: preflight aborts when any reachable capability lacks a declaration, and the run record carries the catalog version and content hash in force (FR-022, FR-023, SC-007)
- [X] T124 [P] [US2] Unit test in `tests/unit/test_image_screening.py`: each of the four gates (source, geometry, provenance, content) independently withholds, and a withheld image is never stored and never shown (invariant I6, SC-019)
- [X] T125 [P] [US2] Unit test in `tests/unit/test_camera_validation.py`: an unrevealed target plot, an out-of-range zoom, and a non-human view mode are each rejected with `out_of_parity_camera` and recorded (FR-026, SC-009)
- [X] T126 [P] [US2] Unit test in `tests/unit/test_telemetry_exclusion.py`: model identity, cost, latency, retries, save lineage, run configuration, wall-clock timing, and the game build are absent from every assembled agent context (FR-020, SC-008)

### Implementation for User Story 2

- [X] T127 [US2] Implement the structural parity filter in `src/civsim_harness/parity/filter.py`: the assembler's only input type is a capability result, with **no raw-Lua input path** — a developer wanting new data must add a declaration, because there is no shortcut that also works (FR-018, invariant I1, research R9)
- [X] T128 [US2] Implement the forbidden-field guard in `src/civsim_harness/parity/forbidden.py`: the red-team list asserted at runtime against every assembled context before it leaves the harness — once per decision step — covering both game-state leakage (FR-019) and harness telemetry presented as game information (FR-020)
- [X] T129 [US2] Implement the four screening gates in `src/civsim_harness/parity/screening.py` per research R7 — **source** (the frame came from the declared game-window capture item, never a desktop or region grab), **geometry** (dimensions match the client rect within tolerance), **provenance** (tagged with the declared view capability and camera state that produced it), and **content** (a detector driven by T100's per-platform profile, covering FireTuner chrome, developer console, debug overlay, harness UI, the platform's own chrome, and the WGC capture border) — with any gate failing meaning withhold and re-capture, applied per step. **This task is what unblocks blob persistence in T102**: until it lands, no frame is stored at all (FR-025, FR-030, FR-015, SC-019)
- [X] T130 [P] [US2] Author view declarations in `catalogs/observations/views.yaml` with `camera_requirements` (`mode` ∈ {world, strategic, city_screen, diplomacy, congress}, `zoom_range` within what the standard UI allows, `target_must_be_revealed: true`) and a `screening_profile` (FR-024, contracts/capability-catalog.md)
- [X] T131 [P] [US2] Author camera action declarations in `catalogs/actions/camera.yaml` — move, zoom, and view-mode toggle — each with its `parity_basis`, `availability_predicate`, and `verification_predicate` so camera changes are rejectable like any other action (FR-026, research R8)
- [X] T132 [US2] Implement camera validation and execution in `src/civsim_harness/act/camera.py`: the target must be a plot the run has revealed, zoom must be within the range the standard UI permits, and the view mode must be one a human can toggle; violations are rejected with `out_of_parity_camera` and recorded (FR-026)
- [X] T133 [US2] Implement withheld-capture recording and enable blob persistence in `src/civsim_harness/observe/capture.py`: a screened-clean frame may now be stored; `screening_status = withheld` with its `withheld_reason` is written with `blob = None` and an `image_withheld` event carrying the `step_index` — the record is the evidence screening worked and is never deleted. This is the task that lifts T102's no-persistence restriction, and it must not land before T129 (FR-025, FR-030, SC-019)
- [X] T134 [US2] Attach screened images to the agent context in `src/civsim_harness/agent/context.py`: a capture may be shown only when `screening_status = screened_clean`, its `view_declaration_id` resolves in the run's catalog version, **and it belongs to the current decision step**; unscreened or stale-step images have no path in (FR-024, FR-025, FR-015)
- [X] T135 [US2] Record the observation and action catalog versions plus content hash on every run in `src/civsim_harness/run/preparation.py`, so runs before and after a catalog change stay distinguishable (FR-022, FR-029)
- [X] T136 [US2] Implement the preflight capability-resolution gate in `src/civsim_harness/run/preparation.py`: every observation and action the run could use resolves to a declaration, or the run does not start, naming the offending capability (FR-023, V5, SC-006)
- [X] T137 [US2] Enforce declaration attribution on the record path in `src/civsim_harness/observe/assemble.py` and `src/civsim_harness/act/dispatch.py`: an `ObservationEntry` without a `declaration_id` and a `Decision` without an `action_declaration_id` cannot be constructed — including the end-turn decision, which resolves to `turn.end_turn` like any other (SC-007, FR-008)
- [X] T138 [US2] Implement `civsim audit parity <run_id>` in `src/civsim_harness/operator/audit.py`: from the record alone, enumerate every distinct observation and action the run used — structured and visual, across every decision step — and resolve each to its parity declaration and catalog version, reporting any unresolved entry as a finding (SC-006, SC-007)
- [X] T139 [US2] Implement `civsim audit prompts <run_id>` and `civsim audit decisions <run_id>` in `src/civsim_harness/operator/audit.py`: every prompt encountered is a recorded `prompt_response` decision or a recorded stall, and zero decisions exist without a `model_call_id` (SC-005, SC-012, invariant I5)
- [X] T140 [US2] Implement `civsim audit steps <run_id>` and `civsim audit loop <run_id> --turn N` in `src/civsim_harness/operator/audit.py`: assert one decision, one model call, and one observation per step; contiguous `step_index` from 1; and that each step's observation was assembled after the prior step's verification with no observation or capture reused (SC-003, invariants I13, I14, I15)
- [X] T141 [US2] Implement `civsim audit capabilities <run_id>` in `src/civsim_harness/operator/audit.py`: every capability the run used is `path: firetuner` or carries a non-empty `firetuner_gap` stating what it does, what it reads or writes, and why Firetuner could not do it (FR-028, SC-020)

**Checkpoint**: A completed run's parity boundary is auditable end to end from its record alone

---

## Phase 5: User Story 3 - Keep a long run alive across crashes, stalls, and provider failures (Priority: P3)

**Goal**: The harness detects a crashed, hung, or unresponsive client and a failing provider within
bounded time, recovers from the last good save as the same continuous run, and — when it genuinely
cannot — stops in a clearly recorded failed state. It never skips a turn, invents an action, or
plays on from a stale picture of the board.

**Independent Test**: Start a run, kill the game client mid-turn, and verify the harness detects the
crash, records it, resumes from that turn's quicksave as the same run, re-observes before acting, and
completes the run — with both the abandoned attempt and the replayed turn present in the record.

### Tests for User Story 3 ⚠️

- [X] T142 [P] [US3] Integration test in `tests/integration/test_recovery.py`: the fake Nexus drops mid-turn at step *n*; a `crash_detected` event is recorded, the run resumes from **that turn's** quicksave as the same run, re-observes before acting, and the turn carries one `abandoned` and one authoritative attempt (FR-044 – FR-047)
- [X] T143 [P] [US3] Integration test in `tests/integration/test_provider_resilience.py`: transient failures retry with backoff, then fall back to the next model, with every attempt a recorded run event; chain exhaustion pauses the run in a recorded state with zero fabricated, skipped, or defaulted turns (FR-041, FR-042, SC-012)
- [X] T144 [P] [US3] Unit test in `tests/unit/test_detection.py`: each of the four signals — process liveness, tuner heartbeat, per-operation bounds, and the screen-identity probe — independently trips detection within the 60 s budget. **No signal may key off elapsed turn time**: a scripted multi-hour productive turn must trip nothing (SC-010, FR-014, research R12)
- [X] T145 [P] [US3] Unit test in `tests/unit/test_completeness.py`: a turn number with no authoritative attempt forces `record_completeness_status = has_gaps`; a missing `step_index` within a turn does the same; a contiguous authoritative turn sequence with contiguous steps yields `complete` (FR-052, SC-003, SC-011, invariant I11)
- [X] T146 [P] [US3] Contract test in `tests/contract/test_provider_resilience_contract.py` asserting P5 (retry then fall back, every attempt a recorded event), P6 (exhaustion raises; the harness has no fabricate, skip, or default-move path), and P11 (no turn-level clock cancels a call)

### Implementation for User Story 3

- [X] T147 [P] [US3] Implement the process liveness monitor in `src/civsim_harness/resilience/liveness.py` using `psutil` against the client PID, catching hard crashes and process exit (research R12)
- [X] T148 [P] [US3] Implement heartbeat monitoring in `src/civsim_harness/resilience/heartbeat_monitor.py` on top of the T034 probe, raising `hang_detected` when the nonce fails to round-trip within its bound (research R12)
- [X] T149 [P] [US3] Implement per-operation bound enforcement in `src/civsim_harness/resilience/operation_bounds.py`: every Nexus command, capture, and post-action read-back carries its own timeout, catching a client that is alive and answers the heartbeat but will not service a specific operation. **These bounds are scoped to a single operation, never to a turn** — a long turn is not evidence of a fault (research R12, FR-014)
- [X] T150 [US3] Implement the detection aggregator in `src/civsim_harness/resilience/detector.py`: any of the four signals trips detection within 60 s, each recorded as its own run event — `crash_detected`, `hang_detected`, `unresponsive_detected`, or `unknown_screen`. The removed `stall` event type must not reappear; a turn that stops accomplishing anything is the turn cycle's no-progress backstop, not a detection concern (FR-044, SC-010, data-model.md §14)
- [X] T151 [US3] Implement recovery in `src/civsim_harness/resilience/recovery.py`: preserve the last-known-good save and state, resume from **this turn's** start quicksave as the same continuous run, and re-observe and re-capture before acting — nothing obtained before the interruption is reusable (FR-045, FR-046)
- [X] T152 [US3] Implement attempt bookkeeping in `src/civsim_harness/run/turn_cycle.py`: the abandoned attempt is retained with all the steps it completed, and the replayed attempt is marked authoritative, with exactly one authoritative attempt per `(run_id, turn_number)` (FR-047, invariant I9)
- [X] T153 [US3] Wire mid-turn observation failure into recovery in `src/civsim_harness/resilience/recovery.py`: T096's `ObservationAssemblyError` takes the same abandon-and-replay path as a crash, since a turn cannot continue on a stale board after earlier steps have executed (spec edge case, FR-046)
- [X] T154 [US3] Implement the recovery bound in `src/civsim_harness/resilience/recovery.py`: after `recovery_attempt_limit` consecutive failures, stop in a recorded `failed` state identifying the last-known-good save via `get_last_known_good`, with a `recovery_limit_reached` event and no indefinite retry loop (FR-048, SC-021)
- [X] T155 [US3] Implement retry and fallback in `src/civsim_harness/provider/chain.py`: exponential backoff with jitter on transient failures (HTTP 429 and transient 5xx), then the next model in the chain, recording `provider_failure`, `provider_retry`, and `provider_fallback` events; context-length and modality refusals map to `context_rejected` as a chain-level failure rather than a retry. Fallback is decided **per call**, so a single turn may be served by more than one model (FR-041, P5)
- [X] T156 [US3] Implement chain exhaustion handling in `src/civsim_harness/provider/chain.py` and `src/civsim_harness/run/runner.py`: the provider layer raises `ProviderChainExhausted`, a `model_chain_exhausted` event is recorded, and the run pauses in a recorded state — there is no fabricate, skip, or default-move path to take instead (FR-042, P6, SC-012)
- [X] T157 [US3] Implement bounded capture retries and degradation marking in `src/civsim_harness/observe/capture.py`: after bounded retries, set `DecisionStep.visually_degraded = true`, roll it up to `TurnCycle.visually_degraded`, downgrade `Run.comparability_status`, and record a `capture_failed` event with its `step_index` — so a turn that lost its images part-way is distinguishable from one that never had them, rather than proceeding on structured state alone as if nothing changed (FR-050, SC-013)
- [X] T158 [US3] Implement completeness derivation in `src/civsim_harness/store/completeness.py`: gap markers from `turn_gaps` **and `step_gaps`** drive `record_completeness_status`, so a turn present but internally incomplete is not reported as complete (FR-052, SC-003, SC-011)
- [X] T159 [US3] Implement reconnect discipline in `src/civsim_harness/nexus/client.py`: a reconnect re-runs the full handshake and re-resolves state indices; indices from before a disconnect are never reused (contracts/nexus-protocol.md)
- [X] T160 [US3] Implement `civsim audit recovery <run_id>` and `civsim audit completeness <run_id>` in `src/civsim_harness/operator/audit.py`, reporting crash/resume event pairs, abandoned-vs-authoritative attempts per turn, and zero silently missing turns or steps (SC-003, SC-011, SC-021)

**Checkpoint**: A run survives client crashes and provider failures unattended, or fails loudly

---

## Phase 6: User Story 4 - Branch and replay a run from any turn (Priority: P4)

**Goal**: Any recorded turn's save can start a new run that records what it branched from, plays
forward under different conditions, and leaves the parent's record untouched — and no save is ever
removed except by an operator's explicit archival.

**Independent Test**: Take a completed run, branch from its turn 23 save twice under two different
model configurations, play both to turn 30, and verify each branch records its parent run and turn,
both diverge from the identical starting position, and the parent run's record is unchanged.

### Tests for User Story 4 ⚠️

- [X] T161 [P] [US4] Integration test in `tests/integration/test_branching.py`: two branches from the same save point each record `parent_run_id` and `parent_turn`, and the parent's record is byte-identical before and after (FR-033, FR-034, invariant I12, SC-014)
- [X] T162 [P] [US4] Unit test in `tests/unit/test_retention.py`: a **finished, unarchived, year-old** run yields zero eligible save points — not by age, not by quota, not by retention window, not by being terminal, not by thinning. Only `archive_run` makes a save eligible, and a required save found missing is reported rather than worked around (FR-036, invariant I17, research R17)
- [X] T163 [P] [US4] Unit test in `tests/unit/test_headroom.py`: with free space below `min_free_disk_gb`, the run halts in a recorded state with a `disk_headroom_low` event and **zero saves deleted** — the harness must never free space by removing an unarchived save (spec edge case, research R17)
- [X] T164 [P] [US4] Contract test in `tests/contract/test_branch_configuration.py`: a `branch_from` configuration that restates seed, civilization, ruleset, mod set, map, or game settings differently is rejected; only `model_config`, `guidance_set`, `stop_condition`, `no_progress_step_limit`, `recovery_attempt_limit`, and `min_free_disk_gb` may vary (contracts/run-configuration.md "Branch configuration")

### Implementation for User Story 4

- [X] T165 [US4] Implement save point addressing in `src/civsim_harness/saves/addressing.py`: resolve any save point by run, turn, and lineage without inspecting the filesystem or the game client — `save_name` is a naming convention, not the address (FR-032)
- [X] T166 [US4] Implement branch configuration in `src/civsim_harness/config/run_config.py`: the `branch_from` block (`run_id`, `turn`), with seed, civilization, ruleset, mod set, map, and game settings **inherited from the parent and non-overridable**, and only `model_config`, `guidance_set`, `stop_condition`, `no_progress_step_limit`, `recovery_attempt_limit`, and `min_free_disk_gb` variable
- [X] T167 [US4] Implement branch creation in `src/civsim_harness/saves/branching.py`: load the parent's turn-start save, create a new run recording `parent_run_id` and `parent_turn` as its lineage, and emit a `branch_created` event. A branch from a save that no longer exists is rejected with the missing save named, never retargeted to a nearby turn (FR-033, FR-036)
- [X] T168 [US4] Enforce parent immutability in `src/civsim_harness/store/sqlite_adapter.py`: reject any write from a child run that targets parent records; the port has no delete operation on turn records and that is deliberate (FR-034, invariant I12)
- [X] T169 [US4] Implement branch abandonment in `src/civsim_harness/saves/branching.py`: record a `branch_abandoned` event and mark the affected turns superseded via `mark_turn_superseded` rather than deleting them (FR-035)
- [X] T170 [US4] Implement archival in `src/civsim_harness/saves/archival.py`: `archive_run` is the **only** operation that makes a save point eligible for removal. It sets `Run.archived_at`, writes a `run_archived` event, leaves records, events, and captures intact, and is rejected on a non-terminal run. No age, quota, retention-window, or thinning rule may exist anywhere in this module (FR-036, invariant I17, research R17)
- [X] T171 [US4] Implement the operator-invoked reaper in `src/civsim_harness/saves/reaper.py`: delete save **files** only for save points returned by `list_eligible_save_points()`, defaulting to `--dry-run`. It is never a background job and never runs unattended; the `SavePoint` record survives the file so a later branch attempt fails explainably (FR-036, research R17)
- [X] T172 [US4] Implement missing-save reporting in `src/civsim_harness/saves/addressing.py` and `src/civsim_harness/resilience/recovery.py`: a required save found absent records a `save_missing` event and fails, never resuming from a different turn (FR-036)
- [X] T173 [US4] Implement `run branch`, `run resume-from`, `run archive`, and `saves reap` in `src/civsim_harness/operator/cli.py` and `src/civsim_harness/operator/api.py`, rejecting a `resume-from` or `branch` whose save is missing with the missing save named, and rejecting `archive` on a non-terminal run (FR-004, contracts/operator-surface.md)
- [X] T174 [US4] Implement `civsim seedset accept-build` in `src/civsim_harness/operator/cli.py`: append a `BuildAcceptance` to the seed set, record a `game_build_change_accepted` event, and make the acceptance referenceable by every dependent run. Acceptance is scoped to one set and **one composite transition** (platform + version) — there is no global override, and accepting a version bump never implicitly accepts a platform change. **A version-only transition** (e.g. `win/1.0.12.9 → win/1.0.12.11`) **requires no spike result; a platform-crossing transition** (e.g. `win/1.0.12.9 → mac/1.0.12.9`) **is refused unless a passing R20 spike result (T199) is on record** — the command populates `BuildAcceptance.r20_spike_ref` from that recorded result or rejects the acceptance (FR-002, research R18, R20)
- [X] T175 [US4] Enforce same-build branching in `src/civsim_harness/saves/branching.py` per **FR-033**: a branch whose game build — **platform or version** — differs from the parent run's is refused by default with the mismatch recorded, under the same recorded-acceptance mechanism as FR-002's build change, and a branch relying on an acceptance records it. **A version-only branch transition needs no spike result; a platform-crossing branch is refused unless the `BuildAcceptance` it relies on carries a passing R20 spike result (`r20_spike_ref`, T199)** — Civ VI's cross-platform saves are account-gated and version-matched, which is not a foundation for FR-034's "two branches from the same save point begin from an identical position" (FR-033, FR-034, research R20)
- [X] T176 [US4] Implement `civsim audit lineage <branch_id>`, `civsim audit immutability <run_id>`, and `civsim audit builds <seed_set>` in `src/civsim_harness/operator/audit.py`, the last reporting a set with any accepted build change as **non-uniform** with its runs partitioned by build (quickstart.md Scenarios 5 and 9, FR-031)
- [ ] T177 [P] [US4] Live test in `tests/live/test_branch_identity.py` (marked `live`): two branches from one save point begin from an identical game position (SC-014). *(Audit 2026-09-21: client-gated — the test file does not exist yet; needs the Linux node's client at a verified save point, two `run branch` invocations and a position read-back; ~15 min wall-clock, $0 with the fake provider. Note the R5 finding that saves are not byte-stable across save/load/save, so "identical position" must be asserted on game state, not file bytes.)*

**Checkpoint**: Branching and replay work, the parent record is provably untouched, and no save disappears without an operator saying so

---

## Phase 7: User Story 5 - Swap the agent's model without touching the harness (Priority: P5)

**Goal**: Changing which model plays is a change to run configuration and nothing else, with every
call recording the model that actually served it and no path that drops images to make a call fit.

**Independent Test**: Run the same seed and configuration twice, changing only the model identifier
in the run configuration, with no code or integration change between the two; verify both complete
and both records name the model that actually served each call.

### Tests for User Story 5 ⚠️

- [X] T178 [P] [US5] `ModelProvider` conformance suite in `tests/contract/test_model_provider_port.py` asserting P1–P11 against both the OpenRouter adapter and the fake provider (contracts/model-provider-port.md)
- [X] T179 [US5] Red-team test in `tests/contract/test_model_provider_port.py`: a fake provider advertising a tiny context or `accepts_images = False` produces a **failed run**, never a reduced-image call (P2, FR-039, SC-017)
- [X] T180 [US5] Red-team test in `tests/contract/test_model_provider_port.py`: a fake returning **two decisions for one request raises** — the extras are neither dropped nor queued, since silently discarding the second is as wrong as executing it (P10, FR-008, invariant I13)
- [X] T181 [US5] Red-team test in `tests/contract/test_model_provider_port.py`: a key planted in the environment appears in no serialized record, log line, or raised exception (P8, FR-043, SC-018)
- [X] T182 [P] [US5] Unit test in `tests/unit/test_model_swap.py`: no model identifier appears anywhere outside `RunConfiguration.model_config`, so a swap is configuration-only. Additionally, run the same seed and configuration through two distinct-vendor models routed via OpenRouter (e.g. `anthropic/claude-sonnet-5` and `google/gemini-3-pro`) and verify both complete and both records name the model that actually served each call, discharging SC-015's "verified on at least two providers" against two vendors rather than two models from one (FR-038, P9, SC-015)

### Implementation for User Story 5

- [X] T183 [US5] Implement `describe()` in `src/civsim_harness/provider/openrouter.py` reading OpenRouter's models endpoint for modality and context length rather than hard-coding them, returning `confirmed = False` when the endpoint cannot confirm (research R10)
- [X] T184 [US5] Implement chain preflight in `src/civsim_harness/provider/preflight.py`: before turn 1, `describe()` every model in `primary + fallbacks`; any model with `accepts_images = False`, `confirmed = False`, or context insufficient for a **worst-case decision step** (late-game structured state plus that step's declared views) fails the run, naming the failing model (FR-039, P1, V4, SC-017)
- [X] T185 [US5] Enforce the no-image-drop rule in `src/civsim_harness/provider/chain.py`: `image_count` must equal `len(request.images)`, and a mismatch raises as a defect — there is no code path that removes images to make a call fit (FR-039, P2, invariant I7)
- [X] T186 [US5] Implement per-call accounting in `src/civsim_harness/provider/accounting.py`: write a `ModelCall` record carrying `decision_step_id`, `model_requested`, `model_served`, `latency_ms`, provider-reported `cost`, `retry_count`, `fallback_occurred`, `image_count`, and `outcome` through the store. Per-call cost and latency are recorded and never aggregated away, since per-turn cost is unbounded by construction and the trade-off must stay visible (FR-040, P3, P7)
- [X] T187 [US5] Link every `Decision` to the `ModelCall` that produced it through its `DecisionStep` in `src/civsim_harness/agent/decisions.py`, so the model that served each step is resolvable and fallback-served steps roll up to a turn that is distinguishable from a primary-served one (FR-040, SC-016, invariant I5)
- [X] T188 [US5] Enforce adapter obligations in `src/civsim_harness/provider/port.py`: an adapter may not modify the images or observation it was handed, substitute a different model without reporting it in `model_served`, swallow an error and return `decision = None` with a successful outcome, return more than one decision, carry state between calls, or read run configuration or game state directly (contracts/model-provider-port.md "Adapter obligations")
- [X] T189 [US5] Implement call-time credential handling in `src/civsim_harness/provider/openrouter.py`: keys resolve from the environment or secrets file at call time, never appear in a request record, log line, error message, or exception trace (FR-043, P8)
- [X] T190 [US5] Implement `civsim audit models <run_id>` and `civsim audit secrets <run_id>` in `src/civsim_harness/operator/audit.py`: every call names its served model with latency, cost, and retry count, resolved per step and rolled up per turn; zero credential-shaped values appear in records, captures, or logs (SC-016, SC-018)

**Checkpoint**: All five user stories are independently functional

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: The live tier, the release gates, and the validation the quickstart specifies

- [ ] T191 [P] Live test in `tests/live/test_preparation.py` (marked `live`): real client, real settings — an exact match starts, and any mismatch fails before turn 1 with the mismatch recorded (FR-002, V2). *(Audit 2026-09-21: client-gated — file does not exist; the landed-code demo already exercised V2 live end to end (`spikes/demo-evidence-linux-landed/`), so this is mostly formalising that into a `live`-marked test plus one deliberate mismatch; ~10 min, $0.)*
- [ ] T192 [P] Live test in `tests/live/test_build_pin.py` (marked `live`): a seed set pinned to a build the client does not report fails the run before turn 1; after `seedset accept-build`, the run starts and records its acceptance reference, and the set reports as non-uniform (FR-002, FR-031, quickstart.md Scenario 9). *(Audit 2026-09-21: client-gated — file does not exist; needs the client to report its build (`UI.GetAppVersion()`, measured 1.0.12.9 (564030)); ~10 min, $0.)*
- [ ] T193 [P] Live test in `tests/live/test_crash_recovery.py` (marked `live`): a genuinely killed client is detected within 60 s and the run resumes as the same continuous run losing at most the turn in progress (SC-010). *(Audit 2026-09-21: client-gated — file does not exist; needs a run in progress, `kill` of the `Civ6` process, and a relaunch through Steam (`setsid steam steam://rungameid/289070`, ~60 s to the tuner port plus the T248 load); ~10 min per cycle, $0 with the fake provider. Clear any stale `/tmp/civsim_harness/run_locks/*.lock.json` between cycles.)*
- [ ] T194 [P] Live test in `tests/live/test_capture_hygiene.py` (marked `live`): the FireTuner window, a developer console, and an unrelated window placed over the client are absent from captured frames, and no capture border appears (SC-009, SC-019). *(Audit 2026-09-21: client-gated — file does not exist; the R6 Linux spike proved this by hand-written probe, this task is the same assertion through the harness's own `capture_window()` against the real client; ~10 min, $0.)*
- [X] T195 [P] Regenerate the published JSON Schemas into `specs/002-civ-playing-harness/contracts/schemas/` and confirm additive-only evolution against the previous version
- [X] T196 [P] Write the catalog authoring guide in `docs/catalog-authoring.md`: how to add a declaration with its parity basis **before first use**, and how a new capability bumps `catalogs/VERSION` (FR-029, spec edge case on mid-project capability additions)
- [X] T197 [P] Write `README.md` covering setup, expected `doctor` output, and the CI-runnable subset from quickstart.md
- [ ] T198 [P] Live test in `tests/live/test_host_platform.py` (marked `live`), run **on each target platform**: window identity resolves against the real client, capture returns a frame matching the client rect, directories resolve to the real install, and the reported tier matches what the capabilities actually support (research R19). *(Audit 2026-09-21: the Linux half is client-gated — file does not exist; ~5 min, $0 once the client is up. The Windows and macOS halves are other-platform.)*
- [ ] T199 Run the research R20 cross-platform save spike and record the result in `specs/002-civ-playing-harness/spikes/r20-cross-platform-saves.md`: whether a `.Civ6Save` written on one platform loads and resolves identically on another. Until this passes, cross-platform branching stays refused — the spike gates only the relaxation, never a core capability. **Consumed by T174 and T175** (Phase 6): both implement the platform-crossing check against `BuildAcceptance.r20_spike_ref` ahead of this task, but that check has nothing passing to reference — and therefore keeps refusing every platform-crossing acceptance and branch — until this task records a passing result here. *(Audit 2026-09-21: other-platform AND client-gated — needs a `.Civ6Save` written on Windows loaded on the Linux node (or the reverse) and a state read-back on both; the Linux node can supply its half (a save from `Saves/Single/`) on request.)*
- [ ] T200 Run the quickstart.md Scenarios 1–10 end to end against a real client and record the outcomes in `specs/002-civ-playing-harness/validation-results.md`, **noting the platform and tier each scenario ran under** — a scenario passing on a `VALIDATED` host says nothing about a `SUPPORTED` one. *(Audit 2026-09-21: client-gated — Scenarios 1, 2 and 5 have de facto run live through the landed-code demo and the five model-driven runs (`spikes/first-model-driven-runs-linux.md`) but were never recorded as quickstart scenarios; the full 1–10 pass is ~2 h wall-clock and roughly $1–3 for the model-driven scenarios at the measured ~$0.13/turn.)*
- [ ] T201 Soak validation recorded in `specs/002-civ-playing-harness/validation-results.md`: one 300+ turn full game to a victory or defeat outcome — including at least one late-game turn of several hundred decision steps completing untruncated — and a 20+ run unattended batch, both with zero turn or step gaps (SC-002, SC-011, FR-014). *(Audit 2026-09-21: client-gated and the single largest live item — at the measured ~60–150 s per harness turn, 300+ turns is roughly 5–12 h of exclusive client time; $0 with the fake provider, or roughly $40–45 model-driven at the measured ~$0.13/turn (over half the remaining $78.6 cap, so the owner decides which); the 20-run batch at 3 turns each is ~1–2 h and ~$0 (fake) to ~$8 (model). Both must wait for the `TechCivicCompletedPopup` acknowledge (T253) — play currently stops at the first completed civic.)*
- [X] T202 Record per-run cost and duration observations in `specs/002-civ-playing-harness/validation-results.md`: calls per turn, cost per turn, and total run cost under the one-call-per-step model, so the trade-off the spec accepted is measured rather than assumed (spec Assumptions, plan Scale/Scope). *(Closed 2026-09-21: `validation-results.md` §T202 — five model-driven runs read from the store's own step bundles: 7.8 calls/turn (the backstop cap, not the model's need), $0.119/turn, $0.0153/call, ~97 s wall-clock/turn of which ~85 % is model latency, $1.425 total for 93 calls over 12 early-game turns. Late-game cost is T201's measurement and remains open.)*
- [X] T203 Add the per-release parity audit gate to `.github/workflows/release.yml`, running `audit parity`, `audit capabilities`, `audit secrets`, `audit steps`, and the red-team suite, and blocking release on any finding (SC-006, SC-008, SC-009, SC-018, SC-019, SC-020)
- [X] T204 Performance pass in `src/civsim_harness/run/turn_cycle.py` and `src/civsim_harness/store/sqlite_adapter.py`: confirm the store write leaves deliverable 1's 5 s currency window intact for a several-hundred-step turn, and that detection stays inside the 60 s budget under a late-game load (plan Performance Goals)
- [X] T205 Re-check the delivered harness against Constitution v1.0.0 Principles I–VII and record the result in `specs/002-civ-playing-harness/validation-results.md`, as the Governance section's compliance review requires before implementation closes

---

## Phase 9: Composition & Wiring (added after the integration-readiness audit)

**Why this phase exists.** Phases 1-8 built every component and 933 tests pass, but an
end-to-end audit (`integration-readiness.md`) found almost all of them are **seams with no
production caller**. `civsim run start` fails at its first line with "no runner is configured".
Most seriously, `implementation_ref` -- the field binding each catalog declaration to its Lua
file -- is **never read outside its own model definition**, so the 21 declared Lua files and 51
declarations are never loaded or executed by anything.

This is a genuine gap in the original task breakdown: the 205 tasks specify every part and never
specify assembling them. These tasks close it.

- [X] T206 Implement the capability executor in `src/civsim_harness/capability/executor.py`: resolve a `ParityDeclaration` to its `IntegrationCapability`, read that capability's `implementation_ref`, load the `lua/` file from disk, dispatch it through `NexusClient.execute_command` **in the declaration's declared context** (resolved by name at call time, never a cached index), and return a `CapabilityResult`. This is the missing link between the catalog and the game; nothing currently reads `implementation_ref` at all
- [X] T207 Implement the production `ObservationReader` in `src/civsim_harness/observe/reader.py`: for a decision step, execute every observation declaration the run uses via T206, validate each against its `output_schema`, and feed `observe/assemble.py`. The assembler already consumes capability results only (FR-018) -- this is what produces them
- [X] T208 Implement the production `ActionExecutor` in `src/civsim_harness/act/executor.py`: dispatch one action declaration via T206 and hand the result to `act/verify.py`, so the outcome is still **derived** from the verification predicate rather than asserted by the executor (FR-011, invariant I4)
- [X] T209 Implement the composition root in `src/civsim_harness/run/composition.py`: build `RunnerDependencies` from a `RunConfiguration` -- store, `NexusClient`, host adapter, catalog + registry, `ProviderChain` over `OpenRouterProvider`, `LuaSaveCapability` -- and call `configure_runner_factory` from the CLI so `civsim run start` reaches a `Runner` at all
- [X] T210 Chain the preparation sequence in `run/runner.py`'s `prepare_run`: `build_pin_preflight`, `catalog_preflight`, `turn_timer_preflight`, `debug_menu_preflight`, `evaluate_host_gate`, `preflight_chain`, the leader/civilization selection with its read-back, and `verify_configuration` -- then call `NexusClient.refresh_state_indices()` at the **menu to in-game phase boundary**, which currently has zero production callers despite indices being known to change by phase
- [X] T211 Integration test in `tests/integration/test_end_to_end_wiring.py`: drive `civsim run start` against the fake Nexus, fake provider and fake host through the **real** composition root, and assert a turn is played and persisted -- so "the pieces are connected" becomes a test result rather than a claim

**Checkpoint**: the harness can actually be run, not merely assembled

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — starts immediately
- **Foundational (Phase 2)**: Depends on Setup — **BLOCKS all user stories**
- **User Story 1 (Phase 3)**: Depends on Foundational. No dependency on any other story
- **User Story 2 (Phase 4)**: Depends on Foundational. Independently testable against a US1 run, but its screening and audit work assumes US1's per-step capture pipeline and turn records exist
- **User Story 3 (Phase 5)**: Depends on Foundational and on US1's turn cycle and save discipline. T157 (degradation marking) also assumes US2's screening
- **User Story 4 (Phase 6)**: Depends on Foundational and on US1's save points; recovery reporting (T172) touches US3's recovery module
- **User Story 5 (Phase 7)**: Depends on Foundational and US1's provider adapter; T185 builds on US3's chain module
- **Polish (Phase 8)**: Depends on all desired stories being complete
- **Composition & Wiring (Phase 9)**: Depends on every component it assembles — in practice on Phases 2–8 — and was added after the integration-readiness audit found that none of them had a production caller. Nothing downstream of it can be exercised end to end until it lands
- **Convergence (Phase 10)**: Depends on Phase 9. Every task in it names a seam Phase 9 made reachable for the first time and found incomplete; each is independent of the others except that T223 assumes T215's `resume_from` exists and T224 assumes T220's capture-path resolution

### User Story Dependencies

- **US1 (P1)**: Foundational only — the MVP, and the only story with no upstream story
- **US2 (P2)**: Structurally independent (the filter and screening are new modules), but its Independent Test reads a completed US1 run
- **US3 (P3)**: Builds on US1's turn cycle and quicksaves; its Independent Test kills a client mid-run
- **US4 (P4)**: Builds on US1's save points; designed into the save model from the start rather than bolted on
- **US5 (P5)**: Builds on US1's provider adapter; its Independent Test needs two completed runs

### Within Each User Story

- Tests are written and failing before implementation
- Spikes before the work they gate: **T077 (R5) before any save work**; **T099 (R6) before any image reaches the agent**; **T071 (R18) before the build pin is wired**; **T199 (R20) before cross-platform branching is relaxed**
- **The host port (T047–T049) precedes its adapters (T050–T052), which precede anything that
  captures, saves, or resolves a game directory.** Everything downstream talks to the port, never to
  a platform library (research R19)
- Lua and catalog declarations before the modules that bind to them
- Models before services; services before orchestration; orchestration before the operator surface
- Within the run loop specifically: T110 (the loop) → T111 (no-progress accounting) → T112 (termination) → T114 (turn cycle) → T117 (persistence). T113 is a constraint on T111 and lands with it
- Each story is complete and checkpointed before the next priority begins

### Parallel Opportunities

- **`[P]` means a different file from every other task running with it.** Where several tasks edit one file — `pyproject.toml`, `models/config.py`, `models/turn.py`, and the shared contract-test modules — only the first carries `[P]` and the rest are sequential behind it, even though they look independent
- Setup: T004, T006, T007, T009, T010, and T011 run together after T001–T003; T005, T008, and T012 follow T004 sequentially because they all edit `pyproject.toml`
- Foundational: T017, T018, T020, T021, and T024–T026 run together (T019 follows T018, and T022–T023 follow T021, on the same files); the codec (T029–T034), catalog (T035–T037), store (T038–T042), provider (T043), and host-adapter (T050–T052) tracks are independent of each other and of the model definitions
- All test tasks within a story marked [P] run together before that story's implementation
- US1's Lua files (T082–T089) and catalog files (T090–T094) are all separate files and run together
- Once Foundational completes, separate developers can take US1, and then US2/US3/US4/US5 in parallel once US1's turn cycle lands

---

## Parallel Example: User Story 1

```bash
# Launch all US1 tests together (they must fail first):
Task: "Integration test full turn cycle in tests/integration/test_turn_cycle.py"
Task: "Integration test the decision-step loop in tests/integration/test_decision_loop.py"
Task: "Integration test both turn endings in tests/integration/test_turn_endings.py"
Task: "Unit test no-truncation of a 500-step turn in tests/unit/test_no_truncation.py"
Task: "Unit test turn attempt state machine in tests/unit/test_turn_state_machine.py"
Task: "Contract test run configuration validation in tests/contract/test_run_configuration.py"
Task: "Contract test the build pin in tests/contract/test_build_pin.py"
Task: "Integration test preparation mismatch in tests/integration/test_preparation.py"
Task: "Integration test prompt handling in tests/integration/test_prompts.py"
Task: "Integration test stop conditions in tests/integration/test_stop_conditions.py"

# Launch all declared Lua together (separate files, no shared state):
Task: "GameCore_Tuner read Lua for map/units/cities in lua/gamecore/"
Task: "InGame Lua for unit orders in lua/ingame/unit_orders.lua"
Task: "InGame Lua for city orders in lua/ingame/city_orders.lua"
Task: "InGame Lua for empire orders in lua/ingame/empire_orders.lua"
Task: "InGame Lua for screens and prompts in lua/ingame/screens.lua"
Task: "InGame Lua for turn control in lua/ingame/turn_control.lua"

# Launch all catalog authoring together:
Task: "Observation declarations in catalogs/observations/*.yaml"
Task: "Action declarations in catalogs/actions/*.yaml"
Task: "The end_turn declaration in catalogs/actions/turn.yaml"
Task: "Prompt declarations in catalogs/actions/prompts.yaml"
Task: "IntegrationCapability declarations in catalogs/capabilities.yaml"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational — **blocks everything**; the catalog loader, store port, and Nexus
   client are what every later phase binds to
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: run quickstart.md Scenario 1 — a turn-50 run with zero human interaction,
   50/50 authoritative turns, zero step gaps, 50/50 verified quicksaves, zero decisions without a
   `model_call_id` — and Scenario 8, which checks the loop re-observes between decisions, a long
   turn is not truncated, and the backstop ends a stuck turn and is recorded as such
5. That run is independently valuable on the day it first works: it is the deliverable the interface,
   the store, and the optimization layer all have nothing to show until it exists

### Incremental Delivery

1. Setup + Foundational → the boundary and the transport exist
2. + US1 → **MVP**: a run plays itself end to end, one decision step at a time, and records every
   step (structured state; images follow, and until then runs are honestly marked visually degraded)
3. + US2 → the run's data becomes *admissible*: every observation and action resolves to a declared
   parity basis, and screened images enter the agent's context at each step
4. + US3 → the harness becomes usable unattended in volume rather than only in demos
5. + US4 → branch-and-replay, the mechanism deliverable 4's Monte Carlo and ablation work needs,
   with saves that persist until someone decides otherwise
6. + US5 → cost-optimized experimentation across a seed set under swappable models

### Parallel Team Strategy

With multiple developers, after Foundational completes:

1. One developer drives US1 to the MVP checkpoint — it is on everyone else's critical path
2. Then in parallel: Developer A on US2 (parity, screening, audit), Developer B on US3 (detection,
   recovery, provider resilience), Developer C on US4 + US5 (saves, branching, archival, provider
   layer)
3. The two cross-story touch points to coordinate are `observe/capture.py` (US1 per-step pipeline,
   US2 screening, US3 degradation) and `provider/chain.py` (US3 fallback, US5 accounting)

---

## Notes

- **The spikes are gates, not chores.** T077 (R5) must produce a documented negative result
  before the bespoke save path is enabled, or Principle II's Firetuner-first ordering is violated.
  T099 (R6) must pass before any image reaches the agent **on that platform**; if it fails, runs there proceed visually
  degraded under FR-050 — a legitimate operating state, not a blocker. T071 (R18) decides whether
  the build read is a declared Lua observation or the executable's file version; either way the
  check happens.
- **Four negative tests guard the clarification's invariants**, and they matter more than they look,
  because each forbids something that would pass review as a sensible feature:
  - T061 — a 500-step productive turn completes untouched (no time, step, or cost cap) (I16)
  - T162 — a finished, unarchived, year-old run yields zero eligible saves (I17)
  - T180 — a provider returning two decisions for one step raises (I13)
  - T059 — a step reusing the prior step's observation or capture fails (I14)
- **Zero-tolerance criteria drive test placement.** SC-006, SC-008, SC-009, SC-018, SC-019, and
  SC-020 are worded so any finding blocks release, which is why the red-team suites live in
  `tests/contract` and run on every build rather than per release.
- **`turn_time_budget_s` must not reappear anywhere.** It was removed by the spec's clarification and
  data-model.md §2 records its return as a regression rather than an addition.
- `[P]` tasks touch different files and have no dependency on incomplete work
- `[Story]` labels map tasks to spec.md user stories for traceability; Setup, Foundational, and
  Polish tasks carry no story label by design
- Commit after each task or logical group; stop at any checkpoint to validate a story independently
- Avoid: recording an action applied without verification, ending a turn before its record is
  durable, batching decisions within a turn, reusing a step's observation or capture, deleting a
  save nobody archived, and any code path that would drop images to make a model call fit

---

## Phase 10: Convergence

**Why this phase exists.** Phase 9 made `civsim run start` reach a real `Runner` and play a
recorded turn end to end. Doing so turned a set of previously-unreachable seams into reachable
production paths, and several of them are not yet complete. Every item below was found by
assessing the code against the spec after Phase 9 closed; none is captured by an existing task.

- [X] T212 Produce a real `SupportProbeResult` in `src/civsim_harness/host/detect.py` (an R19 per-platform probe: verify the quicksave path, report the R6 capture-hygiene spike result) and wire it through `run/composition.py`'s `build_runner_dependencies` and `operator/cli.py`'s `default_runner_factory` per **FR-054** (missing) — nothing in `src/` constructs anything but `UNPROBED` today, so `evaluate_host_gate` refuses **every** run on **every** host, including the validated Linux one
- [X] T213 Verify on a live client that the four Python-embedded Lua bodies now print their JSON result correctly — `saves/save_game.py`'s save call, `run/preparation.py`'s leader-selection write and `PlayerConfigurations` read-back, and `observe/game_build.py`'s version probe — per **FR-011** (partial); all four previously ended in a bare `return { ... }`, which the tuner's print-only result channel discards, so none of them could ever have returned a value
 **LIVE-VERIFIED (2026-09-21, Linux): all 14 observation bodies dispatched through the production executor and schema-validated at turns 1 and 2 (`spikes/t213-observation-bodies-linux.md`, `tests/live/probe_observation_bodies.py`); 5 fixed on measurement (PlayersVisibility, InGame-only unit API, screen_state aggregate, nullable turn-1 fields); the four Python-embedded bodies print JSON live (V2 read-back, save, load, leader pin all exercised by the demo runs)
- [X] T214 Close the `NexusClient` when a run reaches a terminal state, per **FR-006** (partial): `run/composition.py` opens one client per `prepare_run` and nothing ever closes it, and the client accepts a single tuner connection at a time — so a second `run start` in the same process cannot connect
- [X] T215 Implement `Runner.resume_from` in `src/civsim_harness/run/runner.py` per **FR-004** (missing): it still raises `HarnessError("not implemented in this wave")`, and `civsim run resume-from` now reaches it for the first time rather than failing earlier at "no runner is configured"
- [X] T216 Resolve victory/defeat in `run/composition.py`'s `_evaluate_stop_facts` per **FR-005** (partial): it reports only `current_turn`, so a run configured with a `game_outcome` stop condition can never resolve. Needs a catalog declaration that reads the game's own outcome — none exists in `catalogs/` today
- [X] T217 Provide a production `SaveLoader` for `resilience/recovery.py` per **FR-046**, **FR-048** — **implementation landed, live verification pending on a host.** `saves/load_game.py`'s `LuaSaveLoader` implements the front-end `Network.LoadGame` path the Linux peer verified end to end (`spikes/t217-RESOLVED-frontend-loadgame.md`, which retracts `load-path-linux.md`'s "does not work from Lua"): `Events.ExitToMainMenu()` from `InGame`, the verified table shape (`Location=LOCAL_STORAGE Type=SINGLE_PLAYER Directory=DEFAULT Name=<bare name>` + `SERVER_TYPE_NONE`) from `MainMenu`, connection-refused treated as the expected mid-load state, and a far-side position assertion (turn vs the `SavePoint`, `UI.IsInFrontEnd()`, local player) on the reconnected session — never the call's own return value. Wired as `run/composition.py`'s default (replacing `_NoLiveSaveLoader`) for both `create_branch` and each turn's `RecoveryEngine`, bound to the run's own client; `Runner.resume_from` and `run branch` now reach a real load. Tested against the wire-protocol fake (`tests/unit/test_save_loader.py`, plus two end-to-end branch tests in `tests/integration/test_end_to_end_wiring.py`; confirmed to fail with the composition wiring reverted). **Still live-gated:** no client has run THIS implementation — a live session must confirm the full exit→load→verify sequence, the leader-intro-screen hold (one dismissal click may be needed; the spike left it open whether it appears for mid-game saves), and that `Game.GetCurrentGameTurn()` equals the harness turn number for a mid-game quicksave (the loader fails closed on any mismatch)
- [X] T218 Verify the five UNVERIFIED getters in `run/preparation.py`'s `_SETTING_GETTERS` against a live client (`map_seed`, `map_settings.map_type`, `map_settings.map_size`, `map_settings.resources`, `opponents.major_count`, `mod_set`) per **FR-002**/**V2** (partial) — each currently fails closed to an `UnreadSetting`, which correctly fails the run rather than fabricating agreement, but means a real run cannot pass V2 for those fields until the getters are confirmed
 *(Closed 2026-09-21 on the Linux node's live measurements: `map_seed`, `map_type`, `map_size` and `mod_set` verified in-game and at the front end (`spikes/t218-RESULTS-setting-getters.md`); `resources` measured **unobservable at both phases** and recorded as such (T250); `major_count` and `mod_set` measured phase-dependent and now read in-game only (T250), with `mod_set` versions read by handle (T251). Every getter is either verified or recorded as a measured negative — none is a guess.)*
- [X] T219 Resolve `RunConfiguration.guidance_set_id` through `config/guidance.py` and pass the loaded `GuidanceSet` into `DecisionLoopContext` in `run/composition.py` per **FR-021** (missing) — `guidance` is hard-coded `None`, so configured guidance never reaches the agent
- [X] T220 Set `Run.capture_path` from the host's actual resolved capture mechanism in `run/composition.py` per **FR-050**, **SC-013** (partial) — it is hard-coded to `CapturePath.NONE`, so a run on a host with a working capture path is recorded as having none
- [X] T221 Supply a real `camera_state_provider` to `DecisionLoopContext` in `run/composition.py` per **FR-026** (partial) — it is left at its `dict` default, so every capture records an empty camera state and no view's declared camera requirements are ever satisfied
- [X] T222 Report real tuner and client health from `run/composition.py`'s `connection_health()` per **FR-053** (partial) — both are hard-coded `"unknown"`, so `run status` cannot distinguish a live client from a dead one
- [X] T223 Give `TurnCycleDependencies` an attempt-index base and resolve it in `run/composition.py`'s `build_turn_dependencies` per **FR-004**, **FR-047** (missing): `src/civsim_harness/run/turn_cycle.py`'s `run_turn_cycle` opens every turn at a hard-coded `attempt_index = 0`, so a turn replayed by `Runner.resume_from` (or by any rewind that superseded an earlier attempt) collides with the attempt already on record — `store/sqlite_adapter.py`'s `write_turn_cycle` D4 check rejects a second write of the same `(run_id, turn_number, attempt_index)` whose content differs, and marking the earlier attempt non-authoritative does not free the triple. The replay needs an index *past* the superseded attempt's, which the store can already supply through `get_turn_cycle(run_id, turn, authoritative_only=False)`. Already documented as a known downstream gap in `Runner.resume_from`'s docstring (`run/runner.py`); remove that note when this closes. **Headless-implementable** — no live client is involved; the collision is entirely between `run_turn_cycle` and the SQLite adapter
- [ ] T224 Implement pixel extraction in the three host adapters per **FR-050**, **SC-013**, and research **R6** (partial): every `capture_window` path in `src/civsim_harness/host/{windows,macos,linux}/adapter.py` stops one step short of producing `CaptureFrame.image_bytes`, so **every capture on every platform returns `failed`/`unavailable`**, every step is recorded `visually_degraded`, and every run is comparability-degraded — regardless of T220's `capture_path` resolution or T221's camera-state provider, both of which now report a capture mechanism that cannot actually return a frame. Per platform:
  - **Windows** (`_capture_via_print_window`): `PrintWindow` is issued and the bitmap is selected into a memory DC, then the method returns `failed` with "pixel extraction (GetDIBits) is not implemented". The `GetDIBits` call, `BITMAPINFOHEADER` packing, and row-stride/bottom-up row-order handling are **implementable and unit-testable headless** against any HWND; only the R6 hygiene verdict for the resulting frame (T099) needs a live client. `_capture_via_windows_graphics_capture` (rank 1) is a separate gap: it returns `None` after an import probe because the `IGraphicsCaptureItemInterop` COM interop factory could not be confirmed under `winsdk` — that one needs real hardware. **WINDOWS HALF LANDED (rank 3 only).** `host/windows/adapter.py` gains `extract_bgra8`: a `GetDIBits` call against a packed `BITMAPINFOHEADER` with **negative** `biHeight` (top-down rows, matching the `Image.frombuffer(..., 0, 1)` orientation `parity/screening.py`'s `_decode_frame` already assumes -- a bottom-up buffer would mirror every frame vertically and raise nothing anywhere) at 32bpp (so the row stride is `width * 4`, inherently 4-byte aligned at every width, and the scan-line padding that silently shifts every row after the first at 24bpp cannot arise). `_capture_via_print_window` now returns a real `CaptureFrame(image_format="BGRA8")`. Two defects were fixed alongside it: every Win32 entry point is now declared with `argtypes`/`restype` (undeclared `ctypes` defaults truncate 64-bit HDC/HBITMAP/HWND handles to `c_int` on 64-bit Windows -- a call against a handle that is *almost* right, not a clean error), and the `HBITMAP` is now deleted on every path (it was never deleted: a per-capture GDI handle leak that would exhaust the process's 10,000-object quota within one game at one capture per decision step). **Verified how, honestly:** `tests/unit/test_windows_capture_extraction.py` (5 tests, Windows-only) paints a GDI bitmap this test creates -- red top half, blue bottom half, at a width of 37 deliberately not divisible by 4 -- and asserts the extracted buffer's size, top-down row order, per-row stride, opaque alpha, and that it decodes through the real `parity/screening.py::_decode_frame` to the colours painted. **No Civilization VI window was captured: there is no Civ VI client on the box this ran on.** `_capture_via_print_window` end to end against a real game window, and the R6 hygiene verdict for the resulting frame (T099), both still need a live client. The rank-1 `winsdk` interop path is untouched and still returns `None`
  - **Linux** (`_capture_via_xcomposite`): redirects the window and then returns `failed` with "pixmap readback (NameWindowPixmap + XGetImage) is not implemented". **The Linux peer is already implementing XComposite pixmap readback on its side — do not duplicate it here.** This task's Linux scope is limited to accepting that work and keeping the `CaptureFrame` contract (`image_format`, rect agreement) consistent with the other two adapters. `_capture_via_portal` (Wayland ScreenCast) remains a distinct gap needing an interactive grant and a `dbus-python`/asyncio integration decision — live-gated, and not in scope here.
  - **macOS** (`_capture_via_legacy_window_image`, `_capture_via_screencapturekit`): `CGWindowListCreateImage` returns an image and the method returns `failed` with "pixel extraction (CGImageGetDataProvider) is not implemented"; the ScreenCaptureKit rank-1 path raises `NotImplementedError` and its `pyobjc-framework-ScreenCaptureKit` dependency is not declared. **Both need a macOS host** — the selector bridging and the `CGImageRef` byte layout cannot be exercised from Windows or Linux. Live-gated.
  Until a platform's extraction lands *and* its R6 spike (T099) passes, that platform's runs stay visually degraded under **FR-050** — which is the correct recorded behaviour, not a bug to paper over. **Relationship to T050/T051/T052:** those three remain the per-platform adapter tasks and stay open; this task is the one *named* gap common to all three, filed separately because it is the single reason every capture fails today and because its Windows half is closable without a client while T050–T052 as wholes are not. Closing T224 for a platform does not close that platform's T050–T052, and neither closes without T099
 *(Audit 2026-09-21: PARTIAL — Linux extraction is live-verified (XComposite `BGRA8`, `spikes/r6-xcomposite-readback-linux.md`, 12 real frames in `run-9505f323`, PNG wire form since T252); Windows `PrintWindow` extraction landed (`extract_bgra8`, Windows-only unit tests, skipped on this node); macOS `CGWindowListCreateImage` still returns `failed` — "pixel extraction (CGImageGetDataProvider) is not implemented" — and no macOS machine exists in this project. Other-platform; cannot be closed from the Linux node.)* *(Audit 2026-09-22: **this task's opening premise is now false and is left in place only as history.** It asserts in the present tense that "every `capture_window` path ... stops one step short of producing `CaptureFrame.image_bytes`", so "every capture on every platform returns `failed`/`unavailable`" and every step is recorded `visually_degraded`. Measured read-only from a copy of the repo-root `civsim-match-store.db` on 2026-09-22: of **784** captures, **363** are `screened_clean` and **301** carry `shown_to_agent=True` (290 on 2026-09-21, 11 on 2026-09-22) across **20** runs, and **288** of 635 `model_calls` have `image_count == 1`. Real pixels have been extracted, screened and delivered to the agent for two days. The premise held when the task was written and was already contradicted by this task's own 2026-09-21 PARTIAL annotation above, which records Linux extraction as live-verified. **Checkbox deliberately unchanged** — macOS extraction is still missing and the live lane owns the closure; only the dead present-tense premise is corrected here.)*

---

## Phase 11: Convergence

**Why this phase exists.** Assessed after Phase 10 closed. Every item below is the *same shape* the
integration-readiness audit found and Phase 9 was written to fix, recurring in modules the audit did
not reach: a complete, individually-tested module with **no caller anywhere in `src/`**. Each was
verified by searching `src/civsim_harness/` for every reference to the symbol outside its own
defining module and finding none — not by reading a docstring that claimed it was wired. None is
captured by an existing task, and none is gated on a live client except where said so explicitly.

- [X] T225 **CRITICAL** Invoke `src/civsim_harness/parity/forbidden.py`'s `enforce_parity_boundary` once per decision step in `src/civsim_harness/run/decision_loop.py`, on the fully assembled `DecisionRequest`/`Observation` immediately before `ctx.provider.complete(request)`, per **Constitution Principle I (NON-NEGOTIABLE)**, **FR-019**, **FR-020** (missing) — T128 built the detective red-team guard and its own module docstring says "call once per decision step … right before it leaves the harness", but `decision_loop.py` never imports `parity.forbidden` and **no module in `src/` references `enforce_parity_boundary`, `scan_observation_entries`, `find_literal_leaks`, or `assert_no_literal_leaks` at all**. The preventive controls (catalog validation, the entry construction in `observe/assemble.py`, the four screening gates) do still run, so this is a missing *layer* rather than an open door — but Principle I is the one principle the constitution says must be **blocked rather than shipped with a caveat**, and a guard that never executes provides none of the assurance SC-006/SC-008 are written against. A leak that slips past prevention currently reaches the agent silently. **Headless-implementable.** The regression test must assert the guard *fires* on a planted leak reaching the real decision loop — not merely that the function rejects a hand-built context, which is what already passes today
- [X] T226 **CRITICAL** Make `civsim run branch` reach a branch at all, per **Constitution Principle IV**, **FR-033**, **FR-034**, **SC-014**, **SC-015** (contradicts) — `operator/cli.py`'s `run_branch` forwards the branch document to `commands.start` → `Runner.start` → `config/run_config.py`'s `load_run_configuration_file`, which is the **non-branch** loader: `RunConfiguration` is `extra="forbid"`, so a `branch_from` block fails validation outright (`PreflightError … {'loc': 'branch_from', 'msg': 'Extra inputs are not permitted'}`, reproduced directly). **Every branch command therefore fails before preparation begins.** `runner_protocol.py`'s design note ("`run branch` needs no new seam — route it through the existing `start`") was never carried into `Runner.start`, and the whole branch machinery has zero production callers: `load_branch_configuration_yaml`/`load_branch_configuration_file`, `parse_branch_from`, `BranchFrom`, `BranchFieldMismatch`, `check_branch_build`, `BranchBuildCheck`, `BranchSource`, and `abandon_branch`. `Runner.branch` additionally still raises `HarnessError("not implemented in this wave")`. Nothing sets `Run.parent_run_id`, writes a `branch_created` event, or runs the T174/T175 build/platform agreement check on a real branch — so Principle IV's "any branch that mutates or abandons a save MUST record which lineage it branched from" is unsatisfiable today. **Split by gate: the configuration, lineage, and build-agreement wiring is headless-implementable and is this task's scope; actually resuming the parent's save in the client is LIVE-GATED behind T217's `SaveLoader`** (`_NoLiveSaveLoader` raises on every call) and must fail loudly there rather than starting an unbranched run
 *(Closed 2026-09-21: the branch path is production — `Runner.branch` / `Runner.start` with a branch document, `run/composition.py` `prepare_branch` (FR-033/FR-034, lineage recorded, T174/T175 build and platform checks applied), `operator/cli.py` `run branch` — with 16 integration tests in `tests/integration/test_branching.py` (two branches from one save each record lineage; missing save, missing parent, unaccepted build and platform-crossing-without-spike all refused). The live identity check of two branches on the real client is T177, client-gated.)*
  **Scoping findings from the Phase 11 implementation pass (recorded so the next wave does not rediscover them).** Three things block a straightforward fix, and the first two are design decisions, not typing:
  1. **`MatchStore` cannot read a `RunConfiguration` back.** `load_branch_configuration_file` requires the *parent's* `RunConfiguration` (a branch inherits seed, civilization, ruleset, mod set, map and game settings from it and refuses to restate them differently), and the port exposes no way to get it. `create_run(run, config)` does persist it — `store/sqlite_adapter.py`'s `runs.config_json` — so the adapter change is trivial; adding the method to the `MatchStore` **Protocol** is the decision. It is safe with respect to `src/civsim_web/`, which defines its own separate read-only `store_client/port.py::MatchStore` and does not implement this one.
  2. **`saves/branching.py`'s `create_branch` and `run/composition.py`'s step 7 construct `Run` differently, and `create_branch`'s version is less honest.** `create_branch` hard-codes `comparability_status=COMPARABLE` and sets no `host_platform`, where step 7 takes `comparability_status` from `evaluate_host_gate` and records the platform. Routing branches through `create_branch` as it stands would record a branch on a degraded host as fully comparable — a direct contradiction of the plan's own "a platform may be less capable, but it may not be less honest" (FR-050, SC-013). `BranchSource` needs `comparability_status` and `host_platform`, **or** step 7 becomes the single run-construction path and `create_branch` is reduced to the lineage/event/save-load half. Pick one; do not add a second `Run` constructor.
  3. **Lifecycle states differ.** `create_branch` returns a run already `playing` with no recorded `preparing -> playing` transition, while the non-branch path creates `preparing` and transitions after V2 verification. Whichever path wins must still run V2 against the loaded save and must leave `_fail_preparation` a legal transition from whatever state it starts in.
  Until then `civsim run branch` fails loudly at configuration load, which is wrong but **safe** — it is not silently starting an unbranched run. Do not "fix" it by making the document load without also landing the lineage, `branch_created` event, and `check_branch_build` call: a branch that starts from turn 1 of a fresh game while claiming to be a branch is far worse than one that refuses.
  **STATUS: the headless half has landed; the task stays open because `civsim run branch` still cannot produce a branch against a real client.** What landed:
  - **All three scoping findings are resolved.** (1) `MatchStore.get_run_configuration` added to the Protocol, `store/sqlite_adapter.py` (a read of the `runs.config_json` `create_run` already writes), and `contracts/match-store-port.md`; `src/civsim_web/` defines its own separate read-only port and is untouched. (2) `BranchSource` now **requires** `comparability_status` and `host_platform` — `create_branch` no longer hard-codes `COMPARABLE` and no longer omits the platform, so a branch on a degraded host records itself degraded and the falsehood is unreachable rather than merely discouraged (Principle IV, FR-050/SC-013). (3) `create_branch` now creates the child `preparing`, not `playing`, and the composition root drives `preparing -> playing` after the same V2 read-back every other run gets — `failed` is reachable only from `preparing`/`resuming`, so a branch born `playing` could never be failed by its own setup verification.
  - **The branch reaches preparation.** `config/run_config.py` gains `peek_branch_from` (one cheap parse deciding which of the two non-interchangeable loaders applies); `Runner.start` routes a `branch_from`-bearing document to the new `Runner.start_branch`, which reads the parent run and its configuration back, loads the branch document against it, and prepares through `RunnerDependencies.prepare_branch` — a new optional seam whose production implementation in `run/composition.py` is literally `prepare_run` with `branch_from` set, so a branch passes every gate an ordinary run passes **plus** `check_branch_build` (T174/T175) and `require_available_save_point` (FR-036). `Runner.branch` is no longer a stub. `operator/cli.py` refuses a document whose `branch_from` disagrees with the command's own `run_id`/`--turn`, naming both, before the runner is reached. Lineage (`parent_run_id`/`parent_turn`) and the `branch_created` event are recorded on the child only; the parent's record is untouched (FR-034, I12).
  - **The blocked half fails loudly and specifically, as required.** With the production `SaveLoader` default (`_NoLiveSaveLoader`), a branch refuses inside `create_branch` at `loader.load(save_point)` — **before any child `Run` is constructed** — with a message naming the missing save-load capability, mirroring `Runner.resume_from`. Nothing is created, and no unbranched run is ever started.
  - Guarded by `test_run_branch_reaches_a_branch_instead_of_failing_at_configuration_load`, `test_run_branch_without_a_save_load_path_fails_loudly_naming_the_missing_capability`, and `test_run_branch_refuses_a_document_whose_lineage_disagrees_with_the_command` (tests/integration/test_end_to_end_wiring.py), plus `test_a_branch_on_a_degraded_host_records_itself_degraded` (tests/integration/test_branching.py). Confirmed to fail with the `Runner.start` routing reverted.
  - **Remaining, and the only thing remaining: T217's live verification.** T217's production `LuaSaveLoader` has landed and is the composition default, so `civsim run branch` now reaches a real front-end load (proven end to end against the wire-protocol fake in `test_run_branch_through_the_production_loader_loads_the_parent_save_and_plays`); no further branch wiring is needed. A branch against a real client remains unproven until a live session verifies the load path itself (see T217's own live-gated note).
- [X] T227 Acquire the run-identity lock in the production preparation path per **FR-006**, **V8**, research **R4** (missing) — `src/civsim_harness/run/identity_lock.py` (T075) defines `RunIdentityLock`/`ActiveRunLock`/`RunIdentityLockError` and **nothing in `src/` constructs any of them**, so `run/composition.py`'s `_prepare_run` starts a run without ever claiming its identity. The game's own single-tuner-connection limit covers two harnesses on one client, which is precisely the case the lock module says it does *not* duplicate; the case it exists for — two harness instances on the same `run_id` against **different** clients, producing two divergent records under one run id — is unguarded. Acquire before the run is created, release on every terminal path (alongside T214's client release in `_release_terminal_run_clients`). **Headless-implementable**
- [X] T228 Call `src/civsim_harness/telemetry/logging.py`'s `configure_logging` from the CLI entry point per **FR-020**, T015 (missing) — `configure_logging`, `attach_handler`, `JsonRedactingFormatter`, and `RedactingFilter` have **no caller in `src/`**, while `nexus/client.py` and `nexus/sentinels.py` both log through `logging.getLogger(__name__)`. T015's requirement is that the T014 redaction filter be "mandatory on every handler"; with nothing configuring handlers, those records propagate to whatever root handler the host process happens to have, with no redaction filter attached. Wire it once at `operator/cli.py` startup — not per module, and never as a second, parallel logging setup. **Headless-implementable**
- [X] T229 Record a `disk_headroom_low` event when a headroom check halts a turn, per **FR-007** and data-model.md §14 (partial) — `saves/headroom.py`'s `build_disk_headroom_event` and `estimate_footprint` have no caller, and `RunEventType.DISK_HEADROOM_LOW` is emitted **nowhere in `src/`**. `run/turn_cycle.py`'s `_take_quicksave` calls `check_headroom` first, but its `except` clause catches only `NexusError`/`SaveVerificationError`, so a `DiskHeadroomError` propagates with no event written — data-model.md §14 calls this event "the warning before the halt", and without it the run's own timeline carries no recorded reason for the stop, only the generic lifecycle-failure detail `run/runner.py` adds. Under Principle III a halt whose cause is absent from the turn-by-turn record is exactly the gap that disqualifies a run from trending. **Headless-implementable**
- [X] T230 Route the three declared camera actions through `src/civsim_harness/act/camera.py`'s `validate_camera_action` per **FR-026**, **FR-027**, research **R8**, T132 (missing) — `catalogs/actions/camera.yaml` declares `camera.move`, `camera.zoom`, and `camera.set_view_mode`, but `run/decision_loop.py` dispatches every decision through `act/dispatch.py`, which has no camera branch, and `validate_camera_action` has **no caller in `src/`**. A refused camera action is therefore recorded as the generic `UNAVAILABLE_TO_HUMAN_NOW` and `RejectionReason.OUT_OF_PARITY_CAMERA` is unreachable in production — the distinction R8 introduced the reason code for. The action is still gated by its own `availability_predicate`, so this is a misattribution plus a missing check, not an unguarded path. **Headless-implementable**
- [X] T231 Collapse the two structural-parity-filter implementations onto one per **FR-018**, T127 (partial) — `parity/filter.py`'s `filter_to_entries` has **no caller in `src/`**, while `observe/assemble.py` reimplements its exact logic inline (resolve the declaration, reject a non-observation/view kind, key by declaration id, carry the declaration's own `context`) and adds `output_schema` validation on top. Both are correct today, which is the problem: `parity/`'s own package docstring calls itself "Principle I's enforcement point", and the copy living there is the dead one, so a future reviewer hardening the parity boundary would edit a module the agent's data never passes through. Either have `assemble_observation` call `filter_to_entries`, or delete it and move its contract into `observe/assemble.py` — **one definition, wherever it lives**. **Headless-implementable**. **LANDED: `assemble_observation` now calls `filter_to_entries`; the `parity/` copy is the live one.** The resolve-and-kind gate was split out of `filter_to_entries` as `parity/filter.py::resolve_observable` (raising `ObservationAssemblyError`, never a bare `CatalogError`, so the assembler's existing error contract is unchanged) because `assemble_observation` needs the resolved declaration to validate `output_schema` against; `observe/assemble.py` keeps only what is genuinely its own -- schema validation and composing many results into one `Observation` -- and its inline `_resolve_observable` twin is deleted. **`parity/filter.py` is now the only module in `src/civsim_harness/` that constructs an `ObservationEntry`**, which is exactly the claim its own docstring always made and could not back. Guarded structurally, because two correct implementations agree on every input and no behavioural test can distinguish "one definition" from "two that match today": `test_exactly_one_module_in_the_harness_builds_an_observation_entry` (an AST scan over `src/civsim_harness/**`) and `test_the_assembler_routes_through_the_parity_filter_rather_than_copying_it` (a spy proving the call happens, since an unused import is the exact failure mode Phase 11 was written about), both in tests/contract/test_parity_redteam.py and both confirmed to fail with the de-duplication reverted
- [X] T232 Reuse or retire `src/civsim_harness/provider/accounting.py` per **FR-040**, T186 (partial) — `record_model_call`, `build_model_call`, `ModelCallSink`, and `ImageCountAccountingMismatch` have **no caller in `src/`**; `run/decision_loop.py` constructs its `ModelCall` inline and writes it via `ctx.store.write_model_call`. The production path is **not** unguarded — `_ChainBackedProvider` routes every call through `ProviderChain.complete_step`, whose `_assert_image_count` enforces the same P2 invariant — so this is a dead deliverable rather than a hole. Either route the loop's accounting through `record_model_call` (giving the P2 re-check the module's docstring says exists "for any caller that assembled a `DecisionResponse` without going through the chain") or delete the module and record that the chain is the single accounting path. **Headless-implementable**. **LANDED: reused, not retired.** `run/decision_loop.py` now builds its `ModelCall` through `provider/accounting.py`'s `build_model_call` instead of constructing one inline field by field, so there is one definition of "a completed provider call becomes a `ModelCall`" and the P2 image-count re-check runs on the loop's own path. It is **not** redundant with `ProviderChain._assert_image_count`: that guards `complete_step`, while `ctx.provider` is a chain only in the production composition (`_ChainBackedProvider`) and a bare `ModelProvider` in every other caller. A response disagreeing with its request on image count now halts the turn rather than writing a `ModelCall` whose `image_count` cannot be trusted (FR-039, I7). Guarded by `test_a_call_whose_image_count_disagrees_with_its_request_is_never_recorded` (tests/integration/test_decision_loop.py), confirmed to fail with the wiring reverted. `record_model_call`/`ModelCallSink` remain uncalled by design: the loop writes the failed-call `ModelCall` itself and lets a successful one ride along in the `TurnCycleRecord`, so a build-and-write helper would double-write the success path
- [X] T233 Wire the crash/hang detection layer into the run loop per **FR-044**, **FR-045**, **SC-010** (missing; found while implementing Phase 11, same shape as the rest of it) — `src/civsim_harness/resilience/detector.py`'s `DetectionAggregator`, `resilience/heartbeat_monitor.py`'s `HeartbeatMonitor`, and `resilience/operation_bounds.py`'s `OperationTimedOut`/`OperationKind` are **constructed nowhere in `src/`**: outside their own modules they appear only in `resilience/__init__.py`'s re-export list. `run/composition.py` and `run/runner.py` import exactly one name from the package, `RecoveryEngine`, and reach it only through `run/turn_cycle.py`'s mid-turn observation-assembly path. So research R12's four detection signals — process liveness against the client PID, the tuner heartbeat, the screen-identity probe, and per-operation bounds — never run during a real run, and a client that dies between turns is noticed only when the next Nexus call happens to fail, if it does. **SC-010 ("a genuinely killed client is detected within 60 s") has no mechanism behind it today**, and T193's live test would fail for this headless reason rather than for anything about the client. The client PID the aggregator needs is already available from `host.locate_game_process()`, which `_prepare_connected_run` now calls for T227's lock. **Headless-implementable**: the wiring, the aggregator's cadence, and its routing into `RecoveryEngine` are all testable against `FakeHostPlatform` and `FakeNexusServer`; only the 60-second real-kill timing (T193) needs a live client. **LANDED.** `run/detection.py` (new) binds the signals to one run: `DetectionWatch` runs process liveness first and alone (a synchronous `psutil` check that cannot block on `NexusClient`'s command lock), then the whole `DetectionAggregator` pass under `resilience.operation_bounds.run_bounded` -- so `DetectionAggregator`, `HeartbeatMonitor`, `ProcessLivenessMonitor`, `run_bounded`, `OperationKind` and `OperationTimedOut` all have production callers. `run/composition.py` builds one per run from the PID T227 already locates and this run's own connected client; `run/turn_cycle.py` asks it at two points: liveness before the turn's quicksave (the between-turns crash -- recorded and raised by name, since no start save for that turn exists yet to recover from) and the full pass concurrently with the decision loop every `detection_interval_s` (default 10 s, four passes inside SC-010's 60 s), routing a trip into the same `RecoveryEngine` path a mid-turn observation failure takes. A pass that exceeds its own bound is reported as **nothing** -- a busy client is not a faulty one (FR-014). The screen-identity probe is deliberately **not** wired in: `run/decision_loop.py` already polls `game.screen_state` between steps and raises `UnknownScreenEncountered` (FR-049, R13), and a second producer of `unknown_screen` would put two paths on one timeline entry. Guarded by `test_a_killed_client_is_detected_and_recorded_by_the_production_run_loop` / `test_every_turn_is_built_with_the_detection_layer_attached` (tests/integration/test_end_to_end_wiring.py) and `test_a_client_that_dies_mid_turn_is_detected_and_the_turn_is_replayed` (tests/integration/test_turn_cycle.py); each was confirmed to fail with the wiring reverted
- [X] T234 Fix the three live-client wire-protocol defects in `src/civsim_harness/nexus/client.py` and make `tests/fakes/fake_nexus.py` speak the **real** protocol, per **FR-011**, **FR-014**, research **R2** (contradicts; found by the Windows live bring-up session, `spikes/r5-save-path-windows.md` "The three protocol defects") — the stock harness could not drive any real client: (1) `_handshake` never read the `APP:` reply, so `_parse_state_list` received the client's three-field identification frame (`"Civ6\x00Sid Meier's Civilization 6\x00<binary dir>"`) and **every `connect()` failed** with "odd number of NUL-separated fields" (reproduced live by `civsim doctor` on this machine today); (2) `_query_states` raised on any non-`TAG_HANDSHAKE` frame, but the client interleaves unsolicited tag −1 log frames into the handshake window (`O\x00StagingRoom: RefreshStatus()...`, `r5-raw-windows/raw_protocol_transcript.txt`), so the first real `refresh_state_indices()` aborted; (3) `_await_result` fed only `TAG_COMMAND` (3) payloads to the sentinel correlator, but the real client returns tag 3 **empty** and delivers all print output — including every command's sentinel-bracketed result — on **tag −1**, one frame per line, prefixed `O\x00<StateName>: ` (`r5-raw-windows/raw_command_transcript.txt`), so **every command timed out while its Lua ran** (the "timed-out" `Network.LoadGame` had loaded the game). **Why 1539 green tests never caught it: the fake spoke the protocol the code expected, not the protocol the client speaks** — the same fake-shares-the-defect's-assumption shape as the V2/dead-Lua findings. **LANDED.** `nexus/codec.py` gains `TAG_ASYNC_OUTPUT = -1`; `client.py`'s `_handshake` consumes the `APP:` reply (opaque, logged), `_read_handshake_frame` skips interleaved async frames (routing print payloads to telemetry) instead of raising, `_await_result` feeds only prefix-stripped tag −1 frames to the correlator (`_strip_print_prefix`), and `resolve_game_states`/`refresh_state_indices` bound the now-skipping LSQ read with the per-command timeout. No silent dual-protocol tolerance: a non-empty tag-3 payload (the never-observed pre-live framing) is logged loudly and routed to telemetry, never accepted as a result. `tests/fakes/fake_nexus.py` now answers `APP:` with the real odd-field greeting, delivers every command result as prefixed tag −1 print frames plus an **empty** tag-3 acknowledgement, emits strays the same way, and gains `inject_async_frame_before_next_lsq` so tests can force mid-query interleaving; `tests/fakes/test_fakes.py` audits the fake's frames over a raw socket with no `NexusClient` in the loop. Windows and Linux evidence agree on the framing (the Linux t077/sweep spikes strip the identical `O\x00<State>: ` prefix and `nexus_probe.py` drains the `APP:` reply before `LSQ:`); whether the empty tag-3 ack is universal across builds still awaits the Linux peer re-running `raw_command_probe.py`. Two tests asserting the old wrong framing were corrected and are the audit trail: `test_nexus_client.py::_ScriptedTuner` (docstring claimed the real client never answers `APP:` — it does) and `test_connect_raises_nexus_error_when_lsq_response_has_the_wrong_tag` (asserted the interleave-aborts behaviour that IS defect 2; rewritten as `test_a_non_handshake_frame_is_never_parsed_as_the_state_list`). Regression-guarded per defect by `test_live_defect_1_handshake_consumes_the_app_reply_before_the_state_list`, `test_live_defect_2_interleaved_async_frames_do_not_abort_a_state_query`, `test_live_defect_3_result_arrives_on_tag_minus_1_prints_with_an_empty_tag_3`, plus `test_a_non_empty_tag_3_payload_is_telemetry_not_a_result` (tests/unit/test_nexus_client.py) — **each of the three client fixes was reverted once and its test confirmed to fail with the exact live symptom, then restored**. `contracts/nexus-protocol.md` gains the tag −1 row and a "Reply framing — verified against a live client" section citing the spike evidence per claim, including "a timeout means the answer was not read, not that the Lua did not run". Verified live after the fix: `civsim doctor` against the running client now reports `tuner connection : ok (GameCore_Tuner=10, InGame=132)` — the exact indices of the live capture's 136-state table. Suite: 1547 passed / 2 skipped / 0 failed (baseline 1539/2/0 plus 8 new tests)
- [X] T235 Enforce interface reachability structurally — every wired interface must have a production caller in `src/`, with negative controls proving each check can fail — per the adopted counter to this project's defining defect pattern (hypervisor log, Run 4: the Linux peer's reachability rule plus the Scribe's negative-control refinement, "either alone leaves half the family alive") — six ledger entries are the same shape recurring (the fabricated V2 verification, the dead Lua `return`s, T231's dead parity filter, T232's dead accounting module, T233's unwired detection layer, T234's fake speaking the protocol the code expected rather than the one the client speaks: 1539 green tests over a transport no real client could ride), and every one was found by a *manual* after-the-fact search for callers; nothing in the tree made "complete, tested, and called by nothing" a failing state. **LANDED.** `tests/contract/test_reachability.py` (new; no `src/` change) scans `src/civsim_harness/**` by AST — imports and `__init__.py` re-exports deliberately count for **nothing**, since "outside their own modules they appear only in the re-export list" was T233's opening symptom, and prose mentions are invisible for the same reason `test_read_only_boundary.py` gives — and asserts a caller outside the defining module for: every `HostPlatform` port method, every `NexusClient` public method/property, and every `RunnerDependencies` seam field (all three rosters **derived from the production AST at test time**, so a method added to any of those surfaces is guarded the moment it exists), plus the named collaborators where the pattern already bit — parity `filter_to_entries`/`resolve_observable`/`enforce_parity_boundary`, accounting `build_model_call`/`record_model_call`/`ModelCallSink`, the T233 layer (`DetectionAggregator`, `HeartbeatMonitor`, `ProcessLivenessMonitor`, `run_bounded`, `OperationKind`, `OperationTimedOut`, `RecoveryEngine`, `DetectionWatch`, `check_process_liveness`), `build_runner_dependencies`, `NexusClient` itself, `InputEvent`, and the Linux adapter's `capture_preconditions`. Seam fields are checked two-sided (supplied as a keyword outside the defining module AND attribute-read somewhere — a field failing either side is a dead seam), the port's three platform adapters are excluded as callers (an implementation calling itself is not wiring), and matching is by name rather than resolved type — an imprecision that only ever errs toward a false pass on a name something else genuinely uses, never toward a blocked merge, stated in the file rather than hidden. The honestly-uncalled live in a documented allowlist where every entry must cite a task ID or carry `UNRESOLVED - hypervisor review`: an empty or uncited justification fails the suite by itself, an entry whose target has since gained a caller fails as **stale**, and an entry naming no roster target fails as unknown, so the list can only ratchet down. Four entries are justified today (`record_model_call` + `ModelCallSink` per T232's "uncalled by design"; `ScreenIdentityProbe` per T233's recorded deliberate non-wiring; `assert_no_literal_leaks` per T225/T128 — production literal scanning runs inside `enforce_parity_boundary`, the helper is the load-bearing test tier's entry) and **three are UNRESOLVED findings for hypervisor review**: (1) `send_input` has **no caller anywhere in `src/` on any platform** — the demo's "first production caller" (hypervisor log, Run 5) lives in `specs/002-civ-playing-harness/spikes/r5-raw-windows/{bringup,advance_to_frontend}.py`, outside the package, and T217's `Network.LoadGame` resolution removed the planned consumer ("no bespoke driver, ever"); (2) `InputEvent` is constructed nowhere in `src/` (the three adapters only annotate with it) — same evidence, same open question: wire a portable caller or retire the synthetic-input layer by ruling; (3) `capture_preconditions` is called only from `tests/live` and spikes (validation-results.md already flags it: "no production caller — this one is ours") and no Phase 11 entry covers it. Do not silently re-justify these; wire them or retire them by ruling. Guarded by `test_every_wired_interface_has_a_production_caller_or_a_cited_exemption` and `test_every_allowlist_entry_carries_a_cited_justification`, with the derivation itself guarded (`test_the_derived_rosters_found_the_surfaces_they_claim_to_guard` pins the six port methods, the nexus surface, and the seam fields; a ≥50-module floor guards the scan root) so the checker cannot go vacuous by scanning nothing — the checker-that-cannot-fail problem, one level down. **Negative controls, each confirmed to fail when it should:** eight synthetic-tree tests drive the *same* checker functions the real enforcement runs and assert the violation is flagged while its correctly-wired twin passes — an uncalled port method (`test_negative_control_an_uncalled_port_method_is_flagged`), an import/re-export standing in for a caller (`..._an_import_or_reexport_alone_is_not_a_caller`, T233's exact shape), a call inside the defining module (`..._a_call_in_the_defining_module_does_not_count`, self-confirmation), an adapter vouching for itself (`..._an_adapters_self_call_does_not_satisfy_the_port`), a dead seam from either side (`..._a_dead_seam_field_is_flagged_from_either_side`), a stale allowlist entry (`..._a_stale_allowlist_entry_is_flagged`), an entry naming no target, and empty/whitespace/uncited justifications each rejected while cited ones are accepted. The real-tree enforcement was additionally verified falsifiable directly: with the `send_input` allowlist entry removed, the suite failed naming exactly that method ("UNREACHED: method target 'send_input' ..."); with an empty-justification entry planted, both the citation test and the unknown-entry guard failed; both manipulations were reverted. Suite: 1568 passed / 2 skipped / 0 failed (baseline 1547/2/0 plus 21 new tests); `ruff check tests/` clean
- [X] T236 `doctor` must report provider-key presence through the production resolution, per **FR-043**, **SC-018** (found live by the first model-driven run attempt, 2026-09-20) -- `operator/doctor.py::_probe_provider_key` used `bool(env.get(env_var))`, environment-only, while the production path (`config/secrets.py::require_secret`, used by `OpenRouterProvider._resolve_api_key` and `preflight_chain`) also reads the gitignored `secrets.yaml`. Confirmed live: `doctor` printed `provider key : MISSING` on a host whose key lives only in `secrets.yaml`, while `secret_is_present` returned True and a real `describe()` authenticated -- a misleading MISSING is exactly the operator-surface lie SC-018 exists to prevent, and `secret_is_present`'s own docstring already named itself "the one call doctor should make". **LANDED.** `_probe_provider_key` now calls `config.secrets.secret_is_present(env_var.lower(), env=..., secrets_file=...)`; `run_doctor` gains an injectable `secrets_file` (production default None = the resolver's own default path), because the absent-key test cases otherwise fall through to a real operator key in `Path.cwd()/secrets.yaml` -- the same hermeticity trap fixed for the adapter tests in `f45a5ed`. Guarded by `test_run_doctor_sees_a_key_that_lives_only_in_the_secrets_file` (tests/unit/test_operator_api.py), confirmed to fail with the env-only probe restored (presence False on a file-only key -- the exact live symptom); the presence-only guarantee re-asserted on the new path (the file-only value never appears in the report dump)
- [ ] T237 Provide a verified path from a cold client to turn 1 without an operator per **FR-001**, **SC-001** (gap found live, 2026-09-20: the first model-driven run attempt died at bring-up) -- `run/composition.py`'s preparation gate raises `no_game_loaded` unless the client is already at `HostGame` or `InGame`, and its own message records that the harness has no FireTuner path to start a new game or load a preset; T217 covers only "load a NAMED save" (FrontEnd `Network.LoadGame`). So every fresh unattended run is structurally gated on a human first reaching a loaded game. Candidate paths, in T217's spirit (tuner-first, Principle II): the FrontEnd Lua surface around `Network.HostGame` / game-setup + the same `automation_dailysmoketest.lua` corpus that resolved T217; a `firetuner_gap` + scoped UI-driver fallback needs a fresh owner ruling before any work (the T217 B authorization was scoped to exactly one operation and is spent). **DEFERRED-LIVE** (needs a FrontEnd client); the interim operating procedure is T217's: an operator or the recovery path brings the client to a loaded game, and runs resume from saves
 *(Audit 2026-09-21: the Lua path is live-verified and scripted — `tests/live/demo_landed_run.py --bring-up` starts a fresh `CivSim DEFAULT` game with Cyrus pinned through `Network.HostGame` / `PlayerConfigurations` (`spikes/r8-new-game-start-linux.md`, used by every 2026-09-21 run) — but it is explicitly operator scripting, not production: `run/preparation.py`'s gate still requires a loaded game. The production wiring is headless work deliberately not taken in this pass — it decides when the harness may start a game on its own, which is owner-adjacent — and its verification is client-gated.)*

## Phase 12: appended by the 2026-09-20 convergence audit (repo at 60eb482; adversarial read-only pass)

The audit confirmed every Phase 10/11 wiring claim it spot-checked, found src/civsim_web free of production-dead surfaces (001 CONVERGED at 65/65), and found the eight gaps below -- each a place where a test verified the component and nothing verified the wiring, or where a record field is a constant wearing a derivation's name. Ledger corrections established by these entries rather than by rewriting history: T226's status line ("the only thing remaining: T217's live verification") is contradicted by T241; T134's, T157's, T081's, and T158's done-claims are qualified by T238, T240, T243, and T239 respectively; composition.py's "no Lua capability enumerates the active mod list" comment is stale per T242 (preparation.py:1193 registers exactly that getter). T213 is extended: its live list now includes game.outcome_state and camera.read_state Lua (every camera.lua read is UNVERIFIED by the composition's own note). Parked, not appended: DEFAULT_WORST_CASE_CONTEXT_TOKENS=200_000 remains an honestly-labelled injectable placeholder.

- [X] T238 **[CRITICAL]** Attach screened images to the production decision request -- and stop the record claiming the agent saw images it never received -- per **FR-024, FR-015, FR-050, SC-013, SC-019**, T134 (missing): run/decision_loop.py:475-480 calls assemble_context() without the images argument, so every production DecisionRequest carries images=[] -- while agent/context.py:162's select_screened_images (T134's "the only function permitted to turn a step's captures into agent-visible images") has zero callers in src/ and zero references in tests/. Meanwhile observe/capture.py:273 sets shown_to_agent=outcome.is_clean and decision_loop.py:338 lists the capture on the observation whenever shown_to_agent -- so the moment any host produces a frame that passes the four screening gates, the record will assert the agent was shown an image the provider never received. A falsified shown_to_agent is the record-integrity failure this loop exists to prevent, strictly worse than missing images. Scope: decode the step's clean blob to an Image, route through select_screened_images, pass into assemble_context; make shown_to_agent/Observation.captures reflect actual attachment; and enforce T099's rule ("until a platform's spike passes, no run on it may show images to the agent") -- no gate today consults the R6 spike status at all. Regression test must assert a screened-clean capture's bytes reach provider.complete's request AND that shown_to_agent=True is only recorded when they did. **Headless-implementable** **LANDED: production attaches screened images through T134's chokepoint, and the record can no longer say "shown" about an image the provider never received.** Three coordinated changes, all inside the decision-loop lane. (1) `run/decision_loop.py` now builds each step's `images` via `_step_images` -> `agent/context.py::select_screened_images` (its first production caller; still the only function that turns captures into agent-visible images) from the step's own fresh `StepCapture`, whose clean blob is lifted to a wire `Image` via the new `StepCapture.blob_media_type` (`observe/capture.py`: populated exactly when the frame format has a wire form -- a raw BGRA8 framebuffer is stored as evidence but can never be attached as-is), and passes the result into `assemble_context(images=...)`. (2) Shown-ness became attachment, not intention: `observe/capture.py` now constructs every `ScreenCapture` with `shown_to_agent=False` (the old `shown_to_agent=outcome.is_clean` was the falsified-record half), the capture is written at the point its fate is settled -- immediately before the provider dispatch, as a `model_validate`d shown-copy iff its image is in that very request, or un-shown on every path where the frame demonstrably reached no request (trailing verification reads, `UnknownScreenEncountered`, `ParityViolation`, `ObservationAssemblyError`) -- and `Observation.captures` lists the capture id only on actual attachment. (3) T099's rule enforced with no composition change: `_images_permitted_for_run` reads `Run.host_support_tier` through the loop's existing `ctx.store.get_run` -- `VALIDATED` is written at preparation exactly when this platform-and-session's own R6 spike passed; unreadable run record fails closed. Guarded far-side in tests/integration/test_image_attachment.py (new; the real `run_turn_cycle` against `SqliteMatchStore`, a `FakeHostPlatform` serving a real screened-clean PNG, a real `kind=view` declaration): `test_screened_clean_capture_bytes_reach_the_provider_request` asserts the frame's exact bytes arrive in the `DecisionRequest` the provider received, shown/listing recorded for exactly that capture, `model_call.image_count == 1`, and the equally-clean trailing capture recorded NOT shown; `test_no_image_reaches_the_agent_on_a_platform_without_a_passed_r6_spike` asserts a SUPPORTED-tier run's clean frame is stored blob-and-all while the provider receives `images=[]` and every record says none was shown. Revert-confirmed twice: the old shape failed three tests with the exact live symptom (`assert [] == [b'\x89PNG...']` on the provider's received request while the record carried `shown_to_agent is True`); the gate stubbed `return True` failed the platform test with an `Image` reaching the provider. tests/unit/test_capture_for_step.py (new) pins the source half. Suite: 1593/2/0; ruff and mypy (strict) clean
- [X] T239 Make Run.record_completeness_status reflect the record, per **FR-052** and Principle III (contradicts) -- the run is created COMPLETE (run/composition.py:879; branches UNKNOWN, saves/branching.py:344) and nothing in production ever re-derives it: T158's derivation (store/completeness.py:40) is imported only by operator/audit.py:72; run/runner.py:521 serves the creation-time constant through run status. A resume-from rewind that dies mid-flight leaves real turn gaps behind a run still stamped complete; a finished branch stays unknown forever with a perfect record. Deliverable 1's trend-eligibility gate consumes this stale field across the port. Derive-and-persist at the moments it can change (turn persisted, supersede, terminal transition) or derive at read time; the served value must be the derivation, not the constructor default. **Headless-implementable** **LANDED: `record_completeness_status` is now the derivation at every surface.** `store/completeness.py` stays the single definition and gained two things: `first_owed_turn` -- a branch owes its record from its recorded `parent_turn` onward (it replays that turn from the parent's save; everything earlier is the parent's record, reachable through the Principle IV lineage), without which the unfloored derivation stamps every branch from turn N>1 `has_gaps` forever on a perfect record -- and `refresh_run_completeness`, the derive-and-persist half. Production now derives at creation (`run/composition.py` fresh runs and `saves/branching.py` branches both construct from the derivation -- UNKNOWN for an empty record -- never a constant; the hard-coded COMPLETE at composition.py:879 is gone), persists at every moment the answer can change (turn persisted: `run/turn_cycle.py` strictly after `write_then_advance`; supersede: `Runner.resume_from` and `abandon_branch`; every stop the runner records: `_finish`, `_pause_on_failure` -- the FR-042 chain-exhaustion shape, where pausing widens `turn_gaps`' checked range and the trailing quicksave-without-a-TurnCycle becomes a real gap -- and `_resync_after_external_termination` for RecoveryEngine-driven failures), and **serves a fresh derivation on every `run status` read** (`Runner.get_status`), which is what makes a run that died mid-flight report the gaps it left behind rather than its last stamp. `operator/audit.py::audit_completeness` applies the same `first_owed_turn` floor (summary gains `first_owed_turn`), so the audit's gap enumeration and the rolled-up status cannot disagree about what a branch owes. The consumer this was filed about is real and unchanged: `src/civsim_web/routes/metrics.py`'s trend-eligibility gate reads the persisted field across the port. Guarded by `test_completeness_served_and_persisted_is_the_derivation_not_the_creation_stamp` (tests/unit/test_runner.py), `test_a_persisted_turn_rederives_the_runs_stored_completeness` (tests/integration/test_turn_cycle.py), `test_a_branch_completeness_becomes_the_derivation_once_it_has_a_record` (tests/integration/test_end_to_end_wiring.py -- created UNKNOWN through the real composition root, COMPLETE once finished; stayed `unknown` forever before this), and five derivation/persist unit tests in tests/unit/test_completeness.py. **Four reverts each confirmed to fail with the live symptom then restored**: the read-time derivation (served value regressed to the stamp), the pause-path refresh (persisted stamp survived a genuine gap), the turn-persisted refresh (mid-run persisted value stale), and the branch floor (a perfect branch derived `has_gaps`)
- [X] T240 Downgrade Run.comparability_status when captures degrade mid-run, per **FR-050, SC-013**, T157 (partial) -- observe/capture.py:26's module docstring claims capture degradation "downgrades Run.comparability_status", and T157 is marked done claiming the same, but no writer exists anywhere in src/: the field is set once from the host gate at creation (composition.py:863,880) and only ever read back (runner.py:522). A run starting COMPARABLE on a validated host that loses its images mid-run keeps comparable on record. Step/turn-level visually_degraded marking is real; the run-level rollup the spec names is absent, and the docstring asserts it exists -- the fabricated-claim family. **Headless-implementable** **LANDED: capture degradation now downgrades `Run.comparability_status` on the record -- the docstring claim is finally true, and the docstring now names the real writer.** `run/decision_loop.py::_downgrade_run_comparability`, called from `_observe` the moment `capture_for_step` reports a step visually degraded (so the downgrade lands even when the attempt is later abandoned -- the frames were lost in reality either way), reads the run through the loop's existing `ctx.store.get_run` and, iff the current status is `COMPARABLE`, persists `update_run(run_id, comparability_status=VISUALLY_DEGRADED)` -- existing port surface, no store file changed. COMPARABLE-only by design: nothing can move a status up, an already-degraded run is never rewritten, a run unknown to the store is a no-op. Guarded by `test_mid_run_capture_degradation_downgrades_run_comparability` (tests/integration/test_image_attachment.py): a VALIDATED/COMPARABLE run whose host serves one clean frame then fails every later capture -- a genuine mid-run loss, not from-birth degradation -- asserts on what the store received: exactly one `update_run` call carrying the downgrade (later degraded captures do not repeat it), the read-back run serving the downgraded status, per-step flags 1 False / 2 True, `TurnCycle.visually_degraded` True, `capture_failed` events naming steps 2 and 3. Other side pinned by `test_an_already_degraded_run_is_never_rewritten_on_further_degradation` (zero `update_run` calls on a SUPPORTED run losing every capture). Revert-confirmed: with the call removed, `update_run_calls == []` -- the store was never told and the run kept `comparable` while losing its images, the exact fabricated-claim state the audit found. Suite: 1593/2/0
- [X] T241 Provide a production path to abandon a branch, per **FR-035**, T169 (missing) -- saves/branching.py:390's abandon_branch (records BRANCH_ABANDONED, marks turns superseded) has no caller in src/, and RunEventType.BRANCH_ABANDONED is emitted nowhere in production -- the same emitted-nowhere shape as T229's disk_headroom_low. No CLI/API command abandons a branch (operator-surface.md has none; run stop + archive emit neither the event nor the supersede). T226's own opening list named abandon_branch among "the whole branch machinery [with] zero production callers", but T226's landed-half status does not wire it and then claims live verification is "the only thing remaining". **Headless-implementable** **LANDED: branch abandonment has a production path.** `saves/branching.py` gains `abandon_branch_run` -- the one production caller's policy around T169's `abandon_branch` mechanism: it resolves *which* turns from the branch's own FR-007 save points (every turn the branch itself attempted; `abandon_branch` already skips a turn with no authoritative attempt, and a turn that is some further branch's lineage point is refused loudly by the store's FR-034/I12 guard, never worked around), accepts only a branch nothing may still be driving (`paused`/`finished`/`failed`; `BranchStillActiveError` otherwise -- the same pause-first discipline `resume_from` applies, since abandonment strips the record a live loop is still writing), records `BRANCH_ABANDONED` with the operator's `--reason`, and leaves the lifecycle honestly terminal: an already-terminal branch keeps the state it earned, and a `paused` one is driven through SS4's only legal exit -- `paused -> playing -> finished`, both edges recorded, `stop_resolution=operator_stop`. Respecting T239, `abandon_branch` itself re-derives and persists `record_completeness_status` after superseding -- honestly `has_gaps`, since an abandoned branch has no authoritative record left to trend on. Surfaced as `civsim run abandon <run_id> [--reason]` in `operator/cli.py` (records `lifecycle_command_received` before effect; needs no runner, like `archive`) and documented in contracts/operator-surface.md (command-table row, its own section, and an error-table row); CLI-only, no HTTP form, matching `saves reap`'s operator-invoked posture. Guarded by six tests in tests/integration/test_branching.py -- event/supersede/terminal/completeness far-side, the paused-to-terminal legal path (asserting `("paused","finished") not in` the recorded edges), both refusals, and two CLI tests asserting on the store the command wrote through, not its output. **Three reverts each confirmed to fail then restored**: the CLI registration (exit 2, "No such command" -- the emitted-nowhere shape back), the terminal-honesty half (paused zombie), and the abandon-side completeness refresh (stale field)
- [X] T242 Close the V2 no-read-path fallback, per **FR-002**/V2 (partial; the fabricated-V2 pattern in miniature) -- run/composition.py:923-938's _read_setting returns the configured value itself for any field whose name has no entry in _SETTING_GETTERS, so V2 passes vacuously for an open-ended set of fields. Its in-code justification ("mod_set chief among them: no Lua capability... enumerates the client's actually-active mod list") is stale and wrong: run/preparation.py:1193 registers a Modding.GetActiveMods() getter for mod_set. The fields actually passing vacuously include game_settings.victory_types -- present in configs/turn50-validation.yaml and load-bearing for FR-005 game_outcome stops. Scope: author getters for the reference config's uncovered fields (UNVERIFIED shape is fine -- fails closed), and make any field that does pass through the fallback recorded on the run rather than silently treated as verified. **Headless** (getter authoring; live confirmation rides T218) **LANDED: the V2 no-read-path fallback is closed to the extent it can be and honest where it cannot.** The stale in-code justification is gone: `mod_set` reads back through preparation.py's registered `Modding.GetActiveMods()` getter, and `run/preparation.py::_SETTING_GETTERS` gains `game_settings.victory_types` -- the reference config's one uncovered field, load-bearing for FR-005 `game_outcome` stops -- as an UNVERIFIED shape (enumerate `GameInfo.Victories`, report the enabled `VICTORY_*` names sorted; each read pcall-guarded; an empty/unreadable result returns nil and is reported unread, failing V2 closed, per T218's precedent -- live confirmation, including whatever normalisation the configured `[SCIENCE, ...]` spelling needs, rides T218 with the table's other UNVERIFIED entries). Every field named by configs/turn50-validation.yaml now has a registered read path, so what reaches `composition.py::_read_setting`'s fallback is only the open-ended tail (custom keys no getter has been authored for) -- and **every field that takes the fallback is recorded on the run**: the `preparing -> playing` lifecycle-transition event carries `v2_unverified_fields` plus a reason naming that those values were accepted on V3 seed-set agreement alone and are NOT verified against the live client. Fields with getters still fail closed on a genuine mismatch or an errored getter, unchanged. Guarded by `test_a_field_passing_v2_only_by_fallback_is_recorded_on_the_run` (tests/integration/test_end_to_end_wiring.py), which configures both `victory_types` (must NOT be in the recorded list -- a real read path exists) and a custom no-getter key (must be the list's exact single entry) through the real composition root against the wire-protocol fake. **Both reverts confirmed then restored**: with the recording reverted, the transition event carries bare `{'from','to'}` -- the silent vacuous pass verbatim; with the getter reverted, `victory_types` falls back into the recorded list
- [X] T243 Perform V11's preflight, per **contracts/run-configuration.md V11**, T081 (partial) -- V11: free disk space at or above min_free_disk_gb AND the estimated footprint of the run's saves and captures fits within it. saves/headroom.py:56's estimate_footprint has no caller in src/; check_headroom's footprint parameter (docstring: "the preflight call, V11") is never supplied; _prepare_run/_prepare_connected_run perform no headroom check at all. A run below headroom starts and dies at its first quicksave instead of being refused for free at preflight. T063 asserts V1/V3/V7/V8/V9/V12/V13 -- V11 is asserted nowhere. **Headless-implementable** **LANDED: V11's preflight is performed.** `run/composition.py::_prepare_run` step 2 -- the gates that need no live client, before any `Run` exists and before the tuner is dialled -- now calls `saves/headroom.py::estimate_footprint(config)` (its first production caller) and `check_headroom(..., footprint=...)` (the docstring's "the preflight call, V11" parameter, supplied for the first time) against the composition root's `disk_check_path`, refusing for free with a `PreflightError` whose detail carries the check's own numbers plus `estimated_footprint_bytes`/`estimated_turns` -- the existing preflight refusal idiom, and R17 intact: nothing is ever deleted to make room. Both `_prepare_run` and the branch path run through this one gate. Guarded end to end by `test_a_run_whose_estimated_footprint_cannot_fit_is_refused_at_preflight` (tests/integration/test_end_to_end_wiring.py): free disk above the floor but below the estimate, composed against a port **nothing listens on** -- so reaching `connect()` at all is the defect -- asserting exit 1 naming V11 and zero run rows in the store; and the missing V11 assertion now lives alongside T063's V-coverage in tests/contract/test_run_configuration.py (module docstring updated to name V11): the footprint-exceeds-free case rejected above the floor with the estimate in the error detail, the below-floor case, the enough-room pass, and a structural guard that `_prepare_run` still makes both calls. **Revert confirmed with the exact live symptom then restored**: with `footprint=None` -- precisely the pre-fix state the audit described -- the below-headroom run sailed past preflight and died dialling the tuner ("Could not connect to the Nexus tuner interface"), refused by nothing and naming no V11. Suite after all four tasks: 1607 passed / 2 skipped / 0 failed; ruff and strict mypy clean
- [X] T244 Assert the published get_run_configuration semantics in the port conformance suite, per **contracts/match-store-port.md E5** (untested) -- the amended contract names tests/contract/test_match_store_port.py as where port conformance is asserted, and the adapter's implementation is correct (store/sqlite_adapter.py:723-737: keyed WHERE run_id = ?, None on unknown id), but zero conformance tests cover it. E5's stated worry -- a config_id/run_id collision silently serving the wrong run's configuration -- is unpinned against the 002 adapter. Add: unknown run_id -> None; a run whose config_id equals another run's run_id never cross-resolves; verbatim round-trip. **Headless, small** **LANDED: E5 asserted in the port conformance suite.** Three tests added to tests/contract/test_match_store_port.py in the file's own conventions (the single `store` fixture over `SqliteMatchStore` -- the suite has no multi-store parametrization to widen): `test_get_run_configuration_returns_none_for_an_unknown_run_id`; `test_get_run_configuration_never_cross_resolves_a_config_id_run_id_collision`, which pins the contract's stated keying collision from **both** directions -- a collider whose `config_id` equals a victim's `run_id` resolves to its own configuration and the victim's id to the victim's, and additionally that an id known to the store *only* as a `config_id` answers `None` rather than resolving against the secondary key; and `test_get_run_configuration_round_trips_the_written_configuration_verbatim` (model equality plus `model_dump(mode="json")` equality). No adapter change: `store/sqlite_adapter.py:723-737`'s `WHERE run_id = ?` implementation passed all three on the first run, confirming the audit's "implementation correct, semantics unpinned" verdict
- [X] T245 Collapse the two observation-assembly-failure event builders onto one, per **FR-046**, T096/T231's rule (partial) -- observe/assemble.py:228's handle_assembly_failure has zero callers in src/ and zero test references, while resilience/recovery.py:292-318 builds the OBSERVATION_ASSEMBLY_FAILED event inline. Two implementations, and the one in the named home is the dead one -- T231's exact shape. Route recovery through the helper or delete it. **Headless, small** **LANDED: routed, not deleted -- the named home is the live one, T231's exact resolution.** `resilience/recovery.py::recover_from_observation_assembly_error` now builds its `observation_assembly_failed` event via `observe/assemble.py::handle_assembly_failure` (T096's named home) instead of its inline `_event(...)` twin, which is deleted; **observe/assemble.py is now the only module in src/civsim_harness/ that constructs the OBSERVATION_ASSEMBLY_FAILED RunEvent**, which is the claim its own module docstring made and could not back. The helper's `step_index` became optional (`int | None = None`, recorded in its docstring): the production caller has no discrete step index by recovery time -- the step attribution rides inside the error's own `detail`, merged onto the event verbatim, so the event recovery writes is unchanged field-for-field. Guarded structurally, because two agreeing builders are behaviourally indistinguishable: `test_exactly_one_module_in_the_harness_builds_the_assembly_failure_event` (an AST scan for `event_type=...OBSERVATION_ASSEMBLY_FAILED` construction sites -- naming the enum as a filter or trigger deliberately not counted, with its own negative control) and `test_recovery_builds_the_assembly_failure_event_through_the_named_home` (a spy proving the store-received event **is** the helper's build, since an unused import beside a revived twin is this family's move), both in tests/unit/test_observe_assemble.py (new), both confirmed to fail with the inline builder restored -- the scan naming resilience/recovery.py by path -- then restored. `handle_assembly_failure` thereby gains its first production caller. Bookkeeping in the same lane: the three reachability allowlist entries formerly marked UNRESOLVED now cite the Run 6 ruling (send_input/InputEvent retained dormant per T217's resolution, next-consumer-must-delete preserved; capture_preconditions referred to the Linux peer), per-entry citation test green

### Phase 12 additions from the Linux peer's live session (2026-09-20 evening, issue #1; account handoff window)

- [X] T246 Honor the client's post-close connection-refusal tail in NexusClient.reconnect(), per **FR-014**, T234 (extends) -- live finding (Linux, 1.0.12.9): the client refuses new connections for ~2s after the previous one closes, an undocumented timing tail on the "one connection at a time" rule. A reconnect() racing that tail reads as "client dead" when the client is fine. Scope: bounded retry-with-backoff inside the reconnect path (never the first connect -- a genuinely dead client must still fail fast), the tail documented in contracts/nexus-protocol.md, regression via fake_nexus gaining a refuse-for-N-ms-after-close mode. **Headless-implementable** **LANDED.** `NexusClient.reconnect()` now rides out the post-close connection-refusal tail with a bounded retry-with-backoff, while `connect()` and a reconnect before this client has ever connected still fail fast (a genuinely dead client never costs the retry budget -- the retry is gated on `_has_connected`, tied to the exact semantic difference: there is no prior socket being released). The refusal is caught via `_is_reconnect_refusal`, which recognizes all three shapes it surfaces as (TCP `PreflightError`, clean-EOF `NexusError` `REASON_CONNECTION_CLOSED`, and Windows `ConnectionResetError`), retries under a doubling capped backoff up to `DEFAULT_RECONNECT_MAX_ATTEMPTS`, and never retries a real protocol violation. `tests/fakes/fake_nexus.py` gained a time-based `refuse_connections_for_after_close(window_s)` mode (distinct from the count-based `refuse_next_connections`) with a raw-socket audit in test_fakes.py, and contracts/nexus-protocol.md gained "The post-close connection-refusal tail" section citing the live evidence (Linux 1.0.12.9, 2026-09-20, issue #1). Regression-guarded by four tests in tests/unit/test_nexus_client.py (rides out the tail; fails fast when never connected; gives up after the bounded budget with the exact backoff schedule; a handshake failure is not retried); the tail-riding test was reverted once and confirmed to fail with the connection-refused symptom, then restored. Suite: 1637/2/0
- [X] T247 A zombie tuner must be detectable, per **FR-044/SC-010**, T233 (extends) -- live finding, reproduced twice: a refused Network.HostGame leaves the client process alive and painting frames with a permanently dead tuner port. Process-liveness and window checks see a healthy client forever. T233's tuner heartbeat is the signal that should catch this, but the bounded-pass rule ("a pass that exceeds its own bound is reported as nothing -- a busy client is not a faulty one", FR-014) may eat it: a dead-forever port and a slow client are indistinguishable to a single timed-out pass. Scope: distinguish sustained heartbeat failure (N consecutive timed-out passes) from a single busy pass, and route the sustained case into recovery. **Headless-implementable** (fake with a permanently-silent-tuner mode) **LANDED.** `DetectionWatch` counts consecutive aggregate passes that exceed their own bound; a single one is still reported as nothing (a busy client is not a faulty one, FR-014), but a streak of `DEFAULT_SUSTAINED_HEARTBEAT_FAILURES` (2, justified in-code against SC-010's 60s budget and the ~10s cadence: the pass bound is FR-014-floored at command+heartbeat = 40s and cannot be shrunk, so 2 is both the smallest meaningful "sustained" and the largest that keeps the backstop bounded; the common zombie is caught on pass 1 via `hang_detected` well inside 60s, and the streak is the backstop for the residual eaten-forever case) is classified `unresponsive_detected` (reason `sustained_heartbeat_failure`) and routed into the same `RecoveryEngine` path a tripped detection takes; any completed pass resets the streak, so a busy-but-servicing client never trips. `tests/fakes/fake_nexus.py` gained `go_silent_after_handshake()`, modeling the reproduced behavior on the already-open connection the heartbeat probes (the "answers the handshake then goes silent" half; the "port holds closed for subsequent connects" half is the refuse-connections modes), stated in the method docstring. Guarded by five tests in tests/unit/test_detection.py (single eaten pass is not a fault; sustained eaten passes trip and route to recovery; a completed pass resets the streak; end-to-end zombie via the real heartbeat and the silent fake; the N/budget arithmetic asserted) plus a fake smoke test in test_fakes.py; the sustained-failure logic was reverted once and confirmed to leave the zombie undetected within the SC-010 window in the test clock, then restored. Suite: 1637/2/0
- [X] T248 Production LuaSaveLoader must dismiss the leader-intro screen, per **FR-031/T217** (completes the named-save-load operation) -- live finding (Linux, SaveLoader checklist): the shipped loader times out on every real load because nothing dismisses the leader-intro screen, and the intro screen is exactly what holds the tuner port closed, so there is no in-band signal to await -- polling the port itself is the only observable. A failed load also strands the client (the closed port refuses subsequent connects). The peer's local Escape-retry-loop patch is the proven shape. **HYPERVISOR RULING (2026-09-20): intro dismissal falls WITHIN the original T217 option-B owner grant** -- the single authorized operation was "load a named save" and the load is not complete while the intro holds the port closed; synthetic input here is bounded to dismissing that one screen during that one operation. Per T235's tripwire, wiring this consumer DELETES the send_input/InputEvent allowlist entries. Wayland limitation pre-accepted by the owner. **Blocked on the peer publishing their patch** (requested via issue #1); then headless transcription + live re-verification on their side. **UNBLOCKED and TRANSFERRED (2026-09-20 night):** the patch is published (`44a2a00`, `spikes/t248-intro-dismissal-patch.md` -- measured, not designed: as-shipped FAIL 160.4s, patched PASS 40.1s; the press must be a retry loop inside `_await_phase`'s except branches, because the closed port is the only observable and the poll succeeding is what stops the presses). With the Windows host offline overnight, T248 implementation is TRANSFERRED to the Linux hypervisor (they wrote the patch and hold the client for the unmodified `tests/live/test_production_save_loader.py` re-verification). **Hypervisor ruling on the flagged gap: Option 1 -- add `focus_window` to the HostPlatform port**, called by the loader before each press; option 2 (document a focus requirement) makes unattended runs fragile in exactly the hard-to-debug way this project exists to avoid, and the port operation kills the works-in-isolation-targets-nothing-in-production class outright. `focus_window` lands within the same T217-B scope as the dismissal itself; the reachability roster auto-guards the new method; the T235 tripwire applies (wiring the consumer deletes the send_input/InputEvent allowlist entries); Windows/macOS halves honest-minimal per T249's precedent. **LANDED `2f1a16d` + LIVE-VERIFIED (2026-09-20 22:45 EDT, Linux, client 1.0.12.9):** `tests/live/test_production_save_loader.py` re-run UNMODIFIED against the landed loader -- ATTEMPT 1 (production loader exactly as shipped, no external dismisser) `load() SUCCEEDED in 38.4s` (was FAIL after 160.4s before the patch); ATTEMPT 2 PASS 34.6s. A third solo run reporting the loader's own counters: SUCCEEDED 42.5s, `intro_dismiss_presses = 1`, `intro_dismiss_skipped = None` -- so `focus_window` (EWMH `_NET_ACTIVE_WINDOW`) returned `ok` live and the single Escape landed in the game. Transcript: `spikes/t248-intro-dismissal-patch.md` ("Re-verified"). The Windows/macOS `focus_window` halves still report `unavailable` and remain for the Windows side to implement for real
- [X] T251 `mod_set` must key on `Id`, not `Name`, per the Linux peer's live carry-forward from T250's landing (issue #1, 2026-09-20 night) -- fold into the T248 wave or the next preparation touch; live confirmation rides the peer's client time
. **LANDED + LIVE-MEASURED (2026-09-20 night, Linux, `spikes/t251-mod-set-identity-linux.md`):** `Modding.GetActiveMods()` entries carry `Id`/`Name`/`Handle` and NO `Version` (the getter printed "nil" x22 -- V2 was un-passable on any modded host); ids are mixed-case in load order; version reachable only via `Modding.GetModProperty(m.Handle, "Version")` (MPH 179, BBM 1.39.5, BBG 70500; every official pack nil). Landed: handle-keyed version read, lower-cased ids, `canonical_mod_set` on both sides, `ModRef.version: str | None`, `reconcile_mod_set_versions` recording unreportable pinned versions as `mod_set[<id>].version` in `v2_unobservable_fields`, V3/branch keys case-folded, seed set rewritten to the measured versions, schemas regenerated, 6 unit tests with negative controls
- [X] T249 Give capture hygiene a production preflight seam on the HostPlatform port, per **Principle I**, T235 finding 3 (peer-confirmed real) -- the peer ruled capture_preconditions stays UNRESOLVED (not allowlisted away): hygiene preconditions running only in live tests is a real gap, and the fix needs a preflight hook on the port that does not exist yet (Windows-owned port work; the peer writes the Linux half once the seam exists). Scope: port seam + call site in the capture path + per-adapter implementations kept honest (UNPROBED platforms refuse, per T099's rule). **Headless for the seam; live per platform for the halves** **LANDED (seam half): capture hygiene has its production preflight seam on the port -- the per-platform live halves stay open.** `host/port.py` gains `check_capture_preconditions() -> CapturePreconditionResult`, the cheap per-capture preflight whose contract states in the port's own idiom that a non-passing result means the frame is never taken and the step is recorded withheld with the reason, and that **passing never substitutes for the R6 spike** -- T099's VALIDATED-tier gate at attachment is untouched; this check can only take a capture away. `CapturePreconditionResult` requires a reason in *both* directions, so a hard-coded evidence-free pass is unrepresentable. The one call site is `observe/capture.py::_attempt_once`, before `capture_window`, routing failure into the existing `_host_failure_outcome` degradation path (withheld `CAPTURE_FAILED` record, `capture_failed`/`image_withheld` events, T240 downgrade) -- no parallel bookkeeping. Per adapter, only what is really checkable today: Linux **delegates to the peer's live-verified `capture_preconditions`** (T235 finding 3's orphan becomes the wired code; seam note records the peer owns live verification, Wayland refuses while the portal path is unimplemented), Windows resolves the PrintWindow path's user32/gdi32 entry points through the capture path's own `_gdi()`, macOS delegates to the same `_preflight_screen_recording` gate `capture_window` runs. The reachability roster auto-guards the new method (revert-confirmed: unwiring it fails the suite naming it); the `capture_preconditions` allowlist entry is updated to cite T249 -- it cannot go stale because its only caller is the adapter's own delegation, which the adapter-self-call exclusion deliberately discounts, and the delegation is pinned instead by `test_the_linux_port_preflight_is_the_live_verified_preconditions_verdict`. Guarded far-side by `test_a_failing_precondition_on_a_validated_host_withholds_and_attaches_nothing` (tests/integration/test_capture_preflight.py, new -- real `run_turn_cycle` over `SqliteMatchStore`, a VALIDATED host that *would* serve a clean frame: provider request carries no image, `capture_window` never called, both captures written withheld with the reason on the timeline, exactly one comparability downgrade) plus 2 source-half tests in tests/unit/test_capture_for_step.py and 7 per-adapter tests in tests/unit/test_host_probe_and_composition.py. **Four reverts each confirmed to fail with the live symptom then restored**: the call site (a clean frame taken and attached despite failing preconditions, plus the reachability UNREACHED), the Linux delegation (a drifted no-op pass), the Windows probe (a constant pass), and the macOS permission gate (denial unconsulted). T238's tests pass unmodified. Suite: 1626/2/0 on the combined three-lane tree; ruff and strict mypy clean. **Open:** the Linux live re-verification (peer's half) and any real Windows/macOS hygiene precondition beyond callability, which today is honestly nothing
 *(Closed 2026-09-21: seam landed (`38903b4`), Linux half **live-verified with a negative control** (`bd1a1a2`, `tests/live/test_t249_precondition_seam.py`), and T252 now feeds the same verdict into the support probe. The Windows and macOS halves are implemented in their adapters but unrun on-platform — other-platform, verification is T198.)*
- [X] T250 Read phase-dependent settings in-game only, per **V2/FR-002**, T218 (closes it) -- the peer closed T218 live and found a second phase-dependent getter: mod_set (Modding.GetActiveMods()) returns 0 at front-end vs 22 in-game, worse than major_count because two differently-modded hosts could both record 0 at prep time and be falsely judged COMPARABLE (Principle IV risk). Accepted ruling from the peer's evidence: mod_set and major_count are read in-game only, never at preparation time; the resources sentinel is dropped (confirmed unobservable in either phase -- an unreadable field fails closed as UNVERIFIED, it does not kill the run). Touches run/preparation.py (in flight in the T239-T243 lane -- SEQUENCE AFTER IT LANDS). **Headless-implementable; live confirmation rides the peer's next session** **LANDED: mod_set and opponents.major_count are in-game-only reads, and map_settings.resources no longer kills a run over a value nothing can read.** `run/preparation.py::_SETTING_GETTERS` entries are now `SettingGetter(expression, verified, phase)` records: `mod_set` and `opponents.major_count` declare `SettingReadPhase.IN_GAME`, and `LuaGameSetupReader.read` at any other Lua state does not dispatch them at all -- the Lua the tuner receives contains no `Modding.GetActiveMods()` and no major-count read -- reporting them in the snapshot's own `phase_deferred` bucket, distinct from `unread`'s no-getter tail, while `read_setting` still fails them closed for any caller that compares anyway. `major_count` no longer reads `GetAIPlayerCount()` in any phase (in-game it counts every non-human player -- T218's measured 16): the getter is the spike's own measured `Players` derivation (alive majors excluding the local player; zero majors reports unread rather than -1). The in-game re-check required no new wiring: the composition root's single V2 pass already executes at exactly the moment T250 names -- `_prepare_connected_run` step 8 reads the snapshot in the `InGame` state, post-load/pre-turn-1 -- so the in-game-only fields are genuinely read and compared there fail-closed (guarded by `test_an_in_game_only_field_mismatch_still_fails_the_run_closed`), and a deferred field reaching any comparison is an `UnreadSetting` mismatch -- a deferred check that never runs fails the run, never passes it. The resources sentinel is dropped the honest way: `map_settings.resources` moved into `UNOBSERVABLE_SETTING_FIELDS` (a live-measured negative result citing T218's three-nil probe), the snapshot reports it in its own `unobservable` bucket, and `composition.py::_read_setting` accepts it on V3 seed-set agreement and records it on the preparing -> playing transition as `v2_unobservable_fields` plus a reason -- deliberately distinct from T242's `v2_unverified_fields`, because "we looked and it is not observable" must not wear "nobody authored a getter". Guarded by tests/unit/test_game_setup_reader.py (7 tests) and two far-side tests through the real composition root against the wire-protocol fake, asserting on the Lua the fake tuner received; the phase rule is stated in contracts/run-configuration.md's V2 prose. **Three reverts confirmed then restored**: the composition's unobservable branch reverted -> the resources run dies before turn 1 (the old sentinel verbatim); the phase declaration reverted to ANY -> the front-end snapshot dispatches `Modding.GetActiveMods()`; `GetAIPlayerCount()` restored -> both the unit revert guard and the wiring's dispatched-Lua assertion fail. Still live-gated on the peer's next session: the Players-walk wrapper as one dispatched expression, mod_set's `{id, version}` shape, and victory_types as before. Suite: 1626/2/0 on the combined tree

## Phase 13: Convergence

Appended 2026-09-21 by the Linux node's headless convergence pass (repo at `dcb1cca`): the
codebase assessed against spec.md, plan.md and the Phase 1–12 tasks, with the five model-driven
runs of 2026-09-21 (`spikes/first-model-driven-runs-linux.md`) as the live evidence of what the
spec requires and the code does not yet do. Severity-ordered. No constitution-MUST violation was
found (Principle I: every new surface below names its in-client human equivalent; Principle II:
every path is Firetuner, none is bespoke).

- [X] T252 Credit the T249 capture-precondition verdict in the support probe and give raw frames a wire form, per FR-050, SC-013 and research R6 (partial). HIGH. *(Landed 2026-09-21: `probe_host_support` now consults `host.check_capture_preconditions()` itself on Linux/X11 when its caller passes no verdict — which fixes `run start` and `doctor` at once — and `observe/capture.py` transcodes raw frames to PNG (RGB, lossless) before screening via `parity/screening.py::transcode_raw_frame_to_png`; 5 new tests. Headless-verified only: the tier flip and a real frame reaching a provider call are client-gated.)* `run/composition.py` and `operator/cli.py`'s `doctor` both call `probe_host_support` without `compositing_verified`, so the recorded Linux/X11 R6 pass is never honoured and the one host whose spike passed reports `SUPPORTED` / `capture path: none`; independently, a screened-clean `BGRA8` frame has no `blob_media_type`, so the run loop can never attach it. Pass `host.check_capture_preconditions().passed` into the probe at both call sites, and transcode raw framebuffer frames losslessly to PNG in `observe/capture.py` *before* screening, so one byte string is screened, hashed, stored and sent. Human equivalent: none needed — this changes only whether the harness's own capture reaches the agent, never what it contains.
- [X] T253 Map `TechCivicCompletedPopup` and `BoostUnlockedPopup` to catalog prompts with an acknowledge action, per FR-010, FR-049 and US1 (missing). HIGH. *(Landed 2026-09-21: `lua/ingame/screens.lua` maps both states to `prompt.tech_civic_completed` / `prompt.boost_unlocked`, offers `["continue"]`, and dismisses through `UIManager:DequeuePopup(<the popup's context>)` with every step pcall'd and returned; `catalogs/actions/prompts.yaml` declares both; catalog version 2026.09.3. The Lua is executed for real in `tests/unit/test_screens_lua.py` (11 tests, embedded Lua with the game's globals stubbed; skips without `lupa`). UNVERIFIED LIVE that the real `UIManager` accepts another state's context from InGame — the client is sitting on exactly this popup, so the first live run settles it.)* Both are on `lua/ingame/screens.lua`'s watchlist but map to no catalog screen id, so the run stalls `UnknownScreenEncountered` at the first completed civic or eureka (measured live, attempt 5 — play stops there today). Add `prompt.tech_civic_completed` and `prompt.boost_unlocked` to the Lua id map with `prompt_options = ["continue"]`, dismiss through the popup's own `UIManager:DequeuePopup(<its ContextPtr>)` — exactly what the popup's Close button and its Escape handler call in Firaxis's shipped `techciviccompletedpopup.lua` / `boostunlockedpopup.lua` — and declare `prompts.tech_civic_completed` / `prompts.boost_unlocked` in `catalogs/actions/prompts.yaml`. Human equivalent: clicking "Continue" (or pressing Escape) on the popup. The civic popup's "Change Government" button is deliberately not offered (a subset of the human's options, reachable through `policies.*` instead).
- [X] T254 Resolve the turn-timer type name by forward hash when the reverse `GameInfo` scan returns nil, per FR-011, FR-014 and T204 hardening item 2 (partial). MEDIUM. *(Landed 2026-09-21: the same one-dispatch snapshot body now also compares the read hash against `DB.MakeHash(<name>)` for the five known type names; reverse still wins when it answers; the snapshot records which resolution answered (`turn_timer_resolution`) and the preflight carries it as its reason; both-nil stays UNVERIFIED with the hash named. 4 new tests. Live confirmation that the forward comparison matches `-1525060181` on 1.0.12.9 is client-gated.)* `civsim_resolve(GameConfiguration.GetTurnTimerType(), "TurnTimerTypes", "TurnTimerType")` returned nil for hash `-1525060181` (= `TURNTIMER_NONE` per the seed set) on Aspyr 1.0.12.9, so every preflight on this build records `UNVERIFIED` where `VERIFIED_NONE` is the truth. Compare the read hash against `DB.MakeHash(<name>)` for the known type names when the scan finds nothing, and record which resolution answered. Human equivalent: the Smart-Timer setting shown on the Advanced Setup screen.
- [X] T255 Produce `city.is_selected` from `UI.GetHeadSelectedCity()`, per catalogs/README.md §4 and FR-018 (missing). HIGH. *(Landed 2026-09-21: new InGame observation `cities.selection` (`lua/ingame/city_selection.lua`, capability `cities.selection`, `has_selection` + id/owner) and a binder overlay in `act/predicates.py` that gives the bound `city` an `is_selected` when its own body carries none — a body that answered is never overridden; `cities.state` stays GameCore_Tuner. 3 predicate tests + 5 Lua-executing tests. UNVERIFIED LIVE that the InGame tuner state answers `UI.GetHeadSelectedCity()`; client-gated.)* Every city order's availability predicate begins `city.is_selected and …`; `cities.state` is `GameCore_Tuner` and `UI` is InGame-only, so no city order has ever been dispatchable (the unit half was fixed 2026-09-21 in `units.state`). Add an InGame `cities.selection` observation (`lua/ingame/city_selection.lua`, capability `cities.selection`) and overlay its `selected_city_id` onto the bound `city` namespace in `act/predicates.py`, *without* moving `cities.state` to InGame — its `GetAvailableProduction` call appears in no shipped InGame UI file and its GameCore_Tuner body is the live-verified one (T213, 14/14). Human equivalent: the city whose panel the player has open.
- [X] T256 Declare each action's target shape and render it to the model, per FR-020 and FR-024 (partial). MEDIUM, owner-reviewable. *(Landed 2026-09-21: optional `target_kind` (11-value enum) + `target_hint` on `ParityDeclaration`, actions-only, additive (`ParityDeclaration.schema.json` regenerated); all 36 actions annotated; `agent/context.py` renders one concrete `{"target": …}` example plus the hint on each action's own line. 6 tests incl. a real-catalog coverage check. Whether the rendered example stops the id-for-plot confusion is a model-driven measurement — client-gated. The separate `subject`/`target` alternative remains the owner's call.)* The single `target` field carries a unit id, a plot, a tech name or a prompt option depending on the action, and the model sent the warrior's id as `units.move_to`'s target 16 times in a row even after the summary said `{"target": {"x": .., "y": ..}}`. Add an optional `target_kind` field to `ParityDeclaration` (additive; regenerate `ParityDeclaration.schema.json`), set it on every action, and render one concrete `{"target": …}` example per action in `agent/context.py`'s catalog text. The alternative — separate `subject` and `target` parameters — changes the predicate binder, the Lua argument convention and every unit/city order, and is recorded here for the owner's ruling rather than taken.
- [ ] T257 Implement the `xdg-desktop-portal` ScreenCast capture path for Linux/Wayland, per plan §Host platform table and research R6 (missing). LOW on this project (no Wayland host exists in it). `LinuxHostPlatform._capture_via_portal` raises `NotImplementedError` and is reported `unavailable` with its reason; T052's X11 half is closed and live-verified. Needs a Wayland session (this node is X11), a `dbus-python`↔asyncio strategy, and a decision on the interactive grant R6 calls "hostile to unattended runs". Other-session gated.
- [X] T258 Record the human-visible per-turn yields on every turn record, per Constitution III (Complete Match Telemetry), FR-013 and data-model.md §5 `TurnCycle.yields` (missing). *(Landed 2026-09-21: `player.yields` observation (`lua/ingame/yields.lua`, the seven top-bar accessors from Firaxis's `toppanel.lua` and nothing else), `run/yields.py::compute_yields_from_result` (the turn's last observation, metric names `science`/`culture`/`gold`/`faith`/`tourism` + the two balances, a missing value is absent never zero) wired in `run/composition.py`; `player.gold`/`player.faith` get their first producer. 7 unit + 2 contract tests through the 003 store's `metric_series` + 4 Lua-executing tests. UNVERIFIED LIVE that the InGame tuner state answers the top-bar accessors; client-gated — every run recorded before this commit has `yields = {}` and stays honestly gap-marked for trending.)* CRITICAL — the one constitution-MUST gap this pass found: `run/turn_cycle.py`'s `compute_yields` is the `_no_yields` placeholder in production, so `TurnCycle.yields` is `{}` on every turn of every run and the store's science / culture / gold / faith series (003, landed in `93a9b7e`) are honestly empty; the constitution requires "enough game state captured to reconstruct decisions, yields, and outcomes without replaying the game". Add a `player.yields` observation (InGame, capability `yields.read`, `lua/ingame/yields.lua`) reading exactly the top bar's own numbers — science, culture, faith (+ balance), gold net of maintenance (+ balance), tourism, the calls Firaxis's shipped `toppanel.lua` makes and nothing more (Principle I) — and a real `compute_yields` in `run/yields.py` that takes them from the turn's last observation, wired by `run/composition.py`. Human equivalent: reading the top bar. Surfaced by Lane A's finished 003 store.
- [X] T259 Point `contracts/match-store-port.md` at its implementer, per plan Complexity Tracking C2 and 003 (partial). *(Done 2026-09-21: the "Implemented by" line now names `MatchTrackingStore(MatchStore)` in `src/civsim_harness/store/contract.py` and the SQLite adapter, landed in `93a9b7e`.)* Original finding: the contract still says "deliverable 3 … in the interim" while `MatchTrackingStore(MatchStore)` in `src/civsim_harness/store/contract.py` (003, `93a9b7e`) now extends 002's port without changing it. A short "Implemented by" note with the path.
- [ ] T260 Confirm the camera's look-at plot from the InGame state so a screened-clean frame can pass the provenance gate, per FR-026, FR-050 and SC-013 (missing). MEDIUM, client-gated. Every capture on the Linux node — the one host whose R6 spike passed and whose frames now have a wire form (T252) — is still withheld because `target_revealed` cannot be confirmed: this build has no `GetCameraTargetPlot` / `GetMapLookAtPlot`. Headless lead from Firaxis's shipped InGame Lua (2026-09-21): `UI.GetMapLookAtWorldTarget()` (3 call sites) returns the camera's world-space target and `UI.GetPlotCoordFromWorld(x, y, z)` (6 call sites) maps it to a plot; `UI.GetMapZoom()` is already wired. Probe both live from InGame, then have `lua/ingame/camera.lua`'s read-back report the plot and `PlayersVisibility[pid]:IsRevealed(x, y)` for it. Human equivalent: looking at where the map is centred. Needs the client; ~20 min, $0. *(Audit 2026-09-22: **the word "every" in this task's premise is false.** It says "Every capture on the Linux node ... is still withheld because `target_revealed` cannot be confirmed." Measured read-only from a copy of the repo-root `civsim-match-store.db` on 2026-09-22: of **784** captures, **421** are withheld and **363** are `screened_clean`, with **301** actually shown to the agent. The provenance gate accounts for **307** of the 421 withholds (`withheld_reason = provenance_failure`); the other **114** are `non_player_ui`. So the gate is the largest single cause of withholding but not the only one, and it is emphatically not withholding everything. **Checkbox deliberately unchanged** — the look-at-plot confirmation is still unimplemented and still client-gated, and the live lane owns that closure; only the "every capture" premise is corrected, because sizing this work off it would overstate what it unblocks.)*
- [X] T263 Detect a game that is already over before the start-of-turn quicksave, and finish the run on it, per **Principle VII** (end or resume a run without silent data loss), **Principle III**, FR-005 and data-model.md §4 (missing). HIGH. *(Landed 2026-09-21: `lua/ingame/game_over.lua` + `run/game_over.py` + a pre-save call in `run/turn_cycle.py` and `Runner._finish_on_game_over`; `RunEventType.game_over_detected` added and `RunEvent.schema.json` regenerated additively; `stop_resolution` `victory`/`defeat` already existed. 13 Lua-executing tests under `lupa` + 16 integration tests through the real `Runner` and `SqliteMatchStore`. **unit/integration only; unverified live**.)* MEASURED 2026-09-21 18:50 EDT: Persia was defeated at game turn 59 and the next turn's quicksave could not land because the game was over, so the run paused with `SaveVerificationError("expected .Civ6Save not found… waited 10 s")` — a save error standing in for a defeat. `game.outcome_state` (T216) cannot catch this shape: it is read inside a decision step and consumed after a turn finishes, and the turn that discovers the ending is the turn that cannot start. The new read mirrors Firaxis's own `endgamemenu.lua` (`:1057` `IsAlive()`, `:1058` `Game.GetWinningTeam()`'s two return values, `:875` the winning-team comparison, `:926`/`:948-951` the victory and winner names) and names the victory type and winner **only once the game is over**, because only then does the end-game screen state them to a human (Principle I). A read that errors is not a game over. Human equivalent: the end-game screen the player is looking at. Evidence and the save-loader follow-up: `spikes/game-over-detection-2026-09-21.md`. `EndGameMenu` → a `game_over` screen id is left for the screens lane.

## Phase 14: Convergence (appended by the 2026-09-22 headless convergence pass; repo at `acd08bc`, suite 2148/18/0)

Three parallel read-only audits ran against different surfaces -- requirement coverage derived from code and tests, last night's five live findings, and cross-artifact consistency -- and returned the same shape. The report is `specs/002-civ-playing-harness/analyze-2026-09-22.md`, which leads with it rather than with a findings table: **the unreached mechanism** -- a mechanism that exists, is well built, is covered by passing tests, and is never reached in the case it was written for, with the suite green throughout. Seven instances in one day. Its sibling pattern, three instances: **a value whose name asserts something the value does not mean**. T261 and T262 were never allocated and stay unallocated.

- [X] T264 Fail closed in the content screening gate: any reject category that no technique addresses MUST withhold, not pass, per **Constitution Principle I (NON-NEGOTIABLE)**, FR-025, FR-030, SC-009 and SC-019 (contradicts). **CRITICAL.** `parity/screening.py:626-657` has three techniques; the declared-text one fires only when `tokens <= detected_text_tokens`, which defaults to `frozenset()` and is supplied by **no production caller anywhere in `src/`**. Five of six Linux reject categories -- `firetuner_window`, `developer_console`, `harness_owned_ui`, `linux_panel`, `linux_notification_toast` -- have no reachable technique at all, and FireTuner must be running for the harness to function. Measured: 287 model calls carried an image through this gate. The two tests that look like coverage inject the one input production structurally cannot produce. Image delivery on Linux stops until T265 lands; that is the correct outcome, not a regression. *(Landed 2026-09-22 `483017c`; VERIFIED against the code by the lane lead, not from the report: `parity/screening.py` now publishes `unaddressed_reject_categories()` and `_check_content` asks coverage before findings, withholding any category no technique addresses; a detector without `addressable_categories` is taken to cover nothing. `CaptureAttempt.detected_text_tokens` is now `frozenset[str] | None` defaulting to `None` -- "no text-evidence source ran" -- so the old permissive `frozenset()` default can no longer read as "checked and clean". Image delivery is closed on all three platforms; that is the intended outcome, not a regression.)*
- [X] T265 Populate `detected_text_tokens` at the one production call site from window-title enumeration, and thread the located `GameProcess` into the source gate, per **Principle I**, FR-025 and FR-030 (partial). **HIGH.** The source gate's pid comparison sits behind `if attempt.expected_process is not None` (`parity/screening.py:332-335`), which no production caller satisfies, so "this frame came from the game's own window" has never run on a real capture. Delete or rewrite `test_source_gate_skips_process_check_when_none_supplied` -- a test asserting the clean result for exactly the production shape encodes the defect as intended behaviour, which is worse than no test. *(2026-09-22: genuinely HALF DONE, left open deliberately. VERIFIED done: the source gate withholds on `expected_process is None` (`parity/screening.py:367`) instead of skipping, and `observe/capture.py:309` resolves the process itself via `host.locate_game_process()`, so the process-identity check runs in production for the first time; the test that asserted the clean result for exactly the production shape is gone. VERIFIED NOT done: `grep detected_text_tokens src/civsim_harness/run/decision_loop.py` returns nothing, so the production call site still supplies no text evidence. BLOCKED: `HostPlatform` exposes no desktop-wide window-title enumeration -- the Linux `_NET_CLIENT_LIST` walk is private inside `find_game_window` and returns only the game's own window -- so this needs `host/port.py` plus each adapter. **This is the single remaining blocker on image delivery.**)* *(Landed 2026-09-22 `17d0ebd` + `99130cd`. `HostPlatform.list_window_titles()` with a tagged `WindowTitleListing`; honest unavailable-with-reason stubs on Windows and macOS; the Linux `_NET_CLIENT_LIST` walk extracted as ONE generator (lazy deliberately -- an eager enumeration would raise `BadWindow` when the operator closes a window mid-run) with `find_game_window` calling it; two hunks in `run/decision_loop.py`. **The live lane's condition was met in the right order**: `tests/unit/test_linux_window_enumeration.py` was written and run against **pre-extraction** code first (8 passed), then unchanged after the extraction (8 passed), pinning root-space translation, both `None` paths, the empty-title case and laziness in both directions. **Principle I proven rather than asserted**, per the lane lead's added requirement: `tests/contract/test_window_title_boundary.py` has an AST arm (no module outside `host/` reads `.titles`, **and** positively that the loop must call `text_tokens()`, so "nobody reads titles" cannot be satisfied by nobody gathering evidence) and a runtime arm (a sentinel desktop title driven through the real `capture_for_step` on both the clean and withheld paths, every field of the `ScreenCapture` and its `RunEvent`s walked recursively plus a JSON-serialised check), each with a negative control that plants the sentinel and is flagged. `text_tokens()` returns `frozenset[str] | None` so a caller cannot hand the gate `frozenset()` meaning "checked and clean", and the `reason` string carries counts only. Image delivery is **reopened on Linux** -- `test_screened_clean_capture_bytes_reach_the_provider_request` now passes with its patching fixture **deleted** -- and closed on Windows/macOS by the stubs. Revert-confirmed: removing the call-site keyword reddens 5 tests including the UNFED GATE check. `expected_process` verified **not** stale and its allowlist entry kept.)*
- [X] T266 Publish the category-to-technique mapping as data and assert every category in every shipped profile is addressable by at least one technique, with a negative control, per **Principle I**, SC-009 and SC-020 (missing). **HIGH.** Extend to the Windows and macOS profiles: the dead technique is platform-independent, so their recorded hygiene passes are unproven too. Derive from the catalog; do not hard-code a duplicate list in the test. *(Landed 2026-09-22 `a395325`; VERIFIED: `tests/unit/test_screening_technique_coverage.py` exists and `techniques_for_category()` is published in `parity/screening.py`, with `DefaultContentDetector.detect` partitioning on that same function so declared and exercised coverage cannot drift. Parametrized over every shipped profile including Windows and macOS, with a synthetic-catalog negative control and a revert confirmation that reddened three named tests.)*
- [X] T267 Re-declare the synthetic-click prompt path honestly and make the catalog validator fire on the real condition rather than the label, per **Constitution Principle II (MUST)** and FR-028 (contradicts). **CRITICAL.** `catalogs/capabilities.yaml:199-204` ships `prompts.orders` as `path: firetuner` with `firetuner_gap: null`, while `run/composition.py:1099-1101` records a **measured** Firetuner gap and `capability/executor.py:396-401` issues real synthetic `mouse_move`/`mouse_click` through `HostPlatform.send_input`. Principle II permits a bespoke path only to fill a **documented** gap; the gap is documented in a Python comment and nowhere an auditor would look -- `host_click` appears in zero 002 markdown artifacts. If no gap can be evidenced the path is not permitted and is blocked, not softened. The load-time validator (`models/catalog.py:186-190`) fires only on `path: bespoke`, so it validates the declaration against itself and cannot see a mislabel; three layers (`plan.md:300-301`, `validation-results.md:34`, the validator) all read that same field. Correct `plan.md:296-302`: C1 is not discharged, it moved. *(DONE 2026-09-22, `5f1e9bd`. The gap **is** evidenced, so the path is permitted and was relabelled rather than blocked: `prompts.orders` is now `path: bespoke` carrying the 2026-09-21 measurement verbatim -- no Lua API reachable from `InGame` fires a control's registered callback, `control:CallCallback(Mouse.eLClick)` fires nothing, and `UI.RespondToPrompt` exists in none of Firaxis' 645 shipped Lua files. **Relabelled, not split**: every answerable prompt family in `CivSim_Screens_RespondToPrompt` can return a host-click request, so a split would leave no declaration on the pure-Firetuner side and would be a fiction; Firetuner-first is preserved inside the capability instead, where a direct call is primary and the click is the fallback with `path`/`path_reason` on every result. The validator hole is closed by `tests/contract/test_synthetic_input_declaration.py`, which fires on the real condition -- it pins every in-harness `HostPlatform.send_input` call site as data, re-derives the set from source with `ast` so a new one cannot ship undeclared, and requires the capability it serves to be `bespoke` with a non-empty gap. Confirmed by reverting the entry to `path: firetuner`/`null` and observing the check fail naming the capability, then restoring (checksum verified identical). `plan.md` and `validation-results.md` corrected; C1 annotated LIVE-and-moved; the gate re-reads PASS because the declaration matches the code, not because the principle was relaxed. **One item referred up:** `saves/load_game.py` presses Escape through `send_input` to clear the leader-intro screen and serves no catalog capability at all (load is lifecycle, not an agent action), so Principle II's capability-path rule has nothing to bind -- pinned as data with its reason, needs a hypervisor ruling.)*
- [X] T268 Give FR-011 a check of any kind, per FR-011 (missing). **HIGH.** "It MUST NOT record an action as applied without verification" holds today only by construction -- empirically sound across 619 recorded decisions (85 applied, every one with `verification.result == True`) -- and nothing observes that it keeps holding. `ActionExecution(outcome=APPLIED, verification={})` validates cleanly, because `verification` is `Field(default_factory=dict)` (`models/decision.py:60`) and the one existing validator checks the bookkeeping rule instead. A dispatch-answer fast path would pass all 2148 tests while re-shipping the "fabricated V2 verification" defect named as defect #1 at `tests/contract/test_reachability.py:5`. Needs the validator **and** a sole-producer AST scan for `ExecutionOutcome.APPLIED` with a negative control -- the structural partition, not a spot check. This is 002's analogue of 003's FR-006 hole. **LANDED: FR-011 is now enforced in two places that can each be shown to fail.** (1) `models/decision.py` gained `_applied_carries_its_confirming_verification`: `outcome == applied` requires a non-empty `verification` whose `result` is `True`, so `ActionExecution(outcome=APPLIED, verification={})` -- the exact dispatch-answer fast path -- is unconstructible, and the class docstring no longer claims the rule cannot be enforced here. (2) `tests/unit/test_verification.py` gained the sole-producer AST scan (T231's idiom, keyed on a call passing `outcome=<ExecutionOutcome.APPLIED>`, qualified so `preparation.py`'s unrelated `LeaderSelectionWriteStatus.APPLIED` cannot register; comparisons are reads, not producers): exactly one module, `act/verify.py`, may derive `applied`. Revert-confirmed both halves: a `{ok=true}` fast path added to `act/dispatch.py` turned the scan red with `Producers found: ['act/dispatch.py', 'act/verify.py']`, and short-circuiting the validator turned both `test_an_applied_execution_cannot_be_recorded_*` tests red with DID NOT RAISE; both green again on restore. Nothing in production changed -- the corpus already satisfied it -- but eleven test fixtures recorded `applied` over an empty verification and now carry the predicate verdict, and the two published schemas were re-exported for the new docstring.
- [ ] T269 **Principle VII's resilience half is unimplementable, not incomplete.** There is **no method to start, stop or restart the Civilization VI client on any platform**, and the word "relaunch" does not appear anywhere in `src/`. Per **Constitution Principle VII**, FR-045, FR-047 and SC-010 (missing). **HIGH. DEFERRED 2026-09-22 by hypervisor ruling -- deliberately not started, and the reason is recorded here so the gap is not mistaken for polish.** An auditor reading the spec must not be able to conclude that crash recovery merely needs finishing: `host/port.py:224-330` declares eight methods and none of them creates or ends a process, so the post-defeat relaunch has nothing to be built *from* -- it is absent from the port contract, not unimplemented on Linux. Consequences today: a refused `Network.LoadGame` at the post-defeat menu raises an undifferentiated `HarnessError` (`saves/load_game.py:416`) whose own comment already guesses the right cause, `_is_front_end()` (`load_game.py:203-207`) cannot tell a menu that can load from one that refuses, and `RecoveryEngine` therefore burns its entire `recovery_attempt_limit` re-issuing a call that can never succeed before failing the run -- correctly recorded, which is exactly why nothing is red. Every defeat becomes a ~17-minute human intervention, against US1's unattended premise. **Why deferred rather than half-landed:** it is a Protocol widening that forces all three adapters to answer, plus a typed discriminator on the refusal, a new recovery escalation, and a third branch in FR-045's currently-binary "resume or stop in a recorded failed state" disjunction; the Linux implementation belongs to the live lane, which cannot take it today. Landing half would leave the port widened and two adapters stubbed, which is worse than the honest gap. **When it is taken**, the escalation asserts the data-safe-kill precondition (client at the empty menu, no game loaded) before the kill and records both the kill and the relaunch on the run timeline per FR-047. The two measured facts that make it safe and cheap -- a kill at the empty menu is data-safe, and a fresh launch reaches a loadable menu in ~38 s -- exist only in `.specify/memory/hypervisor-log.md` and belong in `research.md`; today a reader of `spec.md` would correctly conclude the harness is not supposed to relaunch anything, because `grep -n relaunch spec.md` returns nothing.
- [X] T270 Make an orphaned run lock whose run never reached a store detectable and cleanable, per **Principle VII** and FR-006 (missing). **HIGH.** Observed today: a run reached FINISHED, the process exited, the lock file remained, and the run was **never written to any store** -- so `repair_orphans` had no row to pause and the lock silently blocked the next run. The asymmetry is the finding: `scan_orphans`, the open-time write-mode sweep (`store/sqlite_adapter.py:137-139`) and `civsim store repair` all start from a run **row** and ask about its lock; none starts from a **lock** and asks whether it has a run. Prefer reporting over deleting where a finished-but-unpersisted run cannot be safely distinguished from one about to write its first row -- `DEFAULT_ORPHAN_GRACE_SECONDS` is the precedent, and removing a live run's lock is a worse failure than leaving a stale one. Assert the case with a negative control rather than patching the symptom. *(DONE 2026-09-22: `run/orphans.py` gained the mirror of its own scan -- `scan_stray_locks` walks the lock **directory** and asks of each file whether a run stands behind it, `clean_stray_locks` removes the ones that provably do not, and `sweep_stray_locks` reports on a write-mode store open without removing anything. `StrayLockDisposition` is where "cleanable" is decided and argued: `TERMINAL_RUN` (the store records the run `finished`/`failed`) and `DEAD_HOLDER` (the recorded client process is gone, or the file is malformed) are removed, because each settles the question on its own; `UNRECORDED` -- no row for this run id, live PID, which is exactly today's wreck -- is **reported and kept**, because `create_run` writes the row before `acquire` takes the lock, so "no row here" may mean "a row in another store", and the live PID is the long-lived *game client*, not a driver. `civsim store repair --clean-unrecorded-locks` is the operator saying this is the only store; the plain command names the kept locks and that flag. No lock younger than `DEFAULT_ORPHAN_GRACE_SECONDS` is ever a finding, dated by the lock's own `acquired_at` and falling back to the file mtime so a malformed lock is still covered; `clean_stray_locks` re-inspects each file inside the removal and skips one re-acquired since the scan, as `pause_orphan` re-reads its run. The evidence lands on the harness log at `WARNING` with path, PID and liveness plus `SqliteMatchStore.stray_locks_on_open` -- not on a `RunEvent`, because a stray lock by definition has no run row to carry one, and inventing one would assert a run the harness never saw. Negative control first: with `run-stray.lock.json` on disk, `scan_orphans` returned `[]` at `grace_seconds=0` -- the lock that blocks the next run is invisible to every run-side path -- and `tests/integration/test_stray_lock_repair.py::test_the_run_side_scan_cannot_see_a_lock_whose_run_never_reached_the_store` keeps both halves of that asserted side by side. `LOCK_FILE_SUFFIX` mirrors a private naming convention in `identity_lock.py`, whose drift would silently blind the glob rather than raise, so `test_the_lock_file_naming_this_module_mirrors_is_the_one_the_lock_writes` pins it. Not taken: releasing the lock at the end of a run, which is `run/composition.py`'s and the live lane's fixture guard.)*
- [X] T271 Write `shown_to_agent` unshown and upgrade it after `complete()` returns, per FR-015 and SC-019 (contradicts). **MEDIUM.** The flag is written at `run/decision_loop.py:745` and the provider is called at `:747`, so it records an intention while `:357` claims it "reports what happened, never an intention". Thirteen live captures carry `shown_to_agent=True` for decision steps with no `model_calls` row and no `decision_steps` row at all. Cover `complete` raising after the capture write -- the case with no test today and the one that produced all thirteen. Do not retro-edit the thirteen rows; they are evidence. *(Landed 2026-09-22 in `51c2b1a`: the capture is now persisted **un-shown** before `ctx.provider.complete(request)` -- still durable ahead of any interruption (FR-051, D5), but saying only what is true at that line -- and upgraded to shown only after `complete()` returns. `MatchStore.write_capture` stays write-once with exactly one permitted second write: `shown_to_agent` rising false -> true with every other field identical (`store/sqlite_adapter.py::_is_shown_upgrade`, one-directional; a shown record can never be lowered and a "shown" write that altered anything else is still refused as reuse). `_FreshObservation`'s docstring and `store/port.py`'s `write_capture` contract both now describe the two-phase write instead of contradicting it. New test: `tests/integration/test_image_attachment.py::test_a_dispatch_that_never_returns_leaves_the_capture_recorded_not_shown` -- a provider that accepts the request (image asserted on its own call log) and then raises; the record must say not shown, the blob must survive, and there must be no model call and no turn cycle, so the flag now agrees with the missing evidence instead of contradicting it. `test_screened_clean_capture_bytes_reach_the_provider_request` updated to four writes over three captures. Revert-confirmed: restoring the pre-fix single shown write reds the new test at `assert all(capture.shown_to_agent is False ...)` and reds `test_screened_clean_capture_bytes_reach_the_provider_request` too; restoring the fix greens both. The thirteen live rows are untouched.)*
- [X] T272 Emit the four named metrics that have no producer, or amend FR-018 for any with no human-readable empire-level source, per 003 FR-018, **Principle III** and **Principle I** (missing). **MEDIUM, cross-spec.** `run/yields.py:34-43` publishes seven keys and all 76 populated turn cycles carry exactly those; `production`, `food`, `city_count` and `unit_count` appear in zero records, so their series can never be non-empty. Prefer emitting -- a requirement amended to match whatever the code happens to do stops constraining anything -- but do not invent a number: if a metric has no source a human could read from the standard UI, emitting it breaches Principle I, and FR-018 is amended **for that metric** with the reason recorded in the spec. Pin the emitted key set as data on both sides. Corrects 003's B6, which rated a missing **producer** as a missing **fixture**; record that correction where the next auditor will find it. Root cause of the mis-rating: `tests/unit/test_store_trends.py:32` asserts in prose that `compute_yields` is a no-op, which is false and propagated into two audit reports. *(Landed 2026-09-22: **the brief's premise was wrong for two of the four, and that is the finding.** `city_count` and `unit_count` are produced and populated -- derived per turn by `store/trends.py::turn_metrics_from` from the last observation's `cities.state` / `units.state`, never from `TurnCycle.yields`, which is why a grep of `run/yields.py` made them look absent. MEASURED on a read-only copy of the live store: all 93 recorded last-step observations carry both lists, and 77 of 93 turn cycles carry all seven yields. So zero new metrics were emitted and none needed to be. `production` and `food` are the only genuine gap, and they have no empire-level source: Civilization VI shows food and production **per city** and publishes no empire-wide figure for either anywhere in the standard UI, so emitting one through `player.yields` -- the observation the agent itself reads -- would put a harness-computed aggregate in the agent's context under a `parity_basis` claiming a top-bar reading that does not exist (**Principle I**). FR-018 was amended for exactly those two, with that reason recorded against the requirement itself in `specs/003-match-tracking-store/spec.md`, together with the live-lane ask that would unblock a *per-city* series instead (a new Lua accessor for per-city food/production; `cities.state` carries no yields and no file under `lua/` reads any yield but the top bar's -- not written, `lua/` is the live lane's). `tourism`, `gold_balance` and `faith_balance` were **added** to FR-018's named set, since the harness has produced them since 2026-09-21 and the requirement did not say so. Pinned as data on both sides: `store/trends.py::FR018_METRIC_NAMES` (published) against `run/yields.py::RECORDED_YIELD_METRICS` + `DERIVED_METRICS` (produced), asserted equal by `tests/unit/test_yields.py::test_the_emitted_key_set_is_pinned_and_matches_the_published_fr_018_set`. Revert-confirmed: re-publishing `production` and `food` in `FR018_METRIC_NAMES` with no producer reds that test naming both as extra items; removing them greens it. The 003 B6 rating correction is recorded in `specs/003-match-tracking-store/analyze-2026-09-22.md` as a signed section directly above the "No CRITICAL finding" line, leaving B6 itself unedited -- **LOW / missing fixture** corrected to **MEDIUM / missing producer**, with the live-store measurements and the provenance of the stale premise. Root cause fixed: `tests/unit/test_store_trends.py`'s docstring no longer claims `compute_yields` is a no-op and now records that the claim expired on 2026-09-21 and was read back out of this file into two audit reports on 2026-09-22.)*
- [X] T273 Draw FR-053's boundary around the `civsim store` commands and give it a check, per FR-053 and **Principle VI** (partial). **MEDIUM.** FR-053's only structural guard is the closed `RunStatusView`, which covers `run status` alone, while `operator/cli.py:115` mounts `store_app` on the same console script shipping `store runs`, `store model-calls` and `store coverage` (which renders metrics). `operator/cli.py:11-13` claims "the CLI never presents turn records, decisions, metrics, or captures" -- currently false. Either scope `civsim store` out as deliverable 3's record tooling in `contracts/operator-surface.md` and correct the docstring, or enumerate the permitted output surface as data. *(Landed 2026-09-22: `civsim store ...` confirmed genuinely out of FR-053's scope -- deliverable 3's own record-inspection tooling under FR-011/FR-015/FR-017, mounted on the same console script for convenience, not because it shares this contract's scope. `operator/cli.py:11-13`'s docstring corrected to say so; `contracts/operator-surface.md`'s "FR-053 bound" section gained a matching "Out of this bound's scope, deliberately: `civsim store ...`" paragraph. New check: `tests/unit/test_operator_api.py::test_operator_control_surface_command_roster_is_closed` pins every non-`store` command Typer registers on `civsim` (26 commands) as a closed set, so a new `civsim <verb>` anywhere outside `store` requires deliberately updating this test. Revert-confirmed: a decoy top-level command added to `operator/cli.py` reds this test naming the new verb as an extra item; removing it goes green again.)*
- [X] T274 Add `select_screened_images` to the reachability roster, per FR-016 and FR-024 (missing). **MEDIUM.** It is the sole enforcer of the catalog VIEW-declaration gate and has one production caller; its own history is the argument -- T238 records it once had zero callers in `src/` and zero references in `tests/` while production shipped `images=[]`, filed CRITICAL, and the fix landed the caller without the guard that keeps it there. **LANDED: `Target("select_screened_images", "called", "agent/context.py")` is on the roster.** Reached today by its one production caller, `run/decision_loop.py`. Demonstrated to fire rather than assumed to: driving the file's own `_is_reached`/`_reachability_problems` over the real harness scan with `run/decision_loop.py` alone removed -- T238's exact original state -- flips it to `False` and yields `UNREACHED: called target 'select_screened_images' (defined in agent/context.py) has no production caller outside its defining module`. The file's nine synthetic-tree negative controls already prove this checker can fail, `test_negative_control_an_uncalled_port_method_is_flagged` covering precisely this family; all nineteen tests in the file pass with the entry added.
- [X] T275 Resolve guidance's V6 "runtime assertion", which cannot fire, per FR-021 (partial). **LOW.** `config/guidance.py:108` computes `content_hash` from `content` and `:109` registers it, so the mismatch guard can only fire on a SHA-256 collision; `grep GuidanceRegistry tests/` returns nothing. Either state plainly that content-addressing plus the recorded hash is the enforcement, or move the assertion where it can fire. *(Landed 2026-09-22: chose the smaller, honest option -- dropped `GuidanceRegistry.register`'s raise entirely rather than relocate it. It is now a plain same-process content-by-hash cache; the module docstring and class docstring state plainly that content-addressing itself (the hash is a hash *of* the content, always recomputed at load time) plus the store's recorded `content_hash` per run is what enforces V6's run-independence guarantee, not this in-process check. `data-model.md:156` still calls it a "runtime assertion" -- out of this task's file ownership (only `contracts/**`, `operator/**`, `config/**`, `tests/**` were in scope), left for whoever owns `data-model.md`. New tests in `tests/unit/test_guidance.py` (previously no test file existed) pin: `register` never raises even on a genuine hash/content mismatch; `load_guidance`'s one call path can never produce a mismatch in the first place (two different files hash different, barring an actual SHA-256 collision); and reloading identical content is a cache hit. Revert-confirmed: temporarily restoring the old raise reds `test_registering_the_same_hash_with_different_content_does_not_raise`; removing it again goes green.)*
- [X] T276 Refuse an action argument outside the **current view's** declared range at availability time, naming the view and the range, per **Principle I**, FR-026 and FR-050 (missing). **HIGH.** `camera.zoom` accepted a target of 0.05 while `views.world` declares [0.2, 1.0]; 0.05 is a legal **strategic** zoom reached while still in world mode, and on the standard interface scrolling past a threshold switches view, so mode and zoom move together and a human cannot sit in world mode at 0.05. The harness occupied a camera state no human could occupy; the withheld frames were luck, not detection. What the engine then did with the request is a separate question and is not established (the reset measurement first cited here was withdrawn by the live lane on 2026-09-22); the violation stands on the declarations alone. Note the production wiring: `run/decision_loop.py:834-843` **replaces** the dispatch outcome with `act/camera.py::validate_camera_action`, which binds `{"target": target}` only and therefore cannot know which view the camera is in -- a check landed only in `availability.py` would be dead code for exactly the action that matters. *(Landed 2026-09-22: the check is `act/availability.py::evaluate_argument_availability` -- the current view read from the observation's `camera.read_state` mode, the range read from that view's own `camera_requirements.zoom_range`, no bound written down in code or test. Wired on the production path, not beside it: `act/camera.py::validate_camera_action` now takes a **required** keyword-only `observation` and rejects with `OUT_OF_PARITY_CAMERA` naming the view and the range, and `run/decision_loop.py` passes the step's observation. Root cause recorded in both docstrings: `validate_camera_action` bound `{"target": target}` alone, so it could only ever check `camera.zoom`'s **union** predicate `target >= 0.05 and target <= 1.0` -- the union of `views.world` [0.2, 1.0] and `views.strategic` [0.05, 0.3] -- and any target legal in either view was accepted in both. Revert-confirmed twice: removing the `decision_loop.py` line reds `tests/integration/test_decision_loop.py::test_an_out_of_parity_camera_request_is_recorded_as_exactly_that` at the call site, and disabling the refusal reds `tests/unit/test_camera_validation.py::test_a_strategic_zoom_asked_for_in_world_mode_is_refused_naming_the_view_and_range`. The strategic view's own declared range stays reachable -- clamping to the world range was refused as a fix that breaks a second thing. The cross-view half is untouched and open as T277.)*
- [ ] T277 Decide what happens when a camera target is legal for a **different** view -- refuse it, or perform the mode change first -- per **Principle I**, FR-026 (missing). **MEDIUM, client-gated.** Depends on what a real scroll-wheel zoom does to mode and zoom together on this build, which is a measurement the live lane owns. The seam is parameterized by T276 with the undecided part named in the code; do not guess it.
- [ ] T278 Make `turn_reached: N` unreadable as a game turn, per FR-005 and data-model.md (contradicts). **MEDIUM.** The field counts the **harness's own turn cycles from 1**, not the game's turn number. It is already in this project's gotcha list and still bit twice in one day; the second time an agent read `turn_reached@1` firing as proof a real game turn had been played on the live client and published that inference to the coordination board, where it had to be chased and corrected. Treat it as a **naming defect, not a documentation gap**: a fact that must be remembered in order to avoid a wrong conclusion will keep producing wrong conclusions, and the gotcha list is exactly where this one already lived while failing twice. Rename it to name the harness cycle explicitly, or give it a type that cannot be compared against a game turn, and make the stop condition's own reporting say which counter it means. `src/civsim_harness/run/**` belongs to the live lane, so the implementation may need assigning rather than taking.
- [X] T279 Reconcile the write-before-advance claim with what ships, per **Constitution Principle III**, FR-013 and FR-008 (contradicts). **HIGH.** `plan.md:271-273`, `data-model.md:264-265`, `contracts/match-store-port.md:66` and `store/guard.py:26-34` all state the end-turn action is issued strictly after a durable write. For `ended_by_agent` and `end_turn_unconfirmed` -- the two commonest outcomes -- the real `Game.EndTurn()` dispatch already happened inside the decision loop, and `run/turn_cycle.py:37-44` and `:1002-1008` say so; only `ended_on_no_progress` has the asserted ordering. The guard's own test passes a **fake** `end_turn` callable and proves the guard orders its own callable, blind to the dispatch that never goes through it -- the cleanest instance of the day's pattern, with the false claim written two paragraphs above the code contradicting it. The shipped design is defensible; the prose is what is wrong. Narrow "nothing else can call end-turn" to "nothing else can advance", then decide whether FR-013 as written is the requirement, since FR-013 and FR-008 cannot both be literally true today. *(Landed 2026-09-22 `7d205ec`; verified by the lane lead against `git show HEAD:` rather than the report. All four statements rewritten with dated correction notes so the old wording stays visible; `store/guard.py` and `run/turn_cycle.py` narrowed from "nothing else can **call end-turn**" to **"it holds the *advance*, not the client-side end turn"**, and `guard.py`'s docstring now warns explicitly that its own tests are not coverage of the in-loop dispatch. The agent also found and fixed **the same falsehood nine lines above the one it was pointed at** (`data-model.md:255`, `persisted_at` "must precede the end-turn action") -- correcting the instance you are given and leaving its neighbour is this pass's defect in miniature. New test `test_a_failed_write_leaves_the_runs_own_bookkeeping_un_advanced` deliberately does **not** fake coverage of the in-loop dispatch; it asserts what is true, that a store whose write raises yields no token and never invokes the advance callable. Revert-confirmed: wrapping the write in a swallowing `try/except` reddened it **and** the sibling `test_a_failed_write_halts_before_end_turn_is_ever_called`. FR-013 was amended separately under the hypervisor's ruling. **Lane-lead note on how this was nearly mis-recorded:** my first check grepped `plan.md` for an annotation phrase I had invented and returned 0, which would have left a completed task open. `git merge-base --is-ancestor` plus the commit's own `--stat` settled it. A negative search is evidence only if the search could have matched -- third instance today, and the first where the invented string was mine rather than a truncation.)*
- [ ] T280 Bind and persist the `EnableDebugMenu` preflight reading, per **Constitution Principle I** (partial). **HIGH.** `run/composition.py:746` calls `debug_menu_preflight(...)` and **discards the return** while both adjacent gates bind theirs; `run/preparation.py:499-500` says the function exists to "return it for the caller to record on the run". Three documents claim it is recorded. Ask the record "was this run made with `EnableDebugMenu 1`?" and there is no answer for any run ever made -- on a host that shipped with the debug menu **on**, which is what the originating spike was about. Persist it (filtered from agent context like `game_build`), or delete the three claims. `EnableDebugMenu` appears zero times in `tasks.md` today.
- [X] T281 Name a client death during the pre-save probe or sweep, per FR-005, FR-044, SC-010 and invariant I10 (missing). **HIGH.** `ConnectionResetError` is an `OSError`, not a `HarnessError`, and `nexus/client.py`'s `_send_raw` (`:913-919`) and `_read_frame` read (`:928`) do not catch it -- so it bypasses `run/turn_cycle.py:779-833`'s purpose-built pre-save fault handling entirely, reaches `Runner._record_unexpected_failure` (`:1105-1120`), which performs **no lifecycle transition and writes no `stop_resolution`**, and strands the run in `playing` forever. The elaborate, correct handling that would have named it has never once run in the situation it was written for, because the exception never arrives in the right type. Wrapping `OSError` into `NexusError` with a `REASON_CONNECTION_RESET` sibling makes every downstream handler work as already written -- the highest-value few lines in the resilience layer. `StopResolution`'s own five members are fully and correctly documented; the driver-side `stop_reason` in `tests/live/goal_run.py` is a separate three-member vocabulary with no failure member at all, so every exit through FAILED or PAUSED reports `None` -- that half is the live lane's. *(Landed 2026-09-22: the transport, and only the transport, is wrapped. `nexus/client.py`'s `_send_raw` (`write`/`drain`) and `_read_frame` (`read`) now catch `OSError` and re-raise `NexusError` with a new `REASON_CONNECTION_RESET` sibling to `REASON_CONNECTION_CLOSED`, keeping the original as `__cause__`; `TimeoutError` is re-raised first, deliberately, because it is an `OSError` subclass (PEP 3151) and a busy client is not a dead one. **No downstream handling logic was added anywhere** -- that was the whole premise and it held. `_is_reconnect_refusal` had to learn the new reason, or naming the failure would have silently disabled T246's post-close-tail retry, which matched the bare `OSError` before. `execute_command`'s `_send_raw` call stays outside its `try`, which catches only `TimeoutError` to discard an in-flight nonce -- a send that failed put nothing on the wire. **Production-path revert-confirmed**, not merely unit-confirmed: `tests/integration/test_transport_fault_recorded.py::test_a_client_death_on_the_wire_pauses_the_run_in_a_recorded_state` drives a real `NexusClient` over a real TCP **reset** (new `tests/fakes/aborting_tuner.py` -- `SO_LINGER {1,0}`; `FakeNexusServer.drop_connection_at` closes politely and only ever produced the already-handled clean EOF) through the real `observe/reader.py`, `run/decision_loop.py`, `run/turn_cycle.py` pre-save probe and `run/runner.py`. With the two `except OSError` arms removed it fails at `assert status.lifecycle_state is LifecycleState.PAUSED` with the run still `PLAYING`, `last_error.type == "ConnectionResetError"` and the captured log line "run/runner: unhandled failure while recording why a run stopped playing" -- the live 2026-09-21 `stop_reason: None` reproduced exactly; with the arms restored the run reaches a recorded `paused` lifecycle_transition carrying `error_type: NexusError` and `connection_reset`. Seven more in `tests/unit/test_nexus_transport_faults.py` cover all three dead-peer `OSError` shapes on both the read and write paths, the EOF/reset distinction, the `TimeoutError` trap and the reconnect-tail guard. **One correction to the brief's chain:** the named `NexusError` does **not** reach `run/turn_cycle.py:793`'s `MidTurnObservationFailure` handler -- nothing between `capability/executor.py` and `_observe_or_wrap` converts a `NexusError` into an `ObservationAssemblyError`, and `_observe_or_wrap` catches only the latter, so it propagates to `Runner._play_run`'s `except HarnessError` and `_handle_run_failure`'s unclassified-to-`paused` fallback. That is a legal recorded state and closes I10's hole; routing it to the pre-save handler instead would mean new downstream logic and is left unbuilt deliberately, noted as a follow-up for whoever owns `run/decision_loop.py`.)*
- [X] T282 Replace the `/compare` render-budget assertion's load-sensitive wall clock with a work-done assertion, per SC-008 (partial). **MEDIUM.** `tests/integration/test_scale.py:329-341` asserts `elapsed < 2.0`; measured unloaded it runs at **1.44 s**, a 1.39x margin, and it is the only assertion in the module under 1.6x while the rest have 7x or more. Do **not** hide it behind a benchmark marker: SC-008 is a shipped success criterion and excluding its only `/compare` HTML check from the default run is 003's SC-005 finding happening again by another route. The module already contains the right pattern at `:344-372`, whose docstring argues the case verbatim -- count the work done (`/compare` over five runs pays exactly five `yields_by_turn` reads and no more) and keep a wall-clock tripwire with a separately documented, generous ceiling recording the measured figure and date. *(Landed 2026-09-22 `125a74f` and follow-up: `tests/integration/test_scale.py::test_comparing_five_long_runs_pays_one_series_read_per_run` counts the work through a `_CountingStore` pass-through swapped onto `app.state.store` plus a counting wrapper over `catalog.yields_by_turn` -- five compared runs pay exactly 5 series reads and exactly 1600 `get_turn_cycle` reads (5 x 320 turns), with `list_save_points == 5` pinning the cheap `highest_recorded_turn` path, because an inverted save-list path would silently absorb 320 extra reads per run. Revert-confirmed by a **deliberate regression**, not by inspection: a `project_run` that walks the series a second time -- T075's bug one layer out -- scores 10 series reads and 3200 turn reads and reds the test; unpatched it greens. No benchmark marker; nothing excluded from the default run. **The measurement found more than the task assumed, and the finding is the conditions.** T282's premise was that these cases pass "on a quiet box" and that the only load was this project's own parallel suites. Checked directly with `/proc/loadavg` while measuring: 8.48 across 12 cores, `Civ6` at 92% of a core -- the LIVE lane's own game, the machine's normal working state, not an anomaly -- plus a foreign `pytest` and a `qemu`, with the 15-minute average holding at 7.69. So no measurement taken today was unloaded, including the 1.44 s in this task's own text and the 1.42 s the first version of the fix recorded. Twelve samples on the same box found 1.87 s, and interleaved, 3.71 s: **five samples was too few to contain the tail, so a number that looked derived was not.** The full distribution, the load it was taken under, and that warning to anyone re-deriving it are recorded with the constants. Both `/compare` cases moved off `BUDGET_SECONDS` -- the JSON sibling too, since fixing only the HTML one would have left the JSON one as the module's new thinnest margin (1.59x) and merely relocated the defect -- onto `COMPARE_HTML_TRIPWIRE_SECONDS` / `COMPARE_JSON_TRIPWIRE_SECONDS`, each 2x that case's worst observed sample (7.42 s / 7.08 s). 2x deliberately does **not** catch a 2x-3x regression: the measured spread on one unchanged binary is 1.08-3.71 s, which is wider than the cheapest real regression, so that is the read count's job and putting it in the clock too would only rebuild the flake. What the clock still catches is the module docstring's unbounded loop -- a composition quadratic in turns is ~102k reads against 1600, i.e. minutes. `BUDGET_SECONDS = 2.0` is untouched and verbatim for all twelve cases that clear it 7x over; the two exceptions are one named `ceiling` argument each, at their call sites. `tests/integration/test_scale.py`: 35 passed, ruff clean.)*
- [X] T283 Give the `debug_overlay` corner heuristic a negative control before tuning it, per **Principle I**, FR-025 and SC-009 (missing). **MEDIUM.** `parity/screening.py:549-575` trips when any one of four corner patches is 1.8x busier than the whole frame and clears a variance floor of 200 -- and a real Civ VI frame is smooth terrain in the middle with dense HUD chrome pinned in every corner, which is by construction that condition. The detector has a **positive control and no negative control**: every `SCREENED_CLEAN` assertion in `tests/unit/test_image_screening.py` runs against a uniformly flat frame, and the one positive fixture is flat background plus uniform random noise in one corner -- the best possible case for it. Nobody can currently demonstrate the gate passes ordinary gameplay, which is why nobody knows how much of the missing image coverage it accounts for. Write the gameplay-like fixture **first**, then narrow -- excluding corners a view declares as its own HUD, or differencing a corner against the previous capture's same corner (a fixed HUD is constant frame to frame; an FPS counter's digits are not). Raising the floor until live frames pass, dropping the category from a profile, or deleting the technique are **loosening, not narrowing**, and are forbidden. Comment the three thresholds with what they were measured against, or say plainly that they were not. *(Landed 2026-09-22 `5fd35c6`; the strict xfail now PASSES and was deleted because the test is green, not relaxed. Narrowing chosen: a **per-view HUD-corner declaration**. The three constants are untouched, no category dropped, the technique intact -- what changed is the *reference*: a corner a view declares as its own HUD is compared against the **busiest of the other declared HUD corners**, while an undeclared corner keeps the whole-frame comparison exactly as before. Rationale: "busier than the whole frame" is true of every corner of every Civ VI frame by construction, so on a HUD corner the old test carried zero information -- it fired on 5 of 188 clean live frames at ratios 1.800-1.802, decided by rounding. Peer-**max** was chosen over peer-mean after measuring, because peer-mean left the clean frame at 1.79 against a 1.8 threshold, i.e. it would have replaced one rounding-decided test with another. Declared only where there is a measurement (`views.world`, and `views.strategic` as the same in-game HUD, said so in the file); `city_screen`/`diplomacy`/`congress` declare nothing and keep the strict rule, which is the fail-closed direction. Measured offline on the synthetic fixture: clean worst peer-ratio 1.53, full-corner overlay 1.84/2.82/1.84/1.84 -- **a narrow band, 15% clear on one side and 2% on the other, on one synthetic frame** -- and a smaller 220x60 overlay over the minimap reaches only 1.71 and is **NOT caught**. All of that is in the docstring rather than rounded off; no empirical derivation is claimed, because the 421 withheld captures were stored `blob_ref=NULL`.)*
- [X] T284 Document `dispatch_result` in `data-model.md` as what it guarantees, per FR-008 and data-model.md section 9 (missing). **LOW.** The ActionExecution field table documents four fields; the model has five. The correction that matters: `{"ok": true}` means **the call returned**, not that it took effect -- measured, `UI.SetMapZoom(0.5, ...)` returned cleanly while `UI.GetMapZoom()` still read 0.0499997 before and after in one tuner session. Record it as the executor's own answer, never authoritative, with `outcome` still derived from verification; and state in the Validation block that the dispatch answer and the verification result are different facts about a step and neither substitutes for the other. This field is what distinguishes "the Lua refused the order" from "the order was sent and the board did not change" -- the distinction whose absence hid the `cities.set_production` bug through 80 rejected attempts. *(Landed 2026-09-22: verified against `git show HEAD:specs/002-civ-playing-harness/data-model.md` (not the working tree, which carries an unrelated uncommitted T279 edit from another lane) -- already done, correctly, by `a57fc05`. The `dispatch_result` row is present in the ActionExecution field table with exactly this content: the dispatched action's own Lua answer, recorded but never authoritative, `outcome` still derived from the verification predicate, null when rejected before execution or when the capability returned nothing. The Validation block below it carries the required sentence: dispatch answer and verification result are different facts about a step, neither substitutes for the other. The withdrawn `UI.SetMapZoom`/`UI.GetMapZoom` measurement is **not** cited -- `a57fc05` replaced it with the structural argument only, per the note that call was later shown to have worked (the tuner returns the pre-call value when the readback is inlined with the write). No edit needed; tally verified, not assumed.)*
- [X] T285 Publish the screening gates' inputs as data and assert each is supplied on the production path, with the negative control built **first**, per **Principle I** (missing). **HIGH.** The broad version of this check -- "an optional parameter with an empty default that every unit test supplies and no production caller does" -- was built and run read-only in three formulations (24, 32 and 43 hits) and **does not catch the gate defect that motivated it** in any of them: the defect is not "a parameter nobody passes" (that describes good dependency injection as often as a bug) but "a parameter whose empty default makes a **safety gate** vacuous", and the signal lives in what the value feeds, not in the signature. It also spans three hops, with tests entering at hop 3 while production enters at hop 1, so a signature-keyed rule is structurally blind to it. Shipping the broad version would have meant ~40 hits of which ~35 are legitimate, allowlisted down to nothing -- a gate nobody reads that still misses the shape, i.e. this project's defining defect wearing the costume of its fix. Feed the checker a synthetic tree containing the real shape and refuse to ship it if it cannot flag that. *(Landed 2026-09-22 `3ede54d`; VERIFIED: `tests/contract/test_gate_inputs.py` exists with `GATE_INPUT_ALLOWLIST` as data, each entry required to cite a task or finding id. Negative control built first against the synthetic three-hop `capture_for_step` shape -- the one all three formulations of the rejected broad scan missed.)*
- [X] T286 Assert that a public helper in `src/` has a production caller, per FR-008 and **Principle II** (missing). **HIGH.** Sibling arm to T285 with its own negative control, and a genuinely clean signal because the `src/`-versus-`tests/` partition is unambiguous. Worked example, proven live: `act/prompts.py::prompt_screen_for_declaration_id` correctly honoured the one documented exception (`prompts.ai_diplomatic_approach` answers the screen `prompt.diplomatic_approach`) and had **zero callers in `src/`**, while `act/executor.py` re-derived the key inline with a naive prefix swap that was right for twelve of thirteen declarations. The cost was total: the rejected key left the prompt unanswerable, `has_blocking_prompt` stayed true, `turn.end_turn` was refused, and no run on that board could advance a turn at all -- with the suite green and the correct code present and tested. *(Landed 2026-09-22 `3ede54d`; VERIFIED: the derivation-helper arm ships in the same file with `reached_via` checked on both halves, so a same-module reference cannot vouch for itself. It fired for real during development on `prompt_declaration_id_for_screen`.)*
- [X] T287 Declare which tests reach the client instead of inferring it from a directory path, per **Principle VII** and the standing lane rules (missing). **MEDIUM.** `pyproject.toml` registers `live` as its **only** marker and `--ignore=tests/live` as its **only** default exclusion, so anything reaching the client or the tuner from outside `tests/live/` is invisible to both. The guard must be **unconditional**: a marker that skips when the client is *absent* runs when the client is *present*, which is exactly the case that costs another lane its stage. Attribute the resulting suite-count delta explicitly in the commit message so a smaller total is not read as a regression. *(Landed 2026-09-22 `7f4d0b6`; VERIFIED: `pyproject.toml` carries `addopts = ["--ignore=tests/live", "-m", "not client"]`, unconditional. Confirmed live by the lane lead's own suite run at `8992cf6`, which reported **41 deselected** -- 40 from `test_host_platform_port.py` plus one doctor CLI test that was the only `run_doctor` exercise injecting neither a host nor a nexus factory.)*
- [X] T288 Correct the record that no frame has ever been shown to the agent, per SC-009, SC-019 and **Principle III** (contradicts). **HIGH.** `validation-results.md:871-879` states it in bold; measured from the store, 766 captures, 361 `screened_clean`, **300 `shown_to_agent=True`** across 19 runs and **287 model calls carrying an image**, all `host_support_tier=validated`. The provenance gate accounts for 291 of 405 withholds, not all captures. T224 and T260 carry the same dead premise verbatim. SC-009 and SC-019 are release-**blocking** audits whose real-frame test T194 does not exist -- so an auditor reading this document concludes no real-frame audit is owed, which is wrong in the unsafe direction by 287 images, and per T264 the gate could not have caught the contaminant in any of them. State that the real-frame half is **owed and unpaid**. *(Landed 2026-09-22 by the lane lead. NOTE: reported closed earlier and it was not -- the false sentence survived verbatim, which is why this was verified against the file rather than the report. Figures re-measured read-only from a copy of the store rather than taken from any report: 843 captures, 396 `screened_clean`, **314 `shown_to_agent`** across 24 runs, **301 of 670 model calls carrying an image**, 316 of 447 withholds `provenance_failure`. `validation-results.md` now states that SC-009's and SC-019's real-frame half is **owed and unpaid**, and that the correct wording for the retro-audit is "no contamination was found", never "the screening gate held".)*
- [X] T289 Correct the artifacts that describe a system other than the one that shipped (partial). **MEDIUM, documentation.** Grouped because they are one class: `quickstart.md:57` pins `2026.09.1 / 215 declarations` where the code prints `2026.09.14 / 62`, and `README.md` repeats the defect twice with two different wrong values, one labelled as taken against a live client; `quickstart.md`'s canonical all-green doctor block shows a macOS host at `tier VALIDATED` when `host/detect.py:248-256` sets the macOS spike to failed and the tier resolver returns `unsupported`, which quickstart's own text says is refused at preflight -- **the reference example is a run that cannot start**; `quickstart.md` invokes two `civsim audit` subcommands that do not exist among the thirteen registered; the doctor platform and capture lines are quoted three ways, two of which the code never emits, contradicting each other inside one page; `contracts/operator-surface.md:24` publishes an HTTP route that does not exist and `:28-29` states a false absolute that silently loses every `start`; `contracts/capability-catalog.md`'s only worked example of the bespoke rule cites research R5 for the opposite of what R5 returned and names a module that is now forbidden; `data-model.md`'s state machine still shows two loop exits where the model has four; `plan.md`'s counts (53/12/thirteen) are 54/9/16 and FR-054 appears nowhere in it; `contracts/match-store-port.md:145-148` contradicts its own header about E1-E5. Prefer placeholders over freshly-pinned numbers -- the catalog version moved 14 revisions in ~10 days, and these have already drifted twice. *(Audit 2026-09-22: **PARTIAL — the `specs/002-civ-playing-harness/` half is done; `README.md` is not.** Landed in `99f4224` and `35fc2a2`: `quickstart.md`'s doctor block is now Linux/X11 -- the one platform that actually resolves to `VALIDATED` -- with every computed value a placeholder and the reason stated inline; the two non-existent subcommands `civsim audit saves` and `civsim audit endings` are removed (no shipped subcommand covers either check, so they were **not** remapped) and the thirteen that do ship are named; `contracts/operator-surface.md`'s phantom `POST /seedsets/{name}/accept-build` now carries the *(no HTTP form)* marker and its "every command is recorded" absolute is scoped to the run-scoped commands with `start` explained; `contracts/capability-catalog.md`'s bespoke example no longer points at the forbidden `dialog_driver.py` and records R5's Outcome A; `contracts/match-store-port.md:145-148` now matches its own header. `data-model.md`'s two-exit state machine is fixed under this task's sibling finding. **Still open:** `README.md:91` and `README.md:107` each carry a wrong doctor sample (51 and 215 declarations against a real 62), and the second is labelled as a live-client measurement, which makes it worse than an ordinary stale number -- the label is what would stop a reader doubting it. `integration-readiness.md:104` repeats the 51. Both files are **outside this agent's file ownership** and were deliberately left untouched; they need an owner assigned. **`plan.md`'s share is now also done** (`0105931`, `fd242a5`): the stale storage framing is retitled to past tense with the landing commit `93a9b7e` and the fact that deliverable 3 **extended** `SqliteMatchStore` rather than replacing the adapter, with 003's open-time orphan sweep (rule W6) named as behaviour 002's plan did not describe, and C2 annotated the way C1 is; the Constitution Check's self-contradiction is resolved (the never-written save/load dialog row is struck through and kept, and the surface that did ship -- the bespoke prompt click -- gets the row it never had); and the three counts are 54 / nine / 16 with FR-054 given its place in the Check via `observe/host_gate.py`. **"12 subsystems" is deliberately not restated as another number** -- no grouping in these artifacts yields twelve, so it is replaced by the nine groups `spec.md` itself heads the requirements under, which a reader can re-derive. Same for "thirteen subpackages": 16 top-level packages, counting rule written down beside the number.)* *(Landed 2026-09-22, closing the "Still open" remainder: `README.md:91` and `:107` now use the same `<catalog_version>`/`<N>` placeholder pattern `quickstart.md` already established, with the reasoning sentence restated inline; `:104`'s tuner indices are now `<gc_index>`/`<ingame_index>` placeholders too. `:107`'s "taken against a live client" label is dropped -- the sentence now says "the shape of a fully healthy run (see quickstart.md for the live, unpinned version)", since quickstart.md's own block is itself placeholders now and no longer has concrete numbers this page could claim to be reproducing. `integration-readiness.md:104` is different in kind, not just location: that block is a dated, reproduced-live audit capture (2026-09-20), not a documentation example, so its `51` was left as the authentic historical record and a footnote was added directly below stating the number is a stale snapshot (`catalogs/VERSION` now `2026.09.14`) and pointing to `civsim doctor` for the current figure, rather than re-pinning a number for a run that already happened. Re-derived the true count myself rather than trusting the brief: `grep -hc '^- declaration_id:' catalogs/observations/*.yaml catalogs/actions/*.yaml` = 62 (21 + 41). **Noted, not acted on (out of this task's scope):** while verifying, found `operator/cli.py:965` now calls `configure_runner_factory(default_runner_factory)` at import (commit `adda5c2`, dated 2026-09-20, "Phase 9: the composition root"), which may mean `integration-readiness.md`'s central Verdict -- "a live run cannot be attempted today," resting on no `RunnerProtocol` ever being wired -- is itself now stale. Did not verify further or touch the Verdict; flagging for whoever owns that finding next, since auditing it properly is beyond a documentation-correction pass.)*
- [X] T290 Record honestly which success criteria are unobserved rather than leaving them ambiguous, per SC-001, SC-002, SC-010, SC-011 and SC-014 (partial). **MEDIUM.** SC-001's 90% rate has no success-rate counter anywhere in `src/` for a test to assert against; SC-002 is unobserved and the longest recorded run is three harness turns; SC-010's 60 s is asserted only as a configuration bound on a simulated clock; SC-011's >=20-run quantifier returns zero grep hits; SC-014's identical-position half is a spike narrative and its test file does not exist. The per-run derivations underneath several of these are well asserted -- it is the fleet-scale and real-client claims that are empty. Say so in `validation-results.md` in terms an auditor cannot misread, the way the 003 lane did. *(Landed 2026-09-22: added a "Success criteria with no evidence behind them" table to `validation-results.md`, re-verifying every claim against the current tree rather than copying this brief. Each of the five was independently confirmed: no success-rate counter in `src/` (SC-001, plus T237 still open); no era/full-game test and longest single run is 3 harness turns, game turns 5-17 (SC-002); the 60s bound is a configuration assertion in `tests/unit/test_detection.py` only, T193's live file absent (SC-010); zero grep hits for a >=20-run fleet quantifier, largest corpus 7 runs (SC-011). **One correction to this task's own premise:** SC-014's live test file is no longer absent -- commit `c440496` (2026-09-22, same day) added `tests/live/test_branch_identity.py` for T177, but its own commit message says it only "collect[s]/skip[s] cleanly" and it has never been executed against a real client, so the position-equality claim remains unobserved regardless. Did not touch the T288 correction block; added as a new subsection immediately after it, before the `## Verification` divider.)*
- [X] T291 Normalise `tasks.md`'s seven embedded lone `\r` bytes and fix `spec.md`'s `Status: Draft` (partial). **LOW.** The file carries 706 CRLF, 6 bare LF and seven lone `\r` characters embedded mid-line, each at the seam where a completion annotation was appended (lines 619, 624, 634, 648, 670, 690, 691) -- CommonMark treats a lone `\r` as a line ending, so in any rendered view those seven tasks are split from the very text asserting they landed and were verified, and Python's universal-newline `open()` sees 719 lines where `grep` sees 712. Preserve the dominant terminator; do not reflow, renumber or reorder. Separately `spec.md:7` still reads `Status: Draft` for a feature with 245 closed tasks and recorded validation results. *(Audit 2026-09-22: **what actually happened was a whole-file conversion to LF, not the seven-byte fix this task specifies** — `tasks.md` went from 740 CRLF / 7 lone CR / 6 bare LF to uniform LF with zero CR bytes, landing under `a6fad2d`. Recording it plainly because it is a different change from the one briefed: the goal is met more completely (no completed task is split from its own evidence in any renderer, and `grep`/`wc -l` and Python's universal-newline `open()` now agree on the line count) but a reader auditing that diff cannot tell a terminator sweep from a content edit, so a conversion of this kind needs flagging before it lands, not after. Content was verified unaffected. **Also folded into this task's scope:** checkbox case must be `- [X]`, never `- [x]`. This is not cosmetic — every count in this lane's reports comes from grepping `^- \[[ X]\]`, so a lowercase box is invisible to **both** the open and the closed tally and silently corrupts the numbers. Two lowercase instances (T270, T273) were normalised on 2026-09-22; several agents are closing tasks concurrently, so this needs re-sweeping. `spec.md:7` is untouched and still open.)* *(Landed 2026-09-22: CR-byte half re-confirmed, not redone -- `python3` byte count on `tasks.md` shows zero `\r` bytes, uniform LF, 757 LF total. Re-swept checkbox case: `grep -c '^- \['` and `grep -c '^- \[[ X]\]'` both return 292 with no remainder, i.e. every task line is exactly `- [X]` or `- [ ]` -- no lowercase instances remain (the only `[x]` match anywhere in the file is inside this task's own prose, in backticks, not a checkbox). Counts re-derived at edit time: 292 total, 261 `[X]`, 31 `[ ]`; T261/T262 confirmed unallocated (mentioned only in Phase 14 prose at line 723). Fixed `spec.md:7`: `Status: Draft` -> `Status: In Progress (2026-09-22)` with an explanatory note, since this project's own convention (`Implemented` = all tasks closed, per 001/003) does not yet apply with 31 tasks open.)*

- [X] T292 Assert that the resolved screening profile matches the host platform, per **Principle I**, FR-025 and SC-009 (contradicts). **HIGH.** The completed frame retro-audit found **34 capture withholds citing the Windows capture-border category on a Linux/X11 host**, across two runs (`run-227998759f75` x19, `run-fd9c08a1df1d` x15, both recorded `os=linux/x11`): profile resolution returned the **default** profile where it should have returned the Linux one, so a Windows recording-border test was evaluating frames on a host that has no such border and firing on the game's own dark UI edging -- the same test that fires on **185 of the 188 frames that passed**. Those 34 withholds were not the protection the record makes them look like. Bounded: neither run ever delivered a frame to a model, so nothing downstream was affected. **Shape, adjacent to the day's pattern rather than identical:** not a mechanism never reached, but **a mechanism reaching the wrong thing and being counted as if it had worked**. T266's category-to-technique data guard must assert the resolved profile against the host platform or it will pass while this is live. Related and already known: `screening_profile` resolution ignores the declared value unless it is literally `"default"` (`parity/screening.py:264-278`), and all five shipped views declare `platform`, which is not a key in `screening_profiles.yaml`. *(Landed 2026-09-22 `5fd35c6`. Root cause traced to a commit, not inferred: the two affected runs predate `98c71bb` (13:11 EDT), when every view still declared `screening_profile: default` -- the strictest *union* profile, which names every platform's chrome including the ones that cannot apply. `98c71bb` changed the declaration to `platform`, which is **also not a key** in `screening_profiles.yaml`; it only worked because `resolve_screening_profile` treated *every* non-`default` string as "resolve by running host", so `platfrom`, `Default` and `linux-x11` would all have resolved silently too. Now closed: a `ScreeningProfileDeclaration` StrEnum (`default` | `platform`) validated at catalog load, and `resolve_screening_profile` raises `CatalogError` on anything else, with `_check_content` catching it and **withholding with the reason** rather than killing a run mid-turn over a declaration typo. Assertion as data over every shipped view x every `OperatingSystem`, both sides derived from the real catalog. Revert-confirmed: restoring the historical declaration on `views.world` alone reddened 7 tests naming `windows_capture_border` on a Linux host -- the exact string from the 34 withholds.)*
- [ ] T293 Stop reading back state in the same tuner command as the write, per **Constitution Principle III**, FR-013 and FR-008 (contradicts). **HIGH, live lane owns the fix (`run/**`).** **A readback issued in the same tuner command as the write is not a readback** -- the tuner returns the **pre-call value**. Proven: setting zoom to 0.5 read back `0.0499997` in-command and `0.50000047683716` in a later command. The cost is already in the record: `turn.end_turn` now dispatches correctly after the prompt fix, but the harness re-reads the turn number before the asynchronous advance lands, so **three turn cycles recorded `game_turn_advanced: False` while the game demonstrably advanced from 56 to 57 -- the harness caused three turns and credited itself with none**. The turn-by-turn record denies an event it caused, and by the store's own rule a run with gaps cannot feed trending. **The architecture already does the right thing where it re-reads separately**: `cities.set_production`'s success verdict came from the harness's later re-observation, not from the Lua's same-command answer, which said `confirmed: false`. So this is a pattern to hunt at each inline readback site, not a design to redo. Audit every verification shaped that way. **Supersedes** the retracted claims that `UI.SetMapZoom` and `DiplomacyManager.CloseSession()` were no-ops reporting success: both calls worked and the measurement was wrong.
- [X] T294 Check whether the audit and coverage code paths actually read `verification`, per FR-011, FR-017 and 003 FR-018 (partial). **MEDIUM.** Landing FR-011's validator (T268) turned up **eleven test fixtures that recorded `outcome: applied` with an empty `verification`** -- `tests/store_support/builders.py`, `tests/contract/{test_record_schemas,test_match_store_port,test_provider_resilience_contract}.py`, `tests/unit/{test_store_coverage,test_runner_resume_from,test_performance,test_operator_api,test_audit,test_completeness}.py`, `tests/integration/test_branching.py`. That is exactly the record the model now refuses, which means **the store, audit and coverage layers have been exercised almost entirely against applied decisions carrying no verification at all**. The fixtures were widened minimally to `{"declaration_id": ..., "result": True}` so the suite passes; nobody has checked whether those layers *read* the field. If they do, that coverage was vacuous and the widening has only hidden it -- the day's pattern one layer out, in the fixtures rather than the code. *(Closed 2026-09-22 as a **clean negative result**; the negative is the deliverable and no code changed. **Nothing in `src/civsim_harness/store/**` and nothing in the `civsim audit` code paths reads `ActionExecution.verification`, so the eleven widened fixtures were not hiding vacuous coverage -- there is no consumer of the field's content to have covered.** Evidence, every read or write of the field in all of `src/`: `models/decision.py:99,104,109`, the FR-011 validator itself and the only consumer; `act/verify.py:178-187,234-250`, the producer; `run/turn_cycle.py:427`, the end-turn splice, also a producer. A grep for `'"verification"'` across `src/` returns **zero** hits, so there is no dict-key access after a `model_dump()` either. What the named layers actually read off an execution: `store/coverage.py:809-816` takes `outcome` and `rejection_reason` only; `operator/audit.py:538` takes `verified_at`, and that is the single `execution` reference in all 1227 lines of the audit module -- no subcommand (`audit_decisions`, `audit_parity`, `audit_steps`, `audit_loop`, `audit_completeness`) inspects the payload; `store/completeness.py` never touches `decision.execution` at all (its one mention of `outcome`, at `:141`, is prose); `store/trends.py:139-140` reads `action_declaration_id` and `parameters`; `operator/api.py` has no reference to either. **No test was added, deliberately**: with no consumer, a test asserting the field merely round-trips would be exactly the "present, green-looking, not testing" shape this lane has now found five times. Two notes for whoever revisits this. (1) **The widening was load-bearing, not cosmetic.** The store round-trips bundles as whole-model JSON -- `store/sqlite_adapter.py:454` writes `bundle.model_dump_json()`, `store/sqlite_reads.py:213` reads `DecisionStepBundle.model_validate_json(...)` -- so the FR-011 validator runs on **read** as well as on write. Once it landed, an `applied` fixture with an empty `verification` could not survive a store read at all, which is why eleven fixtures had to change; it is also why a future projection-based serializer cannot silently drop the field, since an `applied` record returning without it raises rather than going quiet. (2) **The fixtures carry a thinner payload than production does** -- `{declaration_id, result}` against the real `{declaration_id, predicate, result}` plus the confirm-window keys (`act/verify.py:178-184`). That is immaterial only while nothing reads it. **The check to re-run the day a store, audit or coverage path starts inspecting it is `grep -rn '\.verification\b' --include=*.py src/ | grep -v verification_predicate`**: the moment that returns a consumer outside `models/decision.py`, `act/verify.py` and `run/turn_cycle.py`, those eleven fixtures need realistic payloads and this task reopens.)*

- [ ] T295 Re-verify `integration-readiness.md`'s Verdict, which rests on a premise the code no longer has, per FR-001 and SC-001 (contradicts). **MEDIUM.** That document's central Verdict (`:6-8`) states **"No. A live run cannot be attempted today"**, and its reasoning is that no `RunnerProtocol` is ever wired, so `run start` fails with "no runner is configured" (`:66`, `:110`, both marked *reproduced live*). **That premise is false as of `adda5c2` (2026-09-20, Phase 9's composition root):** `src/civsim_harness/operator/cli.py:965` calls `configure_runner_factory(default_runner_factory)` at **module scope**, with no indentation, under a comment reading "Default wiring (T209)". The factory is assigned at import, so the documented first failure cannot occur as described. The document was honest when written and has aged out, exactly as 003's SC-005 test did. Needs a real re-verification of each numbered step in its failure chain, not a documentation tweak -- the Verdict may still be "no" for a different reason (T237's cold-client-to-turn-1 gap is open and is the likelier current blocker), and replacing one unverified verdict with another would be worse than leaving it. **Found while correcting the doctor sample numbers (T289), and worth recording how:** the finding was nearly missed twice -- once because the reporting agent treated it as out of scope, and once because the lane lead's confirming `grep` was truncated with `head -3` and returned only the three definition-site hits, making the module-scope call at :965 look absent. **A negative search result is evidence only if the search could have matched -- and a truncated search result is not evidence at all.**
- [ ] T296 Screen the border-ring technique against the host it runs on, per **Principle I**, FR-025 and SC-009 (partial). **MEDIUM.** Surfaced by T283 and left deliberately visible rather than passed over: making the realistic-gameplay negative control green needed **two** changes, not one. T283's HUD-corner narrowing alone is insufficient -- the frame also had to be screened against the **linux** profile (T292), because on the union `default` profile the **border-ring** technique withholds it too: the game's own flat dark top bar trips the capture-border test. **So on a Windows host the same ordinary gameplay frame would still be withheld today**, for a different technique than the one T283 fixed. This is the same shape as T292 one technique over -- a check firing on the game's own chrome because it is being applied where it does not belong -- and it means the corner heuristic was never the only thing standing between a clean frame and a withhold. Needs the border-ring technique's own applicability bounded the way the profile resolution now is, and a realistic-frame negative control per platform rather than only on Linux. Note the honest limit inherited from T283: the measured band is narrow (1.53 clean vs 1.84 overlay against a 1.8 threshold) and a 220x60 overlay over the minimap is **not** caught at 1.71, so neither technique should be treated as a strong detector until there is a real negative population to tune against -- and there is none, because the 421 withheld captures were stored with `blob_ref=NULL`.)
- [X] T297 Record the derived screening statistics for every withheld capture, so the detectors have a negative population to be evaluated against, per **Principle I**, **Principle III**, FR-025 and SC-009 (missing). **MEDIUM, and it is a defect this project created.** Every detector threshold in `parity/screening.py` is currently unfalsifiable: the three corner constants were introduced with **no recorded derivation, frame size or sample**, and they cannot be derived now because **all 421 withheld captures were stored with `blob_ref = NULL`**. We discarded exactly the evidence needed to evaluate our own gate, so "is this threshold right?" has no answer and T283's narrowing had to be measured against **one synthetic frame** -- yielding a band of 1.53 clean against 1.84 overlay on a 1.8 threshold, with a 220x60 overlay over the minimap missed entirely at 1.71. **DO NOT FIX THIS BY STORING WITHHELD PIXELS.** A withheld frame is the one most likely to contain the user's desktop chrome -- a FireTuner window, a notification toast, a panel -- which is precisely the data T265 agreed to hash rather than keep, and retaining it would convert a screening gate into an archive of the thing the gate exists to suppress. That would be a Principle I regression dressed as a Principle III improvement. **Store the DERIVED STATISTICS instead**: for each withheld capture, the metric values the detector actually computed (per-corner variance and the whole-frame and peer references behind each ratio, border-ring uniformity, the text tokens' match/no-match verdict **as a boolean, never the token strings**) plus which technique fired and which reject category it was attributed to. That is a handful of floats and an enum per capture -- it costs bytes, carries no image, cannot reconstruct one, and is enough to build the distribution that would tell us whether 1.8 is a threshold or a coin toss. Pair it with the same treatment for **screened-clean** captures, since a detector is only evaluable against both populations; the audit of 188 delivered frames is the one real clean sample this project has and it exists only as a spike narrative. Note the ordering constraint: the statistics must be derived at screening time and persisted with the capture record, because they cannot be recovered afterwards -- which is the whole lesson of this task. *(Landed 2026-09-22 `7668f8b`. Records `CaptureScreeningMetrics` on every capture the content gate judged: per-corner variance **with its reference and whether that reference was a HUD peer**, border-edge uniformity and interior delta, a text-evidence **boolean**, and which techniques ran, fired and matched. **No pixels, no colours, no tokens, no titles.** **Four independent proofs that nothing image-reconstructable or private enters it**, not one: a **literal** frozenset key assertion (not derived from the model, so it can actually fail) with `extra="forbid"`; an **information bound** -- the serialised payload is asserted under 2048 bytes, parametrised over 64x64 **and** 1920x1200, so payload size does not grow with pixel count and therefore cannot contain the image; a **runtime leak scan through the real production path** with a sentinel desktop title, the frame's sha256 and its base64 bytes as probes, over both the metrics and the whole capture record, each with a planted-value negative control; and by design `BorderEdgeMetric` records the *distance between two means*, never a mean colour. **The scan fired on its first run** -- it caught `blob_ref`, which legitimately *is* the frame's content hash -- and that one field was scoped out with a comment rather than the probe being weakened. Schema regenerated from the model, **additivity proven three ways**: `diff` showing **0 removed lines**, a `check_additive_only` returning empty, and `export_all` not refusing (it aborts the whole export on a non-additive change, so a clean run is itself a proof). **No threshold changed** -- verified mechanically, the diff contains no line defining any of the six constants. **One design point that avoided rebuilding the blind spot:** the statistics are derived **before** the coverage check, so a frame withheld for "no technique addresses this category" -- which is the shape most Linux withholds take today -- still records its numbers. Recording only the frames the detector got as far as judging would have reproduced the same hole one gate earlier. And `_check_content` hands the **same** `FrameStatistics` object to the detector that it persists, so what was recorded and what was decided on are one computation. **What it will answer:** whether 1.8 separates real clean frames from real contaminated ones, with `reference_is_hud_peer` recorded per corner so the whole-frame and HUD-peer populations are **not pooled into the uninterpretable mixture** that would reproduce the current problem, and with the abs-min floor's discards included -- a distribution of only the values that cleared a threshold cannot evaluate that threshold. **What it will not:** whether a frame the gate *cleared* was actually clean. That needs a labelled sample, and this records no labels and no pixels by design -- so it can show the threshold sits in a dead zone, and cannot by itself give the right value or measure the false-negative rate.)*
- [X] T298 Make record completeness tell the truth about a trailing turn, per **Constitution Principle III**, FR-013 and 003 FR-018 (contradicts). **HIGH.** `sqlite_reads._turn_gaps_body` deliberately exempts the **in-flight turn of an actively-playing run** from the gap check -- reasonable on its face, since a turn in progress is not yet a gap. The consequence is not reasonable: **a run that dies on a write failure and is not moved out of `playing` reports `complete`.** The halt is what makes the gap visible, so **anything that halts without transitioning lifecycle state hides its own data loss**, and the field reports completeness for precisely the run that lost data. Found while writing T279's write-failure test: a first draft asserted `HAS_GAPS` for a playing run with a turn-1 quicksave and no turn record, and got `COMPLETE`. **This is the second pattern in the one field the store uses to decide whether a run may feed trending** -- a value whose name asserts something it does not mean, alongside `turn_reached`, `dispatch_result` and `shown_to_agent`. Unlike those, this one gates a Constitution III guarantee: 003's rule is that a run with gaps must not feed trending, and a run whose gaps are invisible passes that gate. The fix is not to remove the in-flight exemption -- that would make every live run report gaps -- but to make the two states distinguishable: either completeness answers `unknown`/`in_flight` rather than `complete` for an actively-playing run, or the halt path is made unable to leave a run in `playing`. Prefer the first; the second depends on every future halt path remembering to transition, which is the assumption that produced this. Pair it with a check that a run halted by a write failure cannot report `complete`, with a negative control. *(Landed 2026-09-22 `de85b6f`. Added `RecordCompletenessStatus.IN_FLIGHT` rather than reusing `unknown`, because `unknown` has a published meaning -- "no turn has been attempted, so completeness cannot be judged" -- and overloading it would make that operator-facing sentence false for a run that HAS attempted turns. The burden sits in **one** predicate, `in_flight_turn_is_unpersisted`, asked by both the port-level derivation and the store's in-transaction one, which now also share one definition of the exempt lifecycle states so the carve-out and the state disclosing it cannot drift. The rejected alternative -- "every halt path must transition first" -- is the assumption that produced the bug. **SECOND FINDING, larger than the task: the Principle III trending gate was FAIL-OPEN.** `store/trends.py::exclusion_for` enumerated `has_gaps` and `unknown` and let **everything else fall through to eligible** -- so any completeness value it did not recognise, including one added tomorrow, passed the gate clean. Verified fixed in committed code: it now admits **only** `COMPLETE` (`if completeness is not RecordCompletenessStatus.COMPLETE`), `_REASON_FOR_STATUS` merely *names* the refusal, and an unmapped status is still refused via `_UNRECOGNISED_COMPLETENESS`. Note the direction: unlike the day's three fail-safe defects, this one failed **open** -- the guard enforcing Constitution Principle III admitted what it could not classify. Every consumer traced by reading: `civsim_web/viewmodels/base.py:260` was already fail-closed and refuses correctly; `store/coverage.py` reports `has_gaps=False` for an in-flight run, which is **true** and decides nothing; `operator/audit.py` now summarises `in_flight` and still PASSES, correct because it enumerates gaps and there are none. **003's published criterion was narrowed, deliberately**: US3/AC3 said a still-playing run's turns-so-far are trended. A mid-turn run now derives `in_flight` and is **excluded** -- not because turns 1..N-1 are wrong, but because that shape is **indistinguishable from a run that died on turn N's write**, and Principle III does not let the gate guess. AC3 now holds during the window the record is whole, which is every moment between a turn landing and the next quicksave -- a real window, not a technicality. Amended across `003/spec.md`, `003/contracts/match-tracking-store.md`, `003/data-model.md`, `002/data-model.md`, and `Run.schema.json` regenerated from the model rather than hand-edited. Negative control built first and observed: forcing the predicate to `False` reproduced the discovery exactly -- `playing` run, turn-1 quicksave, no turn record, reported `COMPLETE` -- then byte-identical restore, then green.)*

- [X] T299 Give the declared-text technique a keyword vocabulary it can actually match, per **Principle I**, FR-025, FR-030 and SC-009 (partial). **HIGH -- and it means the coverage guard currently reports coverage the gate does not have.** Found while landing T265. The technique matches a reject id only when **every** `_`-split token of that id appears in the desktop evidence. So `developer_console` genuinely fires on a window titled "Developer Console" -- the technique is real and works -- but three shipped ids contain a word no window title will ever supply: **`firetuner_window` needs "window", `harness_owned_ui` needs "owned" and "ui", `linux_panel` needs "linux"**. Those categories are **addressable in the coverage map and unmatchable in practice.** **Why that is worse than the hole it replaced.** T264's fail-closed rule withholds a frame for any category no technique addresses, and T266's guard asserts every category in every shipped profile is addressable. Both now pass **because `firetuner_window` is nominally addressed by a technique that cannot match it** -- so the frame is delivered, and the guard says it was screened. This is the day's pattern one layer up: the check that was built to prove the gate covers a category is satisfied by coverage that is real in the map and empty on the wire. It is the same structure as the profile that resolved to `default` (T292) -- a mechanism reaching the wrong thing and being counted as if it had worked. **Deliberately not fixed by emitting source-describing tokens.** The T265 agent considered emitting `"window"` and rejected it with the reasoning recorded verbatim in `WindowTitleListing.text_tokens`'s docstring: FireTuner runs on every supported host by construction, so `firetuner_window` would then match whenever a FireTuner window exists **anywhere on the desktop** -- every frame withheld forever, on an XComposite window-scoped capture path that structurally **cannot** contain another application's window. That is fail-closed to the point of uselessness, and it would have looked like a fix. Two real options, neither reachable from the host port: an explicit **per-category keyword vocabulary** in `catalogs/screening_profiles.yaml` (each reject id declares the words that actually indicate it, rather than being tokenised from its own name), or **frame-scoped evidence** -- which windows overlap the captured rect, and stacking order -- which is strictly more informative and would also retire the corner and border heuristics' guesswork. Prefer the first as the shippable step and record the second as where this is going. Until one lands, **do not cite `firetuner_window` as screened**: SC-009's obligation is still unpaid on exactly the contaminant the gate was built for. **HYPERVISOR RULING 2026-09-22:** (1) **Image delivery stays open on Linux -- but not because the gate works.** It stays open on the **structural** evidence: the X11 capture path reads the game window's own off-screen pixmap with no screen-grab fallback, so another window cannot be in the frame, and the retro-audit found nothing in all 188 delivered frames. That reasoning is unaffected by this finding. (2) **`firetuner_window` must not be cited as screened. Anywhere** -- not `validation-results.md`, not a scorecard, not a release note. The honest statement is: *the category is undetectable by the current technique set; the protection on Linux is structural, not gate-derived.* Landed in `validation-results.md` beside the four-gates claim. (3) **This is a RELEASE BLOCKER for Windows and macOS**, whose capture is **not** window-scoped: they have no structural protection and their coverage guard passes anyway. Framing to keep: we reopened Linux delivery partly on the strength of guards that pass here, so this is **the original screening defect surviving its own fix** -- a guard satisfied by a vocabulary mismatch is a guard that cannot fail in the way that matters. *(Landed 2026-09-22 `a6c88dc`. **`firetuner_window` is STILL NOT MATCHABLE, and that is the honest outcome, not a shortfall.** No vocabulary was invented for it. It is now *uncovered* on Windows, macOS and `default`, and covered on Linux **by the capture path's structure only** -- never cited as screened, per the ruling. Each reject id now declares `text_keywords` (any-of groups of all-of words) with an `origin`, and the loader **refuses at load**: a bare-string definition, a missing `text_keywords`, an empty one without both `text_unmatchable` and `text_settled_by`, a keyword the tokeniser cannot emit, and -- the matchability rule -- **a keyword group no declared witness produces**, checked by running each witness through `host/port.py::window_text_tokens`, **the evidence source's own function rather than a reimplementation**. Today's shipped state does not load under it. `DECLARED_TEXT` is claimed only where a vocabulary exists, and the default is deliberately **not** "fall back to the id's tokens", so a forgetful call site cannot silently restore the bug. **Exactly one of ten shipped categories has a vocabulary any evidence source can supply** -- `developer_console` -- and a test pins that number **so it cannot grow by invention**. Even its own basis is recorded honestly: the witness is a naming convention plus the harness's own end-to-end control, not an observed console window, and Civ VI's real debug console is in-engine with no window title at all. Coverage-without-a-technique is now **declared data rather than prose**: a category's `origin` (`separate_window` | `in_frame`) plus a profile's `capture_scope` (`window_backing_store` requiring a recorded basis | `unproven`). That puts the hypervisor's structural ruling **where the coverage map can read it** -- which is precisely why the map could previously be satisfied by a rule that could not match. **The release blocker is now executable rather than prose**: Windows and macOS withhold for want of coverage, their guard parametrisations `xfail(strict=True)` against a hand-written `_UNCOVERED_PROFILES`, so closing it forces a deliberate deletion rather than a quiet green. This changes no frame in practice -- neither implements `list_window_titles` and image delivery is already closed on both -- **but it changes the record**, which previously counted those categories as screened. Linux delivery unchanged. **Judgement call endorsed by the lane lead:** the agent declined to declare `firetuner_window: [[firetuner]]`. Mechanically that differs from the rejected "emit `window` from the host"; **in effect it is identical -- desktop-scoped presence inferred as frame-scoped presence** -- and shipping it would have turned a red guard green without changing what the gate can see. The catalog names that alternative explicitly and records what would settle it: an observed FireTuner window title in a spike (weak), or frame-scoped overlap and stacking evidence (the right answer). Negative control run both ways: a permanent `test_todays_state_does_not_even_load` reproducing the exact pre-T299 vocabulary, refused with and without the most plausible witness; plus a control's control proving the rule rejects **unmatchability**, not name-shaped vocabularies. Verified by the lane lead at `a6c88dc`: 100 passed, 3 xfailed.)*
- [X] T300 Add the discarded-return arm to the long-phase roster's sibling checks, per FR-008 and **Principle I** (missing). **MEDIUM.** The silent-phase sweep (T-liveness, `tests/contract/test_long_phase_liveness.py`) catches *"the phase does not speak"*. Its sibling catches *"the value speaks and nobody listens"*: **a function whose return value must be consumed, called at a production site that discards it.** Worked example, verified by reading twice: `run/composition.py:746` is `debug_menu_preflight(host, home=home)` as a bare `ast.Expr`, sitting between `:745 catalog_result = catalog_preflight(catalog)` and `:747 host_gate_result = evaluate_host_gate(support_probe)` -- **both neighbours bind, this one does not** -- so no run ever made carries the setting. Fourth counted instance of the day's family, and the one that proves the class is **not confined to logging**. **Two things the builder needs, from the agent that verified the example rather than from theory.** First: the defect is **already documented in prose in two places and enforced nowhere** -- `run/preparation.py:452` names the discarding call site explicitly, and `tests/contract/test_match_store_port.py:1213` refers to it again. A finding written down twice and checked zero times is the pattern commenting on its own documentation; that belongs in the module docstring. Second: **the scan's unit must be `ast.Expr` wrapping `ast.Call`, and it needs a negative control for the `await f()` spelling** -- `ast.Expr(ast.Await(ast.Call))` is the same defect one node deeper, and a scan written against the sync shape alone passes the async form silently. Copy the machinery's *shape* from `test_long_phase_liveness.py`'s `_allowlist_problems` and `_phase_problems` (cite-a-task-id **and** name-an-owner, four-shape ratchet) rather than importing it -- `test_gate_inputs.py` and `test_reachability.py` each keep their own copy deliberately, so one file's idea of a valid justification cannot drift into another's. *(Landed 2026-09-22 `8420dbc`: `tests/contract/test_consumed_returns.py`, 34 tests, plus research item **R22**. Nothing under `src/` touched. **It found a second instance nobody had written down: `run/composition.py:991`, `turn_timer_preflight(...)`, also a bare `ast.Expr`, 245 lines down the same function.** Its raise-on-active-timer **gate** survives a discard; its **record** half does not -- so a finished run cannot distinguish `VERIFIED_NONE` from `UNVERIFIED`, **the exact distinction that state was added to preserve** (its own docstring asks for a record carrying an explicit *this precondition was never confirmed* fact). That needs a `Run` field before it can be bound, so it is a two-step fix. Both instances allowlisted to the **LIVE lane**, not taken here. **`composition.py:746` confirmed STILL DISCARDING at HEAD** by `git show HEAD:`, not assumed -- T280 added `Run.debug_menu_state` and the store already round-trips it, so **the field is waiting and only the producer line is missing.** **The prose count was wrong in our favour and is now three, not two**: `run/preparation.py:452`, `tests/contract/test_match_store_port.py:1274`, and `debug_menu_preflight`'s **own docstring** at `preparation.py:503` ("the caller does not record it yet"). Three recordings, zero checks. The `await` control was built and fired: `ast.Expr(ast.Await(ast.Call))` is flagged with the spelling named in the message, and it is **not hypothetical** -- `run/composition.py` awaits `refresh_state_indices` bare at `:839`, `:849`, `:928`. A sync-only scan would have passed those silently, which would have been the discarded-return check containing an instance of its own defect. **Scope measured, not assumed:** an AST sweep found **186 distinct callee names in bare-statement position** and only **28** with a non-`None`, non-scalar return; the two largest populations (`execute` 43 sites, `write_run_event` 40) are correctly discarded, and `preflight_chain` at `:748` is an instructive near miss -- discarded and right to be, since its gate is the raise. A broad scan would be ~150 findings of which a handful matter, which is R21's measured rejection arriving by a second route. Independent proof on the real tree with the allowlist emptied: 124 production modules scanned, **exactly 2 problems reported**, both named with full prose; with the real allowlist, none.)*
- [X] T301 Make the save-load wait and the recovery loop readable to a watchdog, per **Constitution Principle VII**, FR-045 and FR-046 (missing). **HIGH -- the highest-value remaining liveness fix in the repo.** Two of the six rostered long phases are allowlisted `owner: UNRESOLVED` because `saves/**` and `resilience/**` were granted to no lane. **`saves/load_game.py::_await_phase`**: `DEFAULT_LOAD_TIMEOUT_S = 300 s` with a 1 s poll -- **the longest explicit bound anywhere in `src/`, and 1.67x the entire 180 s watchdog budget on its own** -- and the whole `saves/` package has **no logging at all** (confirmed two ways: a grep over `saves/*.py` and an import scan of the same files, per the rule that a negative result is evidence only if the search could have matched). A save load is also the single operation most likely to be genuinely slow *and* genuinely wedged, which is exactly the pair the watchdog exists to tell apart. **`resilience/recovery.py::recover`**: unbounded in aggregate and dominated by that same 300 s load per attempt. It writes `RunEvent`s as it goes -- **which reach the store, not the driver log** -- so a log-polling watchdog cannot see any of it. That distinction is the roster's own criterion and `provider/chain.py` was the worked example of the confusion; this is the second instance. Fix both the way `provider/liveness.py` and `act/liveness.py` did: a per-iteration record naming what is being waited for, elapsed, and the bound, on the `civsim_harness` logger so it reaches stderr and the driver log. Emitting must never be able to fail the operation it describes. Lower `resilience.recover`'s priority behind the save load, since fixing `_await_phase` makes `recover`'s longest leg readable for free. Related and separately owned: **`run/turn_cycle.py::_dispatch_backstop_end_turn`** is a *second*, hand-rolled 200 s deadline/poll loop at `:409-422` that does **not** route through `confirm_execution`, so the `act/liveness.py` fix does not reach it. Its fix is two lines (import `emit_harness_liveness`, call once per iteration) and `run/**` is the LIVE lane's -- allowlisted to them, not to be taken here. *(Landed 2026-09-22 `7c56883`, as ONE change with one enumeration. **The enumeration produced a number neither roster row could state alone: `RELOAD_PATH_WORST_CASE_S = 510 s`, summed from the table rather than asserted -- 2.8x the 180 s watchdog budget.** `saves/liveness.py::RELOAD_PATH` lists every step the reload path can be sitting in while a watchdog counts, in path order, with its shipped bound and whether it emits: seven entries from `rung.abandon_attempt` through `loader.load_game` (300 s) to `rung.resume`. Patching the two rows apart would have left that 510 s unstated -- `recover` is spec 004's rung 4, `_await_phase` is the wait rung 4 is built on, and the 300 s in both rows is the same 300 s. **Work-derived where it counts, and the distinction is asserted, not claimed:** `reload.phase.polled` and `reload.rung.step` are published only after a round trip returns or a durable write lands -- **no thread, no timer, no clock-driven emission anywhere, so the record rate IS the work rate and a stalled loop goes quiet on its own**. `test_await_phase_publishes_nothing_while_a_poll_is_hung` asserts that against a round trip that never returns -- the property `provider/liveness.py`'s ticker structurally cannot have. The two `*.entered` records are registered as observer-derived. **Honest sub-registration carried forward:** during the load the tuner port is closed *by design*, so most polls answer `unreachable`. That is work-derived about the **poll loop** (a refused connect is an answer from the OS) and is **not** evidence the load is progressing -- hence the name `reload.phase.polled`, never `loading`. A run of `unreachable` proves the loop is alive and the client is not answering; the load itself is **undetermined** in that window, and the docstring says so rather than letting a reader assume. Acceptance question answered **yes and asserted**: `test_a_watchdog_can_tell_rung_four_loading_a_save_from_a_wedged_harness` drives the real `RecoveryEngine` over the real `LuaSaveLoader` and pins which rung, which step, and how far into which bound (`elapsed_s` against the phase's own `timeout_s`, not the watchdog's ceiling). Three negative controls plus the ratchet's stale arm observed; both `owner: UNRESOLVED` allowlist entries deleted, verified by `git show HEAD:` returning nothing. No bound, poll interval or deadline was changed -- the waits are exactly as long as they were.)* **Make it legible, do not make it shorter to fit a threshold.** No bound, poll interval or deadline was changed; the waits are exactly as long as they were and are now visible. Shortening a real wait to satisfy a watchdog would have been the fix manufacturing its own confirmation.
- [X] T302 Make every phase that can outlast the watchdog's silence budget emit, and publish the set as data, per **Constitution Principle VII** and the owner's three-minute liveness criterion (missing). **HIGH.** *(Landed 2026-09-22 `33348e3`. Allocated this id retroactively: the shipped modules cited **T290**, which is a different task about unobserved success criteria, so the work was invisible to every tally -- a board and a tree disagreeing, which is the defect this lane spent the day fixing in other people's records. All 21 citations across 7 files re-pointed.)* `tests/contract/test_long_phase_liveness.py` publishes six phases as data with `WATCHDOG_SILENCE_BUDGET_S = 180.0` named once, each asserted to emit, allowlist entries required to cite a task id **and** name an owning lane, stale entries failing the moment their phase starts emitting, twelve negative controls, plus nine behavioural tests (`confirm_execution` at 200 s/2 s emits exactly 101 records, max inter-record gap 2.0 s). **The criterion is what a watchdog can read from outside**, which a store row is not: `provider/chain.py` recorded retries as `RunEvent`s that reach the **store, not the log**, and was the worked example of that confusion -- `resilience/recovery.py` is the second instance (T301). Fixed: the in-flight provider call (measured **146 s**, in a package that had **no logging at all**, confirmed two ways) and `confirm_execution`'s 200 s poll. Revert-confirmed both ways against real source, byte-identical after restore. **Registered limitation, and it matters more than the fix:** the provider emitter is a daemon thread ticking *beside* a blocking POST, so it is **observer-derived, not work-derived** -- a ticker beside a hung socket ticks forever, and `provider.call.waiting` is emitted whether the call is waiting, hung or dead. That is **the second named defect shape inside the fix for the first**. Normative consequence now in the module docstring: a work-derived signal (bytes arriving) may clear a silence bound; an observer-derived one may **not**, and nothing downstream may treat these ticks as clearing one. **The approved real fix is to stream the provider completion so a chunk arriving is the event**, which makes the signal work-derived and removes the tick interval rather than tuning it. Tracked as the successor to this task.

## Phase 15: Directed-goal prerequisites -- ingredients vs. offer (appended 2026-09-22 by the headless live-lane fix for `use_a_builder` / `found_second_city`)

A completed enumeration of every shipped goal (`tests/live/goals/*.yaml`) asked one question of
each `prerequisites` block: does it ask the game **what is offered**, or ask whether **the
ingredients exist**? Ten of thirteen already do the former, all ten the same way (a
game-computed availability field: `available_productions`, `available_beliefs`,
`researchable_techs`, `movable_count`, `promotions_available_count`, `prompt_options`,
`available_policies`, or -- `select_city_then_unit` / `save_named_game` -- the action's own
availability_predicate quoted directly, which for those two actions IS a bare count/turn check).
`use_a_builder` and `found_second_city` were the other two of the three written in the ingredients
idiom; both fixed in this pass (2026-09-22, `use_a_builder`'s prerequisites now consult
`player.units.selected_available_builds`; `found_second_city`'s stay `settler_count >= 1`,
documented as a confirmed observation-surface gap rather than papered over -- no field reports
whether a legal settle site is *reachable*, only whether the unit's current plot is legal).
`live.goal_run.AVAILABILITY_FACT_NAMES` publishes the field set as data;
`tests/unit/test_goal_prerequisites_availability.py` is the ratchet, with a negative control
proven to fail it. The third of the three -- `send_delegation` -- is the confirmed-broken goal
below, deliberately left unfixed. `slot_policy` and `build_a_builder` are a **different, unproven**
concern surfaced by the same read-through: **do not assert they are broken.**

- [ ] T303 Make `send_delegation`'s prerequisites ask whether a delegation is actually offered, not merely whether the player is met-and-solvent, per the same criterion as `use_a_builder`/`found_second_city` (2026-09-22, confirmed broken). **HIGH.** `tests/live/goals/send_delegation.yaml`'s prerequisites are `player.met_civ_count >= 1` and `player.gold >= 25` -- both ingredients, in the exact idiom the enumeration flagged. Neither is the game telling the harness "you may send a delegation now": a relation already carrying a delegation (`other_player.has_delegation` true from a prior send) or a relation at war both leave a met, solvent player reading as feasible while `diplomacy.send_delegation` is refused. CONFIRMED (not uncertain, unlike T304/T305 below): this is the same ingredients-only shape `use_a_builder` and `found_second_city` were both found and fixed in this pass, just left out of that commit per the live lane's own scoping for the day. Before changing the prerequisite, read what `diplomacy.state` actually exposes per relation beyond `has_met`/`has_delegation` (`catalogs/observations/diplomacy.yaml`) for a field that answers "offered", such as a war-state or an existing-delegation flag the goal could gate on directly; if none exists, this is a second, separate observation-surface finding to report rather than paper over -- same rule as `found_second_city`. Registered in `live.goal_run.KNOWN_AVAILABILITY_EXCEPTIONS["send_delegation"]` so the ratchet's exclusion of this goal is a reviewed, visible line rather than a silent gap.
- [ ] T304 Read what populates `slot_policy`'s `available_policies`, then decide whether its prerequisite is sufficient, per FR-013-adjacent goal-authoring hygiene (UNCERTAIN -- 2026-09-22). **MEDIUM.** `tests/live/goals/slot_policy.yaml` checks `player.available_policies != []` and `player.current_government != null`, which already consults a game-computed offer field (`government.state.available_policies`, `catalogs/observations/government.yaml`) -- unlike `send_delegation`, this is NOT the ingredients-only pattern and the enumeration does not classify it as broken. The open question is narrower and unverified: `available_policies` being non-empty says a policy card *exists* to slot, but the goal's action needs an *empty slot* to put it in too, and nobody has read the Lua/observation body to confirm whether a full slot set is even reflected anywhere the harness can see, or whether `available_policies` itself already excludes cards that have nowhere to go. **Do not assert this goal is broken** -- read `catalogs/observations/government.yaml`'s backing and, if there is live or store evidence, what `available_policies` actually reported when every slot was full, before deciding whether a change is needed at all.
- [ ] T305 Read what populates `build_a_builder`'s `available_productions`, then decide whether its prerequisite is sufficient, per FR-013-adjacent goal-authoring hygiene (UNCERTAIN -- 2026-09-22). **MEDIUM.** `tests/live/goals/build_a_builder.yaml` checks `player.cities.available_productions != []`, which -- like T304 -- already consults a real offer field (`cities.state.available_productions`) and is not the ingredients-only pattern. The open question: the goal needs specifically `UNIT_BUILDER` to be among the offered items, not merely that the city is offering *something* (it could be offering only buildings or districts on a given turn, with no unit in the list at all, and the prerequisite would still read as met while the actual objective is infeasible). **Do not assert this goal is broken** -- read a real or fixture `cities.state.available_productions` list to confirm whether `UNIT_BUILDER` is reliably present whenever the list is non-empty (early game, likely yes) before deciding whether `'UNIT_BUILDER' in player.cities.available_productions` is worth tightening to.

## Phase 16: The phantom-predicate-field sweep (appended 2026-09-22 by the headless static-analysis task investigating `diplomacy.declare_war`)

A live stage today recorded a `declare_war` that actually landed (and ended the game -- the
capital was captured) as `rejected`. Root cause: `diplomacy.declare_war`'s and
`diplomacy.make_peace`'s `availability_predicate`/`verification_predicate` all read
`other_player.diplomatic_state`, a name `catalogs/observations/diplomacy.yaml`'s own
`output_schema` declares but `lua/gamecore/diplomacy.lua` never actually emits in the real JSON --
**an output_schema/Lua-body disagreement, itself a finding**: `Player:GetDiplomaticAI()` is
MEASURED absent in `GameCore_Tuner` (that file's own header, 2026-09-20), the GameCore-side
fallback (`player:GetDiplomacy():GetDiplomaticStateIndex`) is marked UNVERIFIED, and a Lua table
constructor silently drops a `key = nil` entry rather than emitting `"key":null`. CONFIRMED
empirically, not by inspection alone: read-only queries against `civsim-match-store.db`'s and its
`.bak-20260921T152108Z` sibling's `decision_steps.bundle_json` (670 + 115 real captured bundles
from today's and yesterday's live runs, including 670 `has_met: true` relations for a real met
civilization, `CIVILIZATION_AUSTRALIA`) found the key union of every `diplomacy.state.relations[]`
entry ever actually captured is exactly `{player_id, has_met, civilization, has_delegation}` --
`diplomatic_state` appears in **zero** of them. `_resolve_attribute`
(`civsim_harness.act.predicates`) resolves an absent key to `None` rather than raising, by design
(so an ordinary "not available now" can be reported instead of a crash), which is exactly why this
defect shape is invisible at runtime: `evaluate_predicate` never raises for it, `declare_war`'s
availability reads `True` forever (the sampler believes war is always declarable) and its
verification reads `False` forever (a war that lands is recorded `rejected`).

A full sweep of every one of the 41 declared actions in `catalogs/actions/*.yaml` and all 13 goals
in `tests/live/goals/*.yaml` found exactly **one other** instance of the identical observable
symptom (a predicate identifier that parses and binds but can never resolve to a real value),
independently discovered by the same method, one layer down in the binding code rather than the
Lua body: `camera.move`/`camera.zoom`/`camera.set_view_mode`'s verification predicates. No other
declaration's predicate identifiers, and no goal's `success`/`prerequisites` identifiers (already
gated by `tests/live.goal_run.validate_predicate` against `known_fact_names` at goal-load time --
confirmed by actually loading all 13 goals; none raised), reference anything unresolvable.

**The ratchet**: `civsim_harness.act.predicates.known_predicate_fields` publishes the field set
each predicate namespace can actually bind to, derived mechanically from the catalog's own
declared `output_schema`s through the exact tables `build_predicate_bindings` itself binds from --
never hand-typed, so it cannot drift from what the evaluator does. `KNOWN_SCHEMA_BODY_DISAGREEMENTS`
publishes the one confirmed schema/body disagreement (`diplomacy.state`'s `diplomatic_state`).
`KNOWN_PHANTOM_PREDICATE_FIELDS` publishes the five declarations (of 41) whose predicates are
known, today, to reference an unresolvable field, each cited to its own reason.
`tests/unit/test_predicate_field_ratchet.py` is the ratchet: every non-exempt declared action's
predicates must resolve; a synthetic negative control (`other_player.frobnicate_the_treaty`, not
in the exception table) is proven, by actually invoking the checker, to raise -- observed failing
before this was committed, message: *"synthetic.never_declared_action: predicate references
other_player.frobnicate_the_treaty, which no observation body emits. A predicate that references a
field no observation body emits can never be true, so the action it guards will be recorded as
refused even when it works."* A second synthetic positive control (real fields only) is proven not
to raise, so the ratchet is shown able to pass as well as fail.

`diplomacy.declare_war`/`diplomacy.make_peace` are **NOT fixed in this pass** -- see T307: no
honest, guardable accessor was confirmed without a live client to verify a new/relocated engine
call against, and `specs/002-civ-playing-harness/spikes/client-segfault-2026-09-21.md`'s own rule
is that an engine call unverified live "may crash the client". `camera.*`'s binding gap is
similarly reported, not fixed (T308) -- both out of this task's authorised scope, which named only
`declare_war` as a fix target and this sweep as the deliverable.

- [ ] T306 (tracking/no-op -- see Phase 16 header above for the full account) Land the phantom-predicate-field ratchet across every declared action, per the defect that let `diplomacy.declare_war` record a landed war as `rejected`. **DONE, this pass.** `civsim_harness.act.predicates.known_predicate_fields`/`KNOWN_SCHEMA_BODY_DISAGREEMENTS`/`KNOWN_PHANTOM_PREDICATE_FIELDS`/`assert_predicate_fields_resolve` and `tests/unit/test_predicate_field_ratchet.py` (8 tests: the action-catalog ratchet with its negative and positive synthetic controls, plus two more exercising `live.goal_run.validate_predicate`'s pre-existing goal-side field-resolution gate and giving IT a negative control it did not previously have) are the artifact; nothing further to do here. Kept as its own numbered entry, not folded into T307/T308, so the ratchet itself is cited by its own id the way `civsim_harness.act.predicates`'s new code already does in-line, rather than only by prose.
- [ ] T307 Give `diplomacy.declare_war`/`diplomacy.make_peace` a real at-war field, guarded the way Firaxis guards it, per FR-011 and the proven live defect above (**PROVEN, not fixed**). **HIGHEST -- this is the project's own scoring metric.** `other_player.diplomatic_state` is absent from the `GameCore_Tuner` context's emitted JSON (see Phase 16 header) -- **not proven structurally unverifiable; proven absent from the tuner context, with the InGame context unprobed.** Both predicates were left in `KNOWN_PHANTOM_PREDICATE_FIELDS` rather than patched blind, on this task's own explicit instruction that an honestly-unsatisfiable predicate beats one weakened to pass, and because that InGame probe is client-gated and out of a static-analysis task's reach. **The most promising direction, read but not attempted live**: `lua/gamecore/diplomacy.lua`'s own header already names the shape of the fix and the precedent for it. `Player:GetDiplomaticAI()` is documented in that same file as "the UI-side accessor (InGame)" -- absent specifically in `GameCore_Tuner`, the context `diplomacy.state` currently runs in. `government.state`/`great_people.state`/`religion.state` were moved from `GameCore_Tuner` to `InGame` for exactly this reason in the client-segfault-2026-09-21 fix (`specs/002-civ-playing-harness/spikes/client-segfault-2026-09-21.md`, "The guard patch"), after `cities.state` set the same precedent in `1d0b372`. The diplomacy file's own header already flags this tension and already names where the moved logic belongs: "anything that turns out to require the InGame UI state at runtime must be re-declared against `lua/ingame/diplomacy.lua`'s read side instead of this file." This is not inventing a new engine call -- `diploAI:GetDiplomaticStateIndex(otherID)` is already written, already reachable from the code that would move -- so it is lower-risk than the new accessors the segfault post-mortem was written about, but it is still an unguarded-context change of the exact shape that crashed the client that same day, and MUST NOT be attempted without a crash-watched live block per that spike's own rule ("every new engine call carries... the guard that call site sits behind... and 'unverified live' is read as 'may crash the client' until a crash-watched block has run it"). Concretely: move `diplomacy.state`'s relevant read (or add a second, InGame-context read alongside the existing GameCore_Tuner one) to `lua/ingame/diplomacy.lua`, retry `GetDiplomaticAI()`/`GetDiplomaticStateIndex` there, keep every existing pcall/nil guard, and confirm live with a dedicated declare_war/make_peace block before this task's exception-table entries are removed. Until then, `diplomacy.send_delegation` is unaffected (it does not reference `diplomatic_state`) and remains the only diplomacy action this catalog can honestly verify.
- [ ] T308 Wire the `camera` predicate namespace to `camera.read_state`, per the same defect class as T307, independently found by the T306 sweep (**PROVEN, not fixed**). **HIGH.** `civsim_harness.act.predicates.build_predicate_bindings` hardcodes `"camera": {}` unconditionally; `catalogs/observations/camera.yaml`'s `camera.read_state` (added T221, backed by `lua/ingame/camera.lua`) was never wired to it, so `camera.move`/`camera.zoom`/`camera.set_view_mode`'s own `verification_predicate`s (`camera.target_plot == target and camera.target_is_revealed`, `camera.zoom == target`, `camera.mode == target`) can never be true regardless of the real camera state -- the same "recorded as refused even when it works" symptom as T307, confirmed by reading the binding code (`known_predicate_fields`'s `fields["camera"] = frozenset()`, observed via `civsim_harness.act.predicates.known_predicate_fields`), not merely suspected. Unlike T307 this needs no new or relocated engine call: `camera.read_state` already reliably backs these fields (per that declaration's own header, `mode`/`target_is_revealed` are `required`). The fix is Python-only: extend `build_predicate_bindings` to source the `camera` namespace from `index.get(DeclarationId("camera.read_state"))` the way `game`/`player` are already merged from their own `_GAME_SOURCES`/`_PLAYER_SOURCES` tables, then remove `camera.move`/`camera.zoom`/`camera.set_view_mode` from `KNOWN_PHANTOM_PREDICATE_FIELDS` and confirm `tests/unit/test_predicate_field_ratchet.py` still passes with the narrower exception table. Not attempted in this pass: out of this task's authorised scope (only `declare_war` was named as a fix target) and this task's remaining time was better spent completing the sweep and the ratchet than a second unreviewed `src/` change.
- [ ] T309 (checked, closed -- no fix needed) Confirmed live whether `units.build_improvement`'s `charges_remaining` accessor ever actually returns a number on this build, per the same defect class again (**CHECKED, found fine -- not a defect**). `lua/gamecore/units.lua:270`: `entry.charges_remaining = unit.GetBuildCharges and unit:GetBuildCharges() or nil -- UNVERIFIED`. Raised as a suspicion during this sweep: if `unit.GetBuildCharges` is absent on this build, the Lua table constructor elides the key exactly as `diplomatic_state` did -- but the schema already declares the field nullable (`type: ['number', 'null']`), so this is NOT flagged by the T306 ratchet (a nullable field legitimately elides when unset; the ratchet checks name resolution, not "is the value ever non-null"). **RESOLVED by checking, same session**: read-only query of `civsim-match-store.db`'s real `decision_steps.bundle_json` (the same method the diplomacy finding used) found 86 real Builder-unit entries across today's captures, and `charges_remaining` was non-null in all 86 -- the accessor does resolve on this build. Left as a closed line rather than deleted so the question and its answer are both on record; no further action.

- [ ] T310 Publish the fabrication-polarity list: every verification predicate where an ABSENT field reads as TRUE, not FALSE, per the general form "absence and unobservability must not share a representation" (spec 004) -- **PROVEN by static AST analysis; whether it has ever actually fired against a real absent field (beyond the one confirmed case, `diplomacy.make_peace`) is UNCHECKED.** **HIGHEST -- ranked above T307/T308's own fixes, and intentionally not fixed in this pass.** `_resolve_attribute` resolves an absent namespace field to `None` (never raises), so every `verification_predicate` in the catalog falls into exactly one of two families depending on its polarity: **absence -> False** (the action is recorded `rejected`/`verification_failed` -- wrong, but visible, recoverable, and the shape every other finding in this sweep already covers), or **absence -> True** (the action is recorded `applied` for a no-op, or for a subject that no longer exists -- invisible, and nothing downstream can tell a fabricated success from a real one). A second static pass over all 41 action declarations, built for this addition, found the full **absence -> True (fabricates)** list -- every verification predicate of the shape `X != "<non-null literal>"`, `target not in X`, or `not X` / `not (target in X)`, where `X` is a namespace attribute that can genuinely be absent (subject vanished entirely, e.g. captured/killed/consumed/recruited-by-someone-else, or the field was never populated for another reason): `diplomacy.make_peace` (`other_player.diplomatic_state != "war"` -- the confirmed member, and harmless TODAY only because its own availability_predicate reads the same always-absent field first and refuses -- see T307's required ordering), `cities.purchase_with_gold` (`not (target in city.purchasable_with_gold)`), `cities.purchase_with_faith` (`not (target in city.purchasable_with_faith)`), `policies.slot_policy` (`not (target in player.available_policies)`), `policies.assign_governor` (`not (target in player.available_governors)`), `religion.select_belief` (`not (target in player.available_beliefs)`), `units.promote` (`not (target in unit.available_promotions)`), and **11 of the 13 `prompts.*` actions** (`game.current_screen != "prompt.<id>"`: `unit_promotion`, `pantheon_selection`, `great_person_selection`, `declare_war_response`, `congress_vote`, `congress_intro`, `era_transition`, `era_dedication`, `tech_civic_completed`, `boost_unlocked`, `great_work_created`) plus `prompts.ai_diplomatic_approach` (`target not in prompt.options`, the `ast.NotIn` form, which explicitly reads `True` when the collection itself is `None` -- confirmed from `_apply_comparison`'s own code: `bool(right is None or left not in right)`). **Two declarations that LOOK like the same shape are confirmed SAFE, and both cited so the distinction is on record, not assumed**: `espionage.assign_mission`'s `spy.mission != null` compares against the LITERAL `null`, not a sentinel string, so a genuinely-unset mission and an absent subject both read `None != None -> False` -- the safe direction, by construction; `units.found_city`'s `not unit.exists` and `great_people.recruit`'s `not great_person.is_recruitable` are DELIBERATELY reading "the subject vanished" as success, which is the correct semantics for those two specific actions (a founded settler is consumed; a fully-recruited great person may legitimately leave the recruitable list), not a bug. **NOT FIXED in this pass, on explicit instruction: enumerate first, publish the list, fix later.** Whether the store already contains a fabricated `applied` record from any of these is a separate, harder question (would need cross-referencing each hit against whether its subject actually vanished at that step) and is out of this task's time box; this list is what makes that audit possible. **Required ordering for T307 specifically, folded in here rather than only there**: fix the predicate's own absence-vs-false handling (e.g. require the subject to still `exist` before evaluating the polarity-sensitive clause, or add an explicit `and other_player.exists` conjunct) BEFORE any InGame-context probe reintroduces real (non-permanently-absent) values into `diplomatic_state` -- fixing the context without first fixing the polarity converts an unreachable action into a silently lying one.

- [ ] T311 Extend T310's fabrication-polarity list to the LEFT operand too, not just the observation field on the right -- **CONFIRMED LIVE, in the project's own headline claim of the day**, found by re-running T310's own criterion against a live-lane claim rather than a theoretical case. T310 asked "can the field referenced resolve to `None`"; it did not ask the same question of `target`, the OTHER operand every `in`/`not in` comparison has. `x not in y` reads `True` when `x` (not `y`) is `None` -- `_apply_comparison`'s own `ast.NotIn` branch is `bool(right is None or left not in right)`, and `None not in [...]` is `True` for ANY non-`None`-containing list regardless of what is in it. **Live evidence, found by the live-lane agent applying this exact criterion to the day's most-cited result**: `prompts.ai_diplomatic_approach`'s `target not in prompt.options` recorded `outcome: applied` with `target: null`, `prompt.options: ["Goodbye"]` (present, not absent), confirmed on the FIRST verification attempt at 0.0 s elapsed -- while the SAME step's `dispatch_result` read `still_in_conversation: true`, i.e. the conversation the action was meant to resolve was, by the harness's own other evidence, still open. Scoped across all steps in the (now-merged) store: this exact shape -- this predicate, evaluated against a null target -- occurs exactly once, and zero `applied` results for this action carry a real (non-null) target. This was the day's own cleanest-looking result and nobody had checked it against this axis before T310 existed. A second static pass (this task, extending T310, not reopening its fix scope) found seven `verification_predicate`s where `target` is the left operand of `in`/`not in`: `target in city.production_queue` (`cities.set_production`, bare `in`, absence-safe: `None in X` -> `False`, under-reports) and, wrapped in `not (target in X)` or as a bare `not in` (both fabricate on a null target the identical way): `cities.purchase_with_gold`, `cities.purchase_with_faith`, `policies.slot_policy`, `policies.assign_governor`, `religion.select_belief`, `units.promote` (all six ALREADY on T310's list for the right-operand/field-absence axis -- now confirmed independently vulnerable on this second, left-operand axis too, which matters because a fix that only guards the field side, e.g. `and other_player.exists`-style, does not address a predicate whose OWN target argument arrives null), and `prompts.ai_diplomatic_approach` (not previously on T310's list at all, since `prompt.options` itself is essentially never absent in practice -- this is a genuinely separate finding T310's field-only criterion could not have caught). **Still enumeration only, per the same instruction T310 was built under**: not fixed here. The general criterion for any future fix: **for every predicate, check whether EITHER operand of a comparison can be null/absent, and record which way each side's nullness resolves the whole expression** -- a field-only audit (T310 as first published) is incomplete on its own, as this task's own live hit demonstrates.

## Phase 17 — client admission control (2026-09-22, from two live collisions)

- [ ] T312 **Make acquiring the run lock and starting the run a single atomic operation**, per the two collisions observed live on 2026-09-22. **The lock file works; the convention around it does not.** Every lane today followed the documented procedure — check `/tmp/civsim_harness/run_locks/`, see it empty, start — and **two lanes still collided, both inside windows where someone had just verified the board was free** (one a **100-second** window: board confirmed clean at 11:44:02, a lane's own block launched at 11:45:44 found a lock already held with three driver processes live). **This is a check-then-acquire race, not a discipline failure**; instructing lanes to check more carefully only moves the collision later, and the lane that held the board won by arriving first rather than by having the better claim.
  **Scope:** replace the read-then-write sequence with an **atomic create-or-fail** on the lock path (`O_CREAT|O_EXCL`, or equivalent), so that **the check *is* the acquisition** and a losing caller is told it lost rather than discovering it later through `tuner unreachable [Errno 111]`. The loser must get a **named refusal identifying the holder** (run id, client pid, acquired-at — the lock file already carries all three), not a generic connection error minutes into a run.
  **Interacts with, and does not duplicate:** `RunIdentityLock.guard`'s `finally` (T227), the `atexit` process-exit backstop and the `SIGTERM` handler (T289 line) — those guarantee a lock is *released*; this guarantees only one caller ever *takes* it. And `run/orphans.py`'s sweep, which repairs a lock whose holder is gone. **Four mechanisms, four distinct failures; a reviewer should not close any of them as a duplicate of another.**
  **Note:** the hypervisor acting as admission control (ask → grant → check the lock as a second safety → go, one block per grant) is a **coordination workaround in force from 2026-09-22, not a substitute for this task.** It evaporates the moment any lane acts without asking, and it cannot bind a process that is not talking to the hypervisor. Also observed the same day: one run wrote into a block output directory another stage was already using — the same race in a second resource, worth fixing in the same pass.
- [X] T313 Teach `civsim_web`'s completeness interface the `in_flight` value, per 003 FR-019 and **Principle III** (partial). **LOW, one line plus a docstring.** `src/civsim_web/viewmodels/base.py:237` has `_KNOWN_INCOMPLETE = ("has_gaps", "unknown")`. `in_flight` (T298) is **correctly refused** -- `derive_trend_eligibility` is fail-closed on `status != "complete"`, so nothing leaks into a trend -- but it is *additionally* tagged `COMPLETENESS_UNRECOGNIZED`, which is honest today (that interface genuinely has not learned the value) and becomes misleading the moment someone reads the tag as a defect rather than as a vocabulary gap. Worth doing for the contrast it draws: this interface was **fail-closed** and so a new status was refused safely, while `store/trends.py`'s gate for the same concept was **fail-open** and would have admitted it. Same value, two consumers, opposite default -- which is the argument for the direction of a default being an explicit decision rather than whatever the first author happened to write. *(Landed 2026-09-22 by the lane lead. `_KNOWN_INCOMPLETE` now names `in_flight`, and the comment records the contrast that produced the fix rather than just the fix. **The gate was already correct**: `derive_trend_eligibility` keys on `status != COMPLETE` and uses this tuple only to *name* the reason, so `in_flight` was refused safely the moment it existed -- the tuple was a stale **vocabulary**, not a stale **guard**, and the `COMPLETENESS_UNRECOGNIZED` tag was honest about this interface while misleading to anyone reading it as a defect in the run. The contrast is the reason this was worth doing at all: the same new status met **two consumers of the same concept with opposite defaults** -- this one fail-closed and refusing it with no code change, `store/trends.py::exclusion_for` fail-open and about to **admit** it into a Principle III trend (fixed under T298). **A default's direction has to be a decision, never whatever the enumeration happened to omit.** `tests/contract/test_web_read_api.py`: 153 passed; ruff clean.)*
- [ ] T314 Give the predicate-field ratchet a companion that asks whether a declared field has **ever carried a value**, per **Principle I**, FR-011 and the `research.set_civic` defect closed at `0989e3b` (missing). **HIGH.** `assert_predicate_fields_resolve` (`src/civsim_harness/act/predicates.py:988`) **passes for `researchable_civics`, and passes correctly** -- it catches a field **no body emits**, and this field was emitted by **every** body, always as `[]`. **A ratchet over PRESENCE cannot see a field that is present and always empty.** The action was undraftable for the entire history of the harness -- `[]` on **914 of 914** stored decision-step records and 18 of 18 block probes -- and every guard we own reported clean, because each was answering a question the defect did not fail. This is a new instance of the project's allowlist pattern rather than a repeat: the check could not return "I do not know", so its silence read as an all-clear.
  **Scope:** a companion check over the store asking, for each declared predicate field, whether it has **ever** been non-empty across all recorded observations. **The hard part is the honest answer for a field whose subject has never occurred**, and it is the whole design: a field about a unit type never built, a screen never opened, or a civ never met is legitimately empty forever, and reporting it as a defect would make the check noise that gets allowlisted down to nothing -- **this project's defining defect wearing the costume of its fix** (the T285 precedent, where three formulations of a broad scan were built and rejected for exactly that). So the check MUST have a **third answer**: `never non-empty AND its subject never occurred` is **undetermined**, not a failure. Build the negative control first, from a field known to be legitimately-always-empty, and refuse to ship the check if it flags that one.
  **Interacts with, does not duplicate:** T306 (phantom predicate fields -- a field **no** body emits), T310/T311 (fabrication polarity -- which way an **absent** field resolves). **Three checks, three distinct blindnesses; a reviewer should not close any as a duplicate.** T310/T311 ask *what absence means*; T306 asks *is it declared and never emitted*; this asks *is it emitted and never populated*.
  **Note the measurement this makes possible:** a field that has never carried a value across 914 records is a stronger signal than any static read of the Lua, because the two halves of `research.lua` were byte-for-byte parallel and **only the engine symbol table could tell them apart**. The store knew for months.
- [ ] T315 Make a turn cycle's record show **every** backstop trip it contained, not just the last, per FR-014, SC-022 and **Principle III** (contradicts). **MEDIUM.** `NoProgressTracker` is constructed as a **per-invocation local** at `src/civsim_harness/run/decision_loop.py:674`, and `run_decision_loop` is invoked **twice** for a turn cycle's first attempt -- once by `_clear_blocking_prompt_before_save` (`run/turn_cycle.py:589`) and once by the main attempt (`:875`) -- while `turn_cycle.py:876-881` concatenates both step lists into **one `TurnCycleRecord` under one `turn_cycle_id`**. So one recorded turn can run **two complete 8-step ladders** and publish `final_no_progress_streak: 8`.
  **Measured, read-only, from the live store:** `turn_cycle 6ade3ee5` (`run-1f2c5d5e`, turn 4) carries **16 consecutive rejected `turn.end_turn` steps** under `limit=8` -- streak climbing 1..8, then restarting at 1 and climbing to 8 again. `turn_cycle bc8e3a2e` repeats the shape over 20 steps. Store-wide: **61** `turn_ended_on_no_progress` events across **54** distinct `(run, turn)` pairs, **7 turns tripped the backstop twice**, against only **53** cycles carrying `outcome=ended_on_no_progress`. **8 trips are represented by no turn cycle outcome at all.**
  **The second ladder is intended** -- `turn_cycle.py:583-584`'s docstring anticipates it ("a prompt that will not clear trips the backstop here; the save is still attempted afterwards") -- **so this is a reporting defect, not a control-flow one, and the fix must not end the clearance path's ladder early.** The whole reason `no_progress.py` exists is that a stuck agent be **visible in the record** (FR-014); a turn that stalled twice and reports one trip of 8 **under-reports the very thing the mechanism exists to expose.** Fail-safe direction, in the mechanism most trusted to surface stalls.
  **This is the same shape as `65bc562`** (a bound living in a local, so re-entering the function granted a fresh allowance) found independently in a second module on the same day -- **two instances make it a pattern worth a structural sweep**, not two fixes.
  **Needs no number.** It is the counter's *lifetime* and its *record*, never its threshold; do not retune `no_progress_step_limit` under this task.
  **Related gap, cheap to close in the same pass:** `NoProgressTracker` has **zero direct tests** (`grep -rn "NoProgressTracker" tests/` returns nothing; the pattern matches 4 times in `src/`), while its own docstring names `tests/unit/test_turn_state_machine.py` and `tests/integration/test_turn_endings.py` as covering it. Neither instantiates it. **A docstring standing in for a test, which is this project's recurring finding applied to its own guard.**
- [ ] T316 Give `RecoveryEngine` the lifetime its own docstring claims, per **FR-048** and **Principle VII** (contradicts). **MEDIUM, third instance of the pattern.** `resilience/recovery.py:154-162` states the contract outright: *"One instance per active run: it is the sole keeper of that run's consecutive-failed-recovery count (FR-048), **which must survive across separate interruptions within the same run**."* It does not. `run/composition.py:1550` constructs a **fresh `RecoveryEngine` inside `build_turn_dependencies`**, which `run/runner.py:922` calls **once per turn** inside `_play_run`'s loop -- so `_consecutive_failures` (`recovery.py:195`, `+=` at `:480`, compared at `:481`) returns to zero at every turn boundary. Only a *successful* recovery is supposed to reset it (`recovery.py:350`); a turn boundary is an undocumented reset the contract does not sanction.
  **The contrast is visible inside one expression, which is what makes this reviewable rather than subtle:** `composition.py:1550` re-mints the recovery engine per turn while `composition.py:1566` -- **sixteen lines below, in the same `TurnCycleDependencies(...)` literal** -- passes `detection=ctx.detection`, the run-scoped `DetectionWatch` built once at `composition.py:1127`. Two collaborators holding run-lifetime counters side by side, one reused and one re-minted. **Fix: build it once per run alongside the other `_RunContext` members and pass `recovery=ctx.recovery`, mirroring the line below it.** No new cap, parameter or constant -- it moves an existing bound to the lifetime it already claims, so there is no I16/FR-014 exposure.
  **Severity is deliberately not High, and the honest reason is that the consequence is UNDETERMINED:** `recover()` re-raises every sub-limit failure (`recovery.py:367-368`), so the first failed recovery escapes `run_turn_cycle` and `runner._handle_run_failure` pauses the run before a second turn can begin. **Nobody has constructed a path where the renewal changes an outcome.** Resolve with a test driving `recover()` to fail below the limit without the error escaping the turn, or by establishing from `Runner.resume_from` whether an engine is expected to outlive a pause. **Do not close this as "no impact" without that lookup** -- the mismatch and the contradicted docstring are verified regardless.
- [ ] T317 Establish whether `recovery_attempt_limit` can **ever** fire, per **FR-048** (UNCERTAIN -- found by the T316 sweep, 2026-09-22). **MEDIUM, and it is a different question from T316.** Independently of the per-turn renewal: every failed recovery **re-raises** (`resilience/recovery.py:367-368` -- `await self._on_recovery_attempt_failed(...)` then a bare `raise`) and every success **resets the count to 0** (`:350`). If both hold on every path, `_consecutive_failures` is permanently 0-or-1 and **the configured limit is inert -- a bound that cannot be reached, guarding nothing.**
  **This is the fail-safe direction in the shape this project keeps finding it:** a limit that never fires produces no incident, no error and no log line, so nothing will ever make it visible. It sits in the resilience layer, which is exactly "a fail-safe defect survives longest in the mechanisms we trust most".
  **Scope: establish reachability first, decide second.** Enumerate every path out of `recover()` and say for each whether it increments without escaping. Three honest outcomes and the task must say which: (i) the limit is reachable and the code is fine; (ii) it is inert and the right fix is to make failures accumulate; (iii) it is inert and that is **correct**, because a single failed recovery is already terminal -- in which case **delete the limit rather than leave a knob that reads as a live control**, and say so in `contracts/run-configuration.md`. **A configured number that cannot change any behaviour is worse than no number, because a reader budgets against it.**
- [ ] T318 Close the **third** fabrication axis -- `==` against a NULL target -- per **Principle I**, FR-011 and the T310/T311 lineage (**PROVEN, and newly reachable**). **HIGH.** T310 enumerated `X != <literal>`, `not (x in y)` and `not X`; T311 extended it to the **left** operand of `in`/`not in`. **Neither asked about `==`, where a null `target` makes `None == None` resolve TRUE and a no-op records as `applied`.** Two declarations have that shape: `research.set_civic` (`player.current_civic == target`) and `policies.change_government` (`player.current_government == target`).
  **Both are guarded today ONLY BY ACCIDENT**, and one accident has just been removed. Their availability predicates are `target in player.researchable_civics` / `target in player.available_governments`, and both backing fields were always `[]`, so `None in []` was False and neither action could be drawn. **`0989e3b` populates `researchable_civics`.** The moment it does, `set_civic` becomes draftable and a decision arriving with `parameters: {}` -- **the exact shape the model produced 56 times for `units.found_city`** (`src/civsim_harness/act/predicates.py:532`) -- reaches a verification predicate that reads `applied` for a no-op. **The class-A fix and a new fabrication land in the same action**, which is loop-state's own warning verbatim: *fixing the context without fixing the predicate shape converts it into a silent liar.*
  **Prefer the CENTRAL fix over two YAML edits:** make a null-target comparison **unevaluable in the evaluator** -- `verify_execution` already maps unevaluable to False -- so the property holds by the absence of an edge rather than by every future declaration author remembering a guard conjunct. That also covers the six already on T310/T311 and every declaration not yet written. A per-declaration guard is the "a rule is not a control" failure in its usual costume. **Whatever is chosen, it must keep a positive control: a correctly-supplied target that genuinely matches must still resolve True**, or the action has been broken rather than fixed.
- [ ] T319 Establish why `available_builds` is EMPTY for a selected Builder holding three charges, per FR-013 and **Principle I** (UNCERTAIN -- found by the reachability sweep, 2026-09-22). **HIGH, and it is the most valuable unmeasured thing in the never-attempted set.** A Builder **did** exist: 121 unit-entries carry `charges_remaining: 3`. `available_builds` is emitted only when `entry.is_selected` (`lua/gamecore/units.lua:276-288`), so it is legitimately absent on 107 of them -- but on the **14 selected-Builder steps it is present and `[]`**, and the production availability replay gives the reason *"nothing to choose: unit.available_builds is empty"* on exactly those 14.
  **The class is A (emitted, present, always empty). The CAUSE is undetermined and must not be guessed** -- three candidates and the task must say which: the unit panel's build row is not populated in the InGame Lua state unless the panel is open; `CanStartOperation(BUILD_IMPROVEMENT)` is answering false for a real reason; or the plot genuinely offered nothing on those 14 steps (only 14 samples, possibly all one plot). **T309 closed only the `charges_remaining` half of this action and explicitly did not check `available_builds`; no open task covers it.** This is precisely the shape T314's companion ratchet exists to surface, and it is the best available test case for building that ratchet against a real instance rather than a synthetic one.
- [ ] T320 Give the dispatcher a way to express a MULTI-ARGUMENT action, per FR-008 and **Principle II** (missing -- found by the reachability sweep, 2026-09-22). **MEDIUM.** `act/executor.py::_build_arguments` supplies sorted non-`target` parameters and then `target` last, and three declarations need arguments that convention cannot produce -- so each is permanently unreachable *after* its board precondition clears, which is why this stayed invisible behind "never available":
  - `policies.assign_governor` -- `CivSim_EmpireOrders_AssignGovernor(governorType, cityId)` (`lua/ingame/empire_orders.lua:230`) refuses `unknown_city` unless `type(cityId) == "number"`; only one positional is ever supplied, so `cityId` is nil forever. The selected-city id is **already resolved** by `resolve_selected_subject_target`, so the information exists and simply has no route.
  - `congress.cast_vote` -- `CivSim_Congress_CastVote(resolutionId, choiceId, votes)` (`lua/ingame/congress.lua:81`). **The body already documents this and refuses by name rather than guessing a side for the player**, which is the correct behaviour and should not be "fixed" by inventing a default. A ballot needs resolution, side and vote count; a single trailing `target` cannot express one.
  - `espionage.assign_mission` -- `CivSim_Espionage_AssignMission(spyUnitId, missionType, targetCityId)` (`lua/ingame/espionage.lua:103`); `missionType` arrives nil, so `GameInfo.UnitOperations[nil]` yields `unknown_mission` forever.
  **Scope: a parameter schema on the declaration, not a special case per action.** The three share one cause and must not be closed as three unrelated fixes. **Note what this reclassifies:** these are not class D ("not reachable yet at this stage"); the board precondition is a *separate* and also-unmet condition. **Four distinct causes were sharing one label, and separating them is what made this visible.**
