{
  "$defs": {
    "CameraMode": {
      "description": "contracts/capability-catalog.md ``view`` entries.",
      "enum": [
        "world",
        "strategic",
        "city_screen",
        "diplomacy",
        "congress"
      ],
      "title": "CameraMode",
      "type": "string"
    },
    "CameraRequirements": {
      "additionalProperties": false,
      "description": "Required on every ``view`` declaration (FR-024, FR-026).",
      "properties": {
        "mode": {
          "$ref": "#/$defs/CameraMode"
        },
        "target_must_be_revealed": {
          "title": "Target Must Be Revealed",
          "type": "boolean"
        },
        "zoom_range": {
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "type": "number"
            },
            {
              "type": "number"
            }
          ],
          "title": "Zoom Range",
          "type": "array"
        }
      },
      "required": [
        "mode",
        "zoom_range",
        "target_must_be_revealed"
      ],
      "title": "CameraRequirements",
      "type": "object"
    },
    "DeclarationKind": {
      "enum": [
        "observation",
        "view",
        "action"
      ],
      "title": "DeclarationKind",
      "type": "string"
    },
    "HudCorner": {
      "description": "A frame corner a view declares as holding the game's own HUD (T283).\n\nSee ``parity.screening._corner_overlay_is_suspect`` for what the content gate does with it:\na declared corner is not exempt, it is judged against the *other declared HUD corners*\ninstead of against the whole frame, which is what makes the detector sharper rather than\nmore permissive.",
      "enum": [
        "top_left",
        "top_right",
        "bottom_left",
        "bottom_right"
      ],
      "title": "HudCorner",
      "type": "string"
    },
    "LuaContext": {
      "description": "Which Lua execution context a catalog entry or observation entry came from.\n\nShared by ``ObservationEntry.context`` (models/turn.py) and\n``ParityDeclaration.context`` (models/catalog.py) -- both name the same\ntwo contexts (contracts/capability-catalog.md), so it is defined once\nhere rather than twice.",
      "enum": [
        "GameCore_Tuner",
        "InGame"
      ],
      "title": "LuaContext",
      "type": "string"
    },
    "TargetKind": {
      "description": "What an action's single ``parameters.target`` names (T256, FR-020/FR-024).\n\nMEASURED (2026-09-21, attempts 4-5 of the first model-driven runs): the one ``target`` field\ncarries a unit id, a plot, a technology name or a prompt option depending on the action, and\nthe model sent the warrior's *unit id* as ``units.move_to``'s target sixteen times in a row\neven after the summary said ``{\"target\": {\"x\": .., \"y\": ..}}``. The declaration now says the\nkind, and ``agent/context.py`` renders one concrete example per action from it -- what a human\nsees as the shape of the command, no predicate text (Principle I / FR-024). Additive: an\naction without it renders as before. The alternative -- separate ``subject`` and ``target``\nparameters -- changes the predicate binder, the Lua argument convention and every unit/city\norder, and is recorded in tasks.md T256 for the owner's ruling rather than taken.",
      "enum": [
        "none",
        "plot",
        "unit_id",
        "city_id",
        "player_id",
        "resolution_id",
        "individual_id",
        "spy_id",
        "name",
        "option",
        "number"
      ],
      "title": "TargetKind",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "One observable, visual view, or action, with its human equivalent\n(data-model.md SS10; contracts/capability-catalog.md).\n\n``parity_basis`` is required and non-empty for every kind (the\nenforcement point for FR-016/FR-017). ``camera_requirements`` and\n``screening_profile`` are required exactly when ``kind == view`` and\nforbidden otherwise -- enforced below rather than left to the catalog\nloader alone, since a malformed declaration should never validate\nsuccessfully as this type in the first place.",
  "properties": {
    "availability_predicate": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Availability Predicate"
    },
    "camera_requirements": {
      "anyOf": [
        {
          "$ref": "#/$defs/CameraRequirements"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "capability_id": {
      "title": "Capability Id",
      "type": "string"
    },
    "context": {
      "$ref": "#/$defs/LuaContext"
    },
    "declaration_id": {
      "title": "Declaration Id",
      "type": "string"
    },
    "hud_corners": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/HudCorner"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Hud Corners"
    },
    "introduced_in_version": {
      "title": "Introduced In Version",
      "type": "string"
    },
    "kind": {
      "$ref": "#/$defs/DeclarationKind"
    },
    "output_schema": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Output Schema"
    },
    "parity_basis": {
      "minLength": 1,
      "title": "Parity Basis",
      "type": "string"
    },
    "screening_profile": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Screening Profile"
    },
    "summary": {
      "title": "Summary",
      "type": "string"
    },
    "target_hint": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Target Hint"
    },
    "target_kind": {
      "anyOf": [
        {
          "$ref": "#/$defs/TargetKind"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "verification_predicate": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Verification Predicate"
    }
  },
  "required": [
    "declaration_id",
    "kind",
    "summary",
    "parity_basis",
    "context",
    "capability_id",
    "introduced_in_version"
  ],
  "schemaVersion": 1,
  "title": "ParityDeclaration",
  "type": "object"
}
