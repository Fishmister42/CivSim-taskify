"""The ``MatchStore`` seam -- the only place this feature touches 002's port.

``port.py`` declares the read-only Protocol; ``fake.py`` is the local
Protocol-conformant fake this feature develops and tests against while
deliverable 3 (and 002's reference adapter) are built in parallel
(plan.md Complexity Tracking C2).
"""
