"""Shared record builders for the deliverable-3 store suites (003 tasks T015).

Every builder goes through ``.model_validate()`` so each fixture also exercises the model's own
validators -- the same discipline ``tests/contract/test_match_store_port.py`` applies.
"""
